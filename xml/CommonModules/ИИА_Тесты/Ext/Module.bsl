#Область СлужебныеПроцедурыИФункции

Функция ПолучитьКоличествоЗаписейРегистра(ИмяРегистра)
	Попытка
		Если ИмяРегистра = "ИИА_Чанки" Тогда
			Запрос = Новый Запрос("ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК Количество ИЗ РегистрСведений.ИИА_Чанки");
		ИначеЕсли ИмяРегистра = "ИИА_ТокенИндекс" Тогда
			Запрос = Новый Запрос("ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК Количество ИЗ РегистрСведений.ИИА_ТокенИндекс");
		Иначе
			Возврат 0;
		КонецЕсли;
		Выборка = Запрос.Выполнить().Выбрать();
		Выборка.Следующий();
		Возврат Выборка.Количество;
	Исключение
		Возврат -1;
	КонецПопытки;
КонецФункции

Функция СформироватьРезультатТеста(Успех, Сообщение, Детали = Неопределено)
	Результат = Новый Структура;
	Результат.Вставить("Успех", Успех);
	Результат.Вставить("Сообщение", Сообщение);
	Результат.Вставить("Детали", ?(Детали = Неопределено, Новый Массив, Детали));
	Возврат Результат;
КонецФункции

#КонецОбласти

#Область ПрограммныйИнтерфейс

// Запускает бесплатные тесты (без вызова ИИ). По умолчанию для run_tests.py.
//
// Возвращаемое значение:
//  Массив из Структура - каждый элемент: Успех, Сообщение, Детали, ИмяТеста
//
Функция ЗапуститьБесплатныеТесты() Экспорт
	Тесты = Новый Массив;
	Тесты.Добавить("ТестСохраненияИЧтенияКонтекстаDSL");
	Тесты.Добавить("ТестCreateReferenceSetFieldМеждуВызовами");
	Тесты.Добавить("ТестRunQuery");
	Тесты.Добавить("ТестИзоляцииСостоянияЗапрос1СПоДиалогам");
	Тесты.Добавить("ТестВалидацииDSL");
	Тесты.Добавить("ТестSaveToStorageLoadFromStorage");
	Тесты.Добавить("ТестIdempotencySaveToStorageOperationId");
	Тесты.Добавить("ТестРежимЗапрос1С");
	Тесты.Добавить("ТестGetMetadata");
	Тесты.Добавить("ТестGetObjectFieldsРегистр");
	Тесты.Добавить("ТестFindReferenceByName");
	Тесты.Добавить("ТестЛогированияДиалога");
	Тесты.Добавить("ТестСозданияОбъекта");
	Тесты.Добавить("ТестПромптаДополненияПлана");
	Тесты.Добавить("ТестКонтекстИзРегистраДляДополнения");
	Тесты.Добавить("ТестПромптаСозданияПланаСКонтекстом");
	Тесты.Добавить("ТестСобратьФактыПоследнихДействий");
	Тесты.Добавить("ТестАвтоГетМетадатаПриТаблицаНеНайдена");
	Тесты.Добавить("ТестСправкиЗапросовПоОшибке");
	Тесты.Добавить("ТестПромптаGetObjectFields");
	Тесты.Добавить("ТестСобратьНеудачныеОбъектыИзФактов");
	Тесты.Добавить("ТестПолучитьКлючПоследнейОшибкиИзФактов");
	Тесты.Добавить("ТестСозданиеИИзменениеОбъекта");
	Тесты.Добавить("ТестRAGНормализацияТекста");
	Тесты.Добавить("ТестОчисткиСущностейRAG");
	Тесты.Добавить("ТестКомпактногоФорматаRAG");
	Тесты.Добавить("ТестRAGОбъединяетСущностиСКлючевымиСловами");
	Тесты.Добавить("ТестЯвныйRunQueryБезОбъектаНеНормализуется");
	Тесты.Добавить("ТестGetMetadataНеПереписываетЯвныйRunQuery");
	Тесты.Добавить("ТестRAGПоискПоЗапросу");
	Тесты.Добавить("ТестRAGПоискСинонимов");
	Тесты.Добавить("ТестRAGЯдроПоиск");
	Тесты.Добавить("ТестЛимитТокеновНаЗапуск");
	Тесты.Добавить("ТестЯдроМетаданных");
	Тесты.Добавить("ТестЯдроМетаданныхУНФТекущаяБаза");
	Тесты.Добавить("ТестКонтрактDSL");
	Тесты.Добавить("ТестПромптТребуетOperationId");
	Тесты.Добавить("ТестКонтрактПровайдера");
	Тесты.Добавить("ТестСтратегияОтветаПровайдера");
	Тесты.Добавить("ТестDSLТребуетНативныйToolCalling");
	Тесты.Добавить("ТестПредставлениеОшибкиИнструмента");
	Тесты.Добавить("ТестСписокМоделейOpenAI");
	Тесты.Добавить("ТестCapabilityПроверка");
	Тесты.Добавить("ТестКонтрактПромптовDSLВерсия");
	Тесты.Добавить("ТестКонтрактПромптовДействия");
	Тесты.Добавить("ТестКонтрактПромптовСекции");
	Тесты.Добавить("ТестКонтрактStateMachine");
	Тесты.Добавить("ТестКонтрактSubgraphResult");
	Тесты.Добавить("ТестКонтрактTransitionRegistry");
	Тесты.Добавить("ТестКонтрактRuntimeState");
	Тесты.Добавить("ТестКонтрактDecisionLogRecord");
	Тесты.Добавить("ТестКонтрактDecisionLogPolicyRegistry");
	Тесты.Добавить("ТестКонтрактTracePolicyRegistry");
	Тесты.Добавить("ТестDecisionTimeline");
	Тесты.Добавить("ТестКонтрактRuntimeTrace");
	Тесты.Добавить("ТестКонтрактCheckpointHistory");
	Тесты.Добавить("ТестКонтрактCheckpointDiff");
	Тесты.Добавить("ТестResumeFromCheckpoint");
	Тесты.Добавить("ТестКонтрактМетрикОркестратора");
	Тесты.Добавить("ТестКонтрактМетрикОркестратораV2");
	Тесты.Добавить("ТестКонтрактReplaySafetyPolicy");
	Тесты.Добавить("ТестResumeFromCheckpointБлокируетNoReplay");
	Тесты.Добавить("ТестSideEffectLogХранитReplaySafety");
	Тесты.Добавить("ТестCheckpointReplaySafetyИмеетПриоритет");
	Тесты.Добавить("ТестКонтрактRuntimeJournalValidator");
	Тесты.Добавить("ТестFailureInvestigationAPI");
	Тесты.Добавить("ТестValidateИСummarizeSubgraphContract");
	Тесты.Добавить("ТестКонтрактRecoveryPolicy");
	Тесты.Добавить("ТестКонтрактExecuteSubgraphRouting");
	Тесты.Добавить("ТестDiscoverySubgraph");
	Тесты.Добавить("ТестPlanningSubgraph");
	Тесты.Добавить("ТестПолныйПутьApprovalApproveValidate");
	Тесты.Добавить("ТестDSLПостроительSetFieldFieldName");
	Тесты.Добавить("ТестКонтрактQuerySafetyGate");
	Тесты.Добавить("ТестКонтрактRAGПоля");
	Тесты.Добавить("ТестКонтрактAntiGiveup");
	Тесты.Добавить("ТестAntiGiveupБлокРаннегоShowInfo");
	Тесты.Добавить("ТестAntiGiveupRecoveryExhausted");
	Тесты.Добавить("ТестНормализацииStepFailedВRecoverable");
	Тесты.Добавить("ТестRecoveryResumeAfterCommittedWrite");
	Тесты.Добавить("ТестRecoveryResumeSameOperationId");
	Тесты.Добавить("ТестRecoveryAbortUnsafeWriteRetry");
	Тесты.Добавить("ТестRecoverySubgraphПереходВValidate");
	Тесты.Добавить("ТестRecoverySubgraphПереходВExecute");
	Тесты.Добавить("ТестRecoverySubgraphПереходВSummarize");
	Тесты.Добавить("ТестQuerySafetyGateВложенныеПоля");
	Тесты.Добавить("ТестВложенияТекстовыйФайл");
	Тесты.Добавить("ТестВложенияUTF8ТекстовыйФайл");
	Тесты.Добавить("ТестВложенияПустойТекстовыйФайл");
	Тесты.Добавить("ТестВложенияИзображение");
	Тесты.Добавить("ТестВложенияMXL");
	Тесты.Добавить("ТестВложенияXLSX");
	Тесты.Добавить("ТестВложенияХранятПолныйТекст");
	Тесты.Добавить("ТестПромптОграничиваетТекстВложений");
	Тесты.Добавить("ТестПоискВоВложениях");
	Тесты.Добавить("ТестКонтрактИнструментовВложений");
	Тесты.Добавить("ТестИсполнениеИнструментаВложения");
	Тесты.Добавить("ТестSkillsSelectorЗапрос1С");
		Тесты.Добавить("ТестSkillsБлокПопадаетВПромпт");
		Тесты.Добавить("ТестSkillsWarnВалидаторDSL");
		Тесты.Добавить("ТестWriteFlowЗаказПокупателяСТранзакционнымКлинапом");
		Тесты.Добавить("ТестSkillsПользовательскийЧерновик");
	Тесты.Добавить("ТестSkillsПользовательскийLifecycle");
	Тесты.Добавить("ТестSkillsJSONLifecycle");
	Тесты.Добавить("ТестSkillsStrictPolicyBlocksForbidden");
	Тесты.Добавить("ТестSkillsJSONПереносимостьИНетриггер");
	Тесты.Добавить("ТестDSLПостроительCreateDocumentFieldsWrite");
	Тесты.Добавить("ТестSkillsГенерацияИзСистемнойКарточки");
	Тесты.Добавить("ТестSkillsГенераторИщетОбъектПоМетаданным");
	Тесты.Добавить("ТестSubmitDSLБезОберткиDSL");
	Тесты.Добавить("ТестSubmitDSLВосстанавливаетсяИзПлана");
	Тесты.Добавить("ТестWriteПропускаетПовторПослеCommittedSideEffect");
	Тесты.Добавить("ТестWriteДокументаЗаполняетДатуПоУмолчанию");
	Тесты.Добавить("ТестНативнаяПодготовкаImagePdf");
	Тесты.Добавить("ТестВложенияDOCX");
	Тесты.Добавить("ТестСообщенияДиалогаОчищаютсяДляXDTO");
	Тесты.Добавить("ТестПромптВключаетКонтекстВложений");
	Возврат ЗапуститьНаборТестов(Тесты);
КонецФункции

// Запускает тесты с реальным вызовом ИИ (медленные, требуют API).
//
// Возвращаемое значение:
//  Массив из Структура - каждый элемент: Успех, Сообщение, Детали, ИмяТеста
//
Функция ЗапуститьТестыСИИ() Экспорт
	Тесты = Новый Массив;
	Тесты.Добавить("ТестИзвлечьСущностиДляRAG");
	Тесты.Добавить("ТестВызовИИГенерацияDSL");
	Тесты.Добавить("ТестАгентЗапрос1С");
	Тесты.Добавить("ТестАгентСоздатьКонтрагента");
	Тесты.Добавить("ТестСоздатьДиалогИВыполнитьАгентаСинхронно");
	Возврат ЗапуститьНаборТестов(Тесты);
КонецФункции

// Запускает тесты в режиме холостого хода (без реального вызова ИИ, с mock-ответами).
//
// Возвращаемое значение:
//  Массив из Структура - каждый элемент: Успех, Сообщение, Детали, ИмяТеста
//
Функция ЗапуститьТестыХолостойХод() Экспорт
	Тесты = Новый Массив;
	Тесты.Добавить("ТестПромптаДополненияПлана");
	Тесты.Добавить("ТестЛоговПослеДополненияПлана");
	Тесты.Добавить("ТестДополнениеПланаСКонтекстомОбъекта");
	Тесты.Добавить("ТестКонтекстИзРегистраДляДополнения");
	Тесты.Добавить("ТестПромптаСозданияПланаСКонтекстом");
	Тесты.Добавить("ТестСобратьФактыПоследнихДействий");
	Тесты.Добавить("ТестАвтоГетМетадатаПриТаблицаНеНайдена");
	Тесты.Добавить("ТестСправкиЗапросовПоОшибке");
	Тесты.Добавить("ТестПромптаGetObjectFields");
	Тесты.Добавить("ТестСобратьНеудачныеОбъектыИзФактов");
	Тесты.Добавить("ТестПолучитьКлючПоследнейОшибкиИзФактов");
	Тесты.Добавить("ТестСозданиеИИзменениеОбъекта");
	Тесты.Добавить("ТестПолныйЦиклАгентаХолостойХод");
	Возврат ЗапуститьНаборТестов(Тесты);
КонецФункции

// Запускает все тесты (бесплатные + с вызовом ИИ).
//
// Возвращаемое значение:
//  Массив из Структура - каждый элемент: Успех, Сообщение, Детали, ИмяТеста
//
Функция ЗапуститьВсеТесты() Экспорт
	Тесты = Новый Массив;
	Тесты.Добавить("ТестСохраненияИЧтенияКонтекстаDSL");
	Тесты.Добавить("ТестCreateReferenceSetFieldМеждуВызовами");
	Тесты.Добавить("ТестRunQuery");
	Тесты.Добавить("ТестИзоляцииСостоянияЗапрос1СПоДиалогам");
	Тесты.Добавить("ТестВалидацииDSL");
	Тесты.Добавить("ТестSaveToStorageLoadFromStorage");
	Тесты.Добавить("ТестIdempotencySaveToStorageOperationId");
	Тесты.Добавить("ТестРежимЗапрос1С");
	Тесты.Добавить("ТестGetMetadata");
	Тесты.Добавить("ТестFindReferenceByName");
	Тесты.Добавить("ТестЛогированияДиалога");
	Тесты.Добавить("ТестСозданияОбъекта");
	Тесты.Добавить("ТестКонтрактDSL");
	Тесты.Добавить("ТестПромптТребуетOperationId");
	Тесты.Добавить("ТестКонтрактПровайдера");
	Тесты.Добавить("ТестИзвлечьСущностиДляRAG");
	Тесты.Добавить("ТестВызовИИГенерацияDSL");
	Тесты.Добавить("ТестАгентЗапрос1С");
	Тесты.Добавить("ТестАгентСоздатьКонтрагента");
	Тесты.Добавить("ТестСоздатьДиалогИВыполнитьАгентаСинхронно");
	Возврат ЗапуститьНаборТестов(Тесты);
КонецФункции

Функция ЗапуститьНаборТестов(Тесты)
	Результаты = Новый Массив;
	Для Каждого ИмяТеста Из Тесты Цикл
		РезультатТеста = ВыполнитьТест(ИмяТеста);
		РезультатТеста.Вставить("ИмяТеста", ИмяТеста);
		Результаты.Добавить(РезультатТеста);
	КонецЦикла;
	Возврат Результаты;
КонецФункции

Функция ВыполнитьТест(ИмяТеста)
	Попытка
		Если ИмяТеста = "ТестСохраненияИЧтенияКонтекстаDSL" Тогда
			Возврат ТестСохраненияИЧтенияКонтекстаDSL();
		ИначеЕсли ИмяТеста = "ТестCreateReferenceSetFieldМеждуВызовами" Тогда
			Возврат ТестCreateReferenceSetFieldМеждуВызовами();
		ИначеЕсли ИмяТеста = "ТестRunQuery" Тогда
			Возврат ТестRunQuery();
		ИначеЕсли ИмяТеста = "ТестИзоляцииСостоянияЗапрос1СПоДиалогам" Тогда
			Возврат ТестИзоляцииСостоянияЗапрос1СПоДиалогам();
		ИначеЕсли ИмяТеста = "ТестВалидацииDSL" Тогда
			Возврат ТестВалидацииDSL();
		ИначеЕсли ИмяТеста = "ТестSaveToStorageLoadFromStorage" Тогда
			Возврат ТестSaveToStorageLoadFromStorage();
		ИначеЕсли ИмяТеста = "ТестIdempotencySaveToStorageOperationId" Тогда
			Возврат ТестIdempotencySaveToStorageOperationId();
		ИначеЕсли ИмяТеста = "ТестРежимЗапрос1С" Тогда
			Возврат ТестРежимЗапрос1С();
		ИначеЕсли ИмяТеста = "ТестСоздатьДиалогИВыполнитьАгентаСинхронно" Тогда
			Возврат ТестСоздатьДиалогИВыполнитьАгентаСинхронно();
		ИначеЕсли ИмяТеста = "ТестИзвлечьСущностиДляRAG" Тогда
			Возврат ТестИзвлечьСущностиДляRAG();
		ИначеЕсли ИмяТеста = "ТестВызовИИГенерацияDSL" Тогда
			Возврат ТестВызовИИГенерацияDSL();
		ИначеЕсли ИмяТеста = "ТестАгентЗапрос1С" Тогда
			Возврат ТестАгентЗапрос1С();
		ИначеЕсли ИмяТеста = "ТестАгентСоздатьКонтрагента" Тогда
			Возврат ТестАгентСоздатьКонтрагента();
		ИначеЕсли ИмяТеста = "ТестGetMetadata" Тогда
			Возврат ТестGetMetadata();
		ИначеЕсли ИмяТеста = "ТестGetObjectFieldsРегистр" Тогда
			Возврат ТестGetObjectFieldsРегистр();
		ИначеЕсли ИмяТеста = "ТестFindReferenceByName" Тогда
			Возврат ТестFindReferenceByName();
		ИначеЕсли ИмяТеста = "ТестЛогированияДиалога" Тогда
			Возврат ТестЛогированияДиалога();
		ИначеЕсли ИмяТеста = "ТестСозданияОбъекта" Тогда
			Возврат ТестСозданияОбъекта();
		ИначеЕсли ИмяТеста = "ТестПромптаДополненияПлана" Тогда
			Возврат ТестПромптаДополненияПлана();
		ИначеЕсли ИмяТеста = "ТестЛоговПослеДополненияПлана" Тогда
			Возврат ТестЛоговПослеДополненияПлана();
		ИначеЕсли ИмяТеста = "ТестДополнениеПланаСКонтекстомОбъекта" Тогда
			Возврат ТестДополнениеПланаСКонтекстомОбъекта();
		ИначеЕсли ИмяТеста = "ТестКонтекстИзРегистраДляДополнения" Тогда
			Возврат ТестКонтекстИзРегистраДляДополнения();
		ИначеЕсли ИмяТеста = "ТестПромптаСозданияПланаСКонтекстом" Тогда
			Возврат ТестПромптаСозданияПланаСКонтекстом();
		ИначеЕсли ИмяТеста = "ТестСобратьФактыПоследнихДействий" Тогда
			Возврат ТестСобратьФактыПоследнихДействий();
		ИначеЕсли ИмяТеста = "ТестАвтоГетМетадатаПриТаблицаНеНайдена" Тогда
			Возврат ТестАвтоГетМетадатаПриТаблицаНеНайдена();
		ИначеЕсли ИмяТеста = "ТестСправкиЗапросовПоОшибке" Тогда
			Возврат ТестСправкиЗапросовПоОшибке();
		ИначеЕсли ИмяТеста = "ТестПромптаGetObjectFields" Тогда
			Возврат ТестПромптаGetObjectFields();
		ИначеЕсли ИмяТеста = "ТестСобратьНеудачныеОбъектыИзФактов" Тогда
			Возврат ТестСобратьНеудачныеОбъектыИзФактов();
		ИначеЕсли ИмяТеста = "ТестПолучитьКлючПоследнейОшибкиИзФактов" Тогда
			Возврат ТестПолучитьКлючПоследнейОшибкиИзФактов();
		ИначеЕсли ИмяТеста = "ТестСозданиеИИзменениеОбъекта" Тогда
			Возврат ТестСозданиеИИзменениеОбъекта();
		ИначеЕсли ИмяТеста = "ТестПолныйЦиклАгентаХолостойХод" Тогда
			Возврат ТестПолныйЦиклАгентаХолостойХод();
		ИначеЕсли ИмяТеста = "ТестRAGНормализацияТекста" Тогда
			Возврат ТестRAGНормализацияТекста();
		ИначеЕсли ИмяТеста = "ТестОчисткиСущностейRAG" Тогда
			Возврат ТестОчисткиСущностейRAG();
		ИначеЕсли ИмяТеста = "ТестКомпактногоФорматаRAG" Тогда
			Возврат ТестКомпактногоФорматаRAG();
		ИначеЕсли ИмяТеста = "ТестRAGОбъединяетСущностиСКлючевымиСловами" Тогда
			Возврат ТестRAGОбъединяетСущностиСКлючевымиСловами();
		ИначеЕсли ИмяТеста = "ТестЯвныйRunQueryБезОбъектаНеНормализуется" Тогда
			Возврат ТестЯвныйRunQueryБезОбъектаНеНормализуется();
		ИначеЕсли ИмяТеста = "ТестGetMetadataНеПереписываетЯвныйRunQuery" Тогда
			Возврат ТестGetMetadataНеПереписываетЯвныйRunQuery();
		ИначеЕсли ИмяТеста = "ТестWriteПропускаетПовторПослеCommittedSideEffect" Тогда
			Возврат ТестWriteПропускаетПовторПослеCommittedSideEffect();
		ИначеЕсли ИмяТеста = "ТестWriteДокументаЗаполняетДатуПоУмолчанию" Тогда
			Возврат ТестWriteДокументаЗаполняетДатуПоУмолчанию();
		ИначеЕсли ИмяТеста = "ТестRAGПоискПоЗапросу" Тогда
			Возврат ТестRAGПоискПоЗапросу();
		ИначеЕсли ИмяТеста = "ТестRAGПоискСинонимов" Тогда
			Возврат ТестRAGПоискСинонимов();
		ИначеЕсли ИмяТеста = "ТестRAGЯдроПоиск" Тогда
			Возврат ТестRAGЯдроПоиск();
		ИначеЕсли ИмяТеста = "ТестЛимитТокеновНаЗапуск" Тогда
			Возврат ТестЛимитТокеновНаЗапуск();
		ИначеЕсли ИмяТеста = "ТестЯдроМетаданных" Тогда
			Возврат ТестЯдроМетаданных();
		ИначеЕсли ИмяТеста = "ТестЯдроМетаданныхУНФТекущаяБаза" Тогда
			Возврат ТестЯдроМетаданныхУНФТекущаяБаза();
		ИначеЕсли ИмяТеста = "ТестКонтрактDSL" Тогда
			Возврат ТестКонтрактDSL();
		ИначеЕсли ИмяТеста = "ТестПромптТребуетOperationId" Тогда
			Возврат ТестПромптТребуетOperationId();
		ИначеЕсли ИмяТеста = "ТестКонтрактПровайдера" Тогда
			Возврат ТестКонтрактПровайдера();
		ИначеЕсли ИмяТеста = "ТестСтратегияОтветаПровайдера" Тогда
			Возврат ТестСтратегияОтветаПровайдера();
		ИначеЕсли ИмяТеста = "ТестDSLТребуетНативныйToolCalling" Тогда
			Возврат ТестDSLТребуетНативныйToolCalling();
		ИначеЕсли ИмяТеста = "ТестПредставлениеОшибкиИнструмента" Тогда
			Возврат ТестПредставлениеОшибкиИнструмента();
		ИначеЕсли ИмяТеста = "ТестСписокМоделейOpenAI" Тогда
			Возврат ТестСписокМоделейOpenAI();
		ИначеЕсли ИмяТеста = "ТестCapabilityПроверка" Тогда
			Возврат ТестCapabilityПроверка();
		ИначеЕсли ИмяТеста = "ТестКонтрактПромптовDSLВерсия" Тогда
			Возврат ТестКонтрактПромптовDSLВерсия();
		ИначеЕсли ИмяТеста = "ТестКонтрактПромптовДействия" Тогда
			Возврат ТестКонтрактПромптовДействия();
		ИначеЕсли ИмяТеста = "ТестКонтрактПромптовСекции" Тогда
			Возврат ТестКонтрактПромптовСекции();
		ИначеЕсли ИмяТеста = "ТестКонтрактStateMachine" Тогда
			Возврат ТестКонтрактStateMachine();
		ИначеЕсли ИмяТеста = "ТестКонтрактSubgraphResult" Тогда
			Возврат ТестКонтрактSubgraphResult();
		ИначеЕсли ИмяТеста = "ТестКонтрактTransitionRegistry" Тогда
			Возврат ТестКонтрактTransitionRegistry();
		ИначеЕсли ИмяТеста = "ТестКонтрактRuntimeState" Тогда
			Возврат ТестКонтрактRuntimeState();
		ИначеЕсли ИмяТеста = "ТестКонтрактDecisionLogRecord" Тогда
			Возврат ТестКонтрактDecisionLogRecord();
		ИначеЕсли ИмяТеста = "ТестКонтрактDecisionLogPolicyRegistry" Тогда
			Возврат ТестКонтрактDecisionLogPolicyRegistry();
		ИначеЕсли ИмяТеста = "ТестКонтрактTracePolicyRegistry" Тогда
			Возврат ТестКонтрактTracePolicyRegistry();
		ИначеЕсли ИмяТеста = "ТестDecisionTimeline" Тогда
			Возврат ТестDecisionTimeline();
		ИначеЕсли ИмяТеста = "ТестКонтрактRuntimeTrace" Тогда
			Возврат ТестКонтрактRuntimeTrace();
		ИначеЕсли ИмяТеста = "ТестКонтрактCheckpointHistory" Тогда
			Возврат ТестКонтрактCheckpointHistory();
		ИначеЕсли ИмяТеста = "ТестКонтрактCheckpointDiff" Тогда
			Возврат ТестКонтрактCheckpointDiff();
		ИначеЕсли ИмяТеста = "ТестResumeFromCheckpoint" Тогда
			Возврат ТестResumeFromCheckpoint();
		ИначеЕсли ИмяТеста = "ТестКонтрактМетрикОркестратора" Тогда
			Возврат ТестКонтрактМетрикОркестратора();
		ИначеЕсли ИмяТеста = "ТестКонтрактМетрикОркестратораV2" Тогда
			Возврат ТестКонтрактМетрикОркестратораV2();
		ИначеЕсли ИмяТеста = "ТестКонтрактReplaySafetyPolicy" Тогда
			Возврат ТестКонтрактReplaySafetyPolicy();
		ИначеЕсли ИмяТеста = "ТестResumeFromCheckpointБлокируетNoReplay" Тогда
			Возврат ТестResumeFromCheckpointБлокируетNoReplay();
		ИначеЕсли ИмяТеста = "ТестSideEffectLogХранитReplaySafety" Тогда
			Возврат ТестSideEffectLogХранитReplaySafety();
		ИначеЕсли ИмяТеста = "ТестCheckpointReplaySafetyИмеетПриоритет" Тогда
			Возврат ТестCheckpointReplaySafetyИмеетПриоритет();
		ИначеЕсли ИмяТеста = "ТестКонтрактRuntimeJournalValidator" Тогда
			Возврат ТестКонтрактRuntimeJournalValidator();
		ИначеЕсли ИмяТеста = "ТестFailureInvestigationAPI" Тогда
			Возврат ТестFailureInvestigationAPI();
		ИначеЕсли ИмяТеста = "ТестValidateИСummarizeSubgraphContract" Тогда
			Возврат ТестValidateИСummarizeSubgraphContract();
		ИначеЕсли ИмяТеста = "ТестКонтрактRecoveryPolicy" Тогда
			Возврат ТестКонтрактRecoveryPolicy();
		ИначеЕсли ИмяТеста = "ТестКонтрактExecuteSubgraphRouting" Тогда
			Возврат ТестКонтрактExecuteSubgraphRouting();
		ИначеЕсли ИмяТеста = "ТестDiscoverySubgraph" Тогда
			Возврат ТестDiscoverySubgraph();
		ИначеЕсли ИмяТеста = "ТестPlanningSubgraph" Тогда
			Возврат ТестPlanningSubgraph();
		ИначеЕсли ИмяТеста = "ТестПолныйПутьApprovalApproveValidate" Тогда
			Возврат ТестПолныйПутьApprovalApproveValidate();
		ИначеЕсли ИмяТеста = "ТестDSLПостроительSetFieldFieldName" Тогда
			Возврат ТестDSLПостроительSetFieldFieldName();
		ИначеЕсли ИмяТеста = "ТестКонтрактQuerySafetyGate" Тогда
			Возврат ТестКонтрактQuerySafetyGate();
		ИначеЕсли ИмяТеста = "ТестКонтрактRAGПоля" Тогда
			Возврат ТестКонтрактRAGПоля();
		ИначеЕсли ИмяТеста = "ТестКонтрактAntiGiveup" Тогда
			Возврат ТестКонтрактAntiGiveup();
		ИначеЕсли ИмяТеста = "ТестAntiGiveupБлокРаннегоShowInfo" Тогда
			Возврат ТестAntiGiveupБлокРаннегоShowInfo();
		ИначеЕсли ИмяТеста = "ТестAntiGiveupRecoveryExhausted" Тогда
			Возврат ТестAntiGiveupRecoveryExhausted();
		ИначеЕсли ИмяТеста = "ТестНормализацииStepFailedВRecoverable" Тогда
			Возврат ТестНормализацииStepFailedВRecoverable();
		ИначеЕсли ИмяТеста = "ТестRecoveryResumeAfterCommittedWrite" Тогда
			Возврат ТестRecoveryResumeAfterCommittedWrite();
		ИначеЕсли ИмяТеста = "ТестRecoveryResumeSameOperationId" Тогда
			Возврат ТестRecoveryResumeSameOperationId();
		ИначеЕсли ИмяТеста = "ТестRecoveryAbortUnsafeWriteRetry" Тогда
			Возврат ТестRecoveryAbortUnsafeWriteRetry();
		ИначеЕсли ИмяТеста = "ТестRecoverySubgraphПереходВValidate" Тогда
			Возврат ТестRecoverySubgraphПереходВValidate();
		ИначеЕсли ИмяТеста = "ТестRecoverySubgraphПереходВExecute" Тогда
			Возврат ТестRecoverySubgraphПереходВExecute();
		ИначеЕсли ИмяТеста = "ТестRecoverySubgraphПереходВSummarize" Тогда
			Возврат ТестRecoverySubgraphПереходВSummarize();
		ИначеЕсли ИмяТеста = "ТестQuerySafetyGateВложенныеПоля" Тогда
			Возврат ТестQuerySafetyGateВложенныеПоля();
		ИначеЕсли ИмяТеста = "ТестВложенияТекстовыйФайл" Тогда
			Возврат ТестВложенияТекстовыйФайл();
		ИначеЕсли ИмяТеста = "ТестВложенияUTF8ТекстовыйФайл" Тогда
			Возврат ТестВложенияUTF8ТекстовыйФайл();
		ИначеЕсли ИмяТеста = "ТестВложенияПустойТекстовыйФайл" Тогда
			Возврат ТестВложенияПустойТекстовыйФайл();
		ИначеЕсли ИмяТеста = "ТестВложенияИзображение" Тогда
			Возврат ТестВложенияИзображение();
		ИначеЕсли ИмяТеста = "ТестВложенияMXL" Тогда
			Возврат ТестВложенияMXL();
		ИначеЕсли ИмяТеста = "ТестВложенияXLSX" Тогда
			Возврат ТестВложенияXLSX();
		ИначеЕсли ИмяТеста = "ТестВложенияХранятПолныйТекст" Тогда
			Возврат ТестВложенияХранятПолныйТекст();
		ИначеЕсли ИмяТеста = "ТестПромптОграничиваетТекстВложений" Тогда
			Возврат ТестПромптОграничиваетТекстВложений();
		ИначеЕсли ИмяТеста = "ТестПоискВоВложениях" Тогда
			Возврат ТестПоискВоВложениях();
		ИначеЕсли ИмяТеста = "ТестКонтрактИнструментовВложений" Тогда
			Возврат ТестКонтрактИнструментовВложений();
		ИначеЕсли ИмяТеста = "ТестИсполнениеИнструментаВложения" Тогда
			Возврат ТестИсполнениеИнструментаВложения();
		ИначеЕсли ИмяТеста = "ТестSkillsSelectorЗапрос1С" Тогда
			Возврат ТестSkillsSelectorЗапрос1С();
		ИначеЕсли ИмяТеста = "ТестSkillsБлокПопадаетВПромпт" Тогда
			Возврат ТестSkillsБлокПопадаетВПромпт();
		ИначеЕсли ИмяТеста = "ТестSkillsWarnВалидаторDSL" Тогда
			Возврат ТестSkillsWarnВалидаторDSL();
		ИначеЕсли ИмяТеста = "ТестWriteFlowЗаказПокупателяСТранзакционнымКлинапом" Тогда
			Возврат ТестWriteFlowЗаказПокупателяСТранзакционнымКлинапом();
		ИначеЕсли ИмяТеста = "ТестSkillsПользовательскийЧерновик" Тогда
			Возврат ТестSkillsПользовательскийЧерновик();
		ИначеЕсли ИмяТеста = "ТестSkillsПользовательскийLifecycle" Тогда
			Возврат ТестSkillsПользовательскийLifecycle();
		ИначеЕсли ИмяТеста = "ТестSkillsJSONLifecycle" Тогда
			Возврат ТестSkillsJSONLifecycle();
		ИначеЕсли ИмяТеста = "ТестSkillsStrictPolicyBlocksForbidden" Тогда
			Возврат ТестSkillsStrictPolicyBlocksForbidden();
		ИначеЕсли ИмяТеста = "ТестSkillsJSONПереносимостьИНетриггер" Тогда
			Возврат ТестSkillsJSONПереносимостьИНетриггер();
		ИначеЕсли ИмяТеста = "ТестDSLПостроительCreateDocumentFieldsWrite" Тогда
			Возврат ТестDSLПостроительCreateDocumentFieldsWrite();
		ИначеЕсли ИмяТеста = "ТестSkillsГенерацияИзСистемнойКарточки" Тогда
			Возврат ТестSkillsГенерацияИзСистемнойКарточки();
		ИначеЕсли ИмяТеста = "ТестSkillsГенераторИщетОбъектПоМетаданным" Тогда
			Возврат ТестSkillsГенераторИщетОбъектПоМетаданным();
		ИначеЕсли ИмяТеста = "ТестSubmitDSLБезОберткиDSL" Тогда
			Возврат ТестSubmitDSLБезОберткиDSL();
		ИначеЕсли ИмяТеста = "ТестSubmitDSLВосстанавливаетсяИзПлана" Тогда
			Возврат ТестSubmitDSLВосстанавливаетсяИзПлана();
		ИначеЕсли ИмяТеста = "ТестНативнаяПодготовкаImagePdf" Тогда
			Возврат ТестНативнаяПодготовкаImagePdf();
		ИначеЕсли ИмяТеста = "ТестВложенияDOCX" Тогда
			Возврат ТестВложенияDOCX();
		ИначеЕсли ИмяТеста = "ТестСообщенияДиалогаОчищаютсяДляXDTO" Тогда
			Возврат ТестСообщенияДиалогаОчищаютсяДляXDTO();
		ИначеЕсли ИмяТеста = "ТестПромптВключаетКонтекстВложений" Тогда
			Возврат ТестПромптВключаетКонтекстВложений();
		Иначе
			Возврат СформироватьРезультатТеста(Ложь, "Неизвестный тест: " + ИмяТеста);
		КонецЕсли;
	Исключение
		Возврат СформироватьРезультатТеста(Ложь, "Ошибка вызова: " + ОписаниеОшибки());
	КонецПопытки;
КонецФункции

// Тест сохранения и чтения контекста DSL между шагами в рамках одного элемента справочника ИИА_Диалоги.
// Проверяет: запись в регистр ИИА_ДанныеДиалогов, чтение, восстановление ссылки и объекта, очистку контекста.
//
// Возвращаемое значение:
//  Структура:
//   * Успех - Булево - успешность теста
//   * Сообщение - Строка - краткое сообщение
//   * Детали - Массив из Строка - этапы выполнения
//
Функция ТестСохраненияИЧтенияКонтекстаDSL() Экспорт

	Результат = Новый Структура;
	Результат.Вставить("Успех", Ложь);
	Результат.Вставить("Сообщение", "");
	Результат.Вставить("Детали", Новый Массив);

	Попытка

		// 1. Создать диалог
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Результат.Детали.Добавить("Создан диалог: " + СсылкаДиалога);

		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// 2. Подготовить контекст DSL
		СтруктураДанных = Новый Структура;
		СтруктураДанных.Вставить("DSL_СсылкаОбъекта", СсылкаДиалога);
		СтруктураДанных.Вставить("DSL_ТипОбъекта", "Справочник");
		СтруктураДанных.Вставить("DSL_ИмяОбъекта", "ИИА_Диалоги");
		Результат.Детали.Добавить("Подготовлен контекст: DSL_СсылкаОбъекта, DSL_ТипОбъекта, DSL_ИмяОбъекта");

		// 3. Сохранить в регистр
		ИИА_DialogStorage.ЗаписатьДанныеДиалогаВРегистр(СсылкаДиалога, СтруктураДанных);
		Результат.Детали.Добавить("Контекст записан в регистр ИИА_ДанныеДиалогов");

		// 4. Прочитать из регистра
		Прочитан = ИИА_DialogStorage.ПолучитьДанныеДиалогаИзРегистра(СсылкаДиалога);
		Результат.Детали.Добавить("Контекст прочитан из регистра");

		// 5. Проверки
		Если Прочитан = Неопределено ИЛИ ТипЗнч(Прочитан) <> Тип("Структура") Тогда
			Результат.Сообщение = "Прочитанные данные не являются структурой";
			Возврат Результат;
		КонецЕсли;

		Если НЕ Прочитан.Свойство("DSL_СсылкаОбъекта") Тогда
			Результат.Сообщение = "Ключ DSL_СсылкаОбъекта отсутствует в прочитанных данных";
			Возврат Результат;
		КонецЕсли;

		Если НЕ ЗначениеЗаполнено(Прочитан.DSL_СсылкаОбъекта) Тогда
			Результат.Сообщение = "DSL_СсылкаОбъекта пустой";
			Возврат Результат;
		КонецЕсли;

		Если Прочитан.DSL_СсылкаОбъекта <> СсылкаДиалога Тогда
			Результат.Сообщение = "DSL_СсылкаОбъекта не совпадает с сохранённой ссылкой";
			Возврат Результат;
		КонецЕсли;

		Если НЕ Прочитан.Свойство("DSL_ТипОбъекта") ИЛИ Прочитан.DSL_ТипОбъекта <> "Справочник" Тогда
			Результат.Сообщение = "DSL_ТипОбъекта не совпадает (ожидается 'Справочник')";
			Возврат Результат;
		КонецЕсли;

		Если НЕ Прочитан.Свойство("DSL_ИмяОбъекта") ИЛИ Прочитан.DSL_ИмяОбъекта <> "ИИА_Диалоги" Тогда
			Результат.Сообщение = "DSL_ИмяОбъекта не совпадает (ожидается 'ИИА_Диалоги')";
			Возврат Результат;
		КонецЕсли;

		Объект = Прочитан.DSL_СсылкаОбъекта.ПолучитьОбъект();
		Если Объект = Неопределено Тогда
			Результат.Сообщение = "Не удалось получить объект по DSL_СсылкаОбъекта";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("Проверки пройдены: ссылка, тип, имя, объект восстановлен");

		// 6. Очистить контекст
		ИИА_Сервер.ОчиститьКонтекстDSLДиалога(СсылкаДиалога);
		Результат.Детали.Добавить("Контекст DSL очищен");

		// 7. Проверить очистку
		ПослеОчистки = ИИА_DialogStorage.ПолучитьДанныеДиалогаИзРегистра(СсылкаДиалога);
		Если ПослеОчистки <> Неопределено И ТипЗнч(ПослеОчистки) = Тип("Структура") И ПослеОчистки.Свойство("DSL_СсылкаОбъекта") Тогда
			Результат.Сообщение = "DSL_СсылкаОбъекта остался после очистки";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("Очистка проверена: DSL_СсылкаОбъекта отсутствует");

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден успешно";

	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;

	Возврат Результат;

КонецФункции

// Тест цепочки CreateReference → SetField между двумя вызовами DSL (контекст передаётся через регистр).
//
Функция ТестCreateReferenceSetFieldМеждуВызовами() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Результат.Детали.Добавить("Создан диалог: " + СсылкаДиалога);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// Вызов 1: CreateReference + SetField + Write (Write нужен для получения валидной ссылки в контексте)
		DSL1 = "{""steps"":[{""action"":""CreateReference"",""object_name"":""Контрагенты""},{""action"":""SetField"",""field_name"":""Наименование"",""value"":""Тест_Контекст""},{""action"":""Write""}]}";
		Рез1 = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSL1, СсылкаДиалога);
		Результат.Детали.Добавить("Вызов 1 CreateReference+SetField+Write: Успех=" + Рез1.Успех);
		Если НЕ Рез1.Успех Тогда
			Результат.Сообщение = "CreateReference не выполнен: " + Рез1.Сообщение;
			Возврат Результат;
		КонецЕсли;

		// Вызов 2: SetField (контекст должен восстановиться из регистра)
		DSL2 = "{""steps"":[{""action"":""SetField"",""field_name"":""Наименование"",""value"":""Тест_Обновлено""}]}";
		Рез2 = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSL2, СсылкаДиалога);
		Результат.Детали.Добавить("Вызов 2 SetField: Успех=" + Рез2.Успех);
		Если НЕ Рез2.Успех Тогда
			Результат.Сообщение = "SetField не выполнен (контекст не восстановился?): " + Рез2.Сообщение;
			Возврат Результат;
		КонецЕсли;

		// Вызов 3: Write для сохранения
		DSL3 = "{""steps"":[{""action"":""Write""}]}";
		Рез3 = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSL3, СсылкаДиалога);
		Результат.Детали.Добавить("Вызов 3 Write: Успех=" + Рез3.Успех);
		Если НЕ Рез3.Успех Тогда
			Результат.Сообщение = "Write не выполнен: " + Рез3.Сообщение;
			Возврат Результат;
		КонецЕсли;

		ИИА_Сервер.ОчиститьКонтекстDSLДиалога(СсылкаДиалога);
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: CreateReference → SetField → Write между вызовами";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест выполнения простого запроса RunQuery.
//
Функция ТестRunQuery() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		DSL = "{""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ 1 КАК Н""}]}";
		Рез = ИИА_DSL.ВыполнитьDSL(DSL, Неопределено, Ложь);
		Результат.Детали.Добавить("Выполнен RunQuery ВЫБРАТЬ 1 КАК Н");
		Если НЕ Рез.Успех Тогда
			Результат.Сообщение = "RunQuery не выполнен: " + Рез.Сообщение;
			Возврат Результат;
		КонецЕсли;
		Если Рез.Результаты.Количество() = 0 Тогда
			Результат.Сообщение = "Нет результатов шага";
			Возврат Результат;
		КонецЕсли;
		РезультатШага = Рез.Результаты[0];
		Если НЕ РезультатШага.Свойство("ExecutedQuery") Или ПустаяСтрока(СокрЛП(РезультатШага.ExecutedQuery)) Тогда
			Результат.Сообщение = "RunQuery должен возвращать ExecutedQuery для формы запроса 1С";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Рез.Свойство("ПоследнийЗапрос1С")
			Или ПустаяСтрока(СокрЛП(Рез.ПоследнийЗапрос1С.ТекстЗапроса)) Тогда
			Результат.Сообщение = "DSL должен сохранять ПоследнийЗапрос1С.ТекстЗапроса";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: RunQuery выполнен";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест изоляции состояния формы Запрос1С между разными диалогами.
//
Функция ТестИзоляцииСостоянияЗапрос1СПоДиалогам() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Диалог1 = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		Диалог2 = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		Запрос1 = "ВЫБРАТЬ 101 КАК ПроверкаДиалога";
		Запрос2 = "ВЫБРАТЬ 202 КАК ПроверкаДиалога";

		ИИА_Сервер.СохранитьЧерновикЗапроса1С(Диалог1, Запрос1, "");
		ИИА_Сервер.СохранитьЧерновикЗапроса1С(Диалог2, Запрос2, "");

		Состояние1 = ИИА_Сервер.ПолучитьСостояниеЗапроса1С(Диалог1);
		Состояние2 = ИИА_Сервер.ПолучитьСостояниеЗапроса1С(Диалог2);

		Если Состояние1.ТекстЗапроса <> Запрос1 Тогда
			Результат.Сообщение = "Первый диалог получил чужой текст запроса: " + Состояние1.ТекстЗапроса;
			Возврат Результат;
		КонецЕсли;
		Если Состояние2.ТекстЗапроса <> Запрос2 Тогда
			Результат.Сообщение = "Второй диалог получил чужой текст запроса: " + Состояние2.ТекстЗапроса;
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: состояние Запрос1С изолировано по ссылке диалога";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест валидации DSL (ПроверитьDSLСценарий).
//
Функция ТестВалидацииDSL() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		// Корректный DSL
		ВалидныйDSL = "{""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ 1""}]}";
		РезВалид = ИИА_Сервер.ПроверитьDSLСценарий(ВалидныйDSL);
		Результат.Детали.Добавить("Валидный DSL: Успех=" + РезВалид.Успех);
		Если НЕ РезВалид.Успех Тогда
			Результат.Сообщение = "Валидный DSL не прошёл: " + РезВалид.Сообщение;
			Возврат Результат;
		КонецЕсли;

		// CreateReference без object_name
		НевалидныйDSL = "{""steps"":[{""action"":""CreateReference""}]}";
		РезНевалид = ИИА_Сервер.ПроверитьDSLСценарий(НевалидныйDSL);
		Результат.Детали.Добавить("CreateReference без object_name: Успех=" + РезНевалид.Успех + " (ожидается Ложь)");
		Если РезНевалид.Успех Тогда
			Результат.Сообщение = "Невалидный DSL прошёл валидацию";
			Возврат Результат;
		КонецЕсли;

		// Неизвестное действие
		НеизвестныйDSL = "{""steps"":[{""action"":""UnknownAction""}]}";
		РезНеизв = ИИА_Сервер.ПроверитьDSLСценарий(НеизвестныйDSL);
		Результат.Детали.Добавить("Неизвестное действие: Успех=" + РезНеизв.Успех + " (ожидается Ложь)");
		Если РезНеизв.Успех Тогда
			Результат.Сообщение = "Неизвестное действие прошло валидацию";
			Возврат Результат;
		КонецЕсли;

		// GetObjectFields для регистра накопления (поддержка регистров)
		DSLРегистр = "{""steps"":[{""action"":""GetObjectFields"",""object_type"":""РегистрНакопления"",""object_name"":""ЗапасыНаСкладах""}]}";
		РезРегистр = ИИА_Сервер.ПроверитьDSLСценарий(DSLРегистр);
		Результат.Детали.Добавить("GetObjectFields РегистрНакопления: Успех=" + РезРегистр.Успех + " (ожидается Истина)");
		Если НЕ РезРегистр.Успех Тогда
			Результат.Сообщение = "GetObjectFields с РегистрНакопления должен проходить валидацию: " + РезРегистр.Сообщение;
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: валидация DSL работает";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест SaveToStorage и LoadFromStorage через DSL.
//
Функция ТестSaveToStorageLoadFromStorage() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Результат.Детали.Добавить("Создан диалог");
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		КлючТест = "ТестКлюч_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		ЗначениеТест = "Значение_" + КлючТест;

		ЗначениеЭкранир = СтрЗаменить(ЗначениеТест, "\", "\\");
		ЗначениеЭкранир = СтрЗаменить(ЗначениеЭкранир, """", "\""");
		DSLЗапись = "{""steps"":[{""action"":""SaveToStorage"",""key"":""" + КлючТест + """,""data"":""" + ЗначениеЭкранир + """}]}";
		РезЗапись = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSLЗапись, СсылкаДиалога);
		Результат.Детали.Добавить("SaveToStorage: Успех=" + РезЗапись.Успех);
		Если НЕ РезЗапись.Успех Тогда
			Результат.Сообщение = "SaveToStorage не выполнен: " + РезЗапись.Сообщение;
			Возврат Результат;
		КонецЕсли;

		DSLЧтение = "{""steps"":[{""action"":""LoadFromStorage"",""key"":""" + КлючТест + """}]}";
		РезЧтение = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSLЧтение, СсылкаДиалога);
		Результат.Детали.Добавить("LoadFromStorage: Успех=" + РезЧтение.Успех);
		Если НЕ РезЧтение.Успех Тогда
			Результат.Сообщение = "LoadFromStorage не выполнен: " + РезЧтение.Сообщение;
			Возврат Результат;
		КонецЕсли;
		Если РезЧтение.Результаты.Количество() > 0 Тогда
			РезШага = РезЧтение.Результаты[0];
			Если РезШага.Свойство("Данные") И Строка(РезШага.Данные) = ЗначениеТест Тогда
				Результат.Детали.Добавить("Прочитанное значение совпадает");
			КонецЕсли;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: SaveToStorage/LoadFromStorage";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тестирует safe retry для SaveToStorage по operation_id.
//
Функция ТестIdempotencySaveToStorageOperationId() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог для проверки idempotency";
			Возврат Результат;
		КонецЕсли;

		КлючТеста = "IdempotencyKey_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		ЗначениеТеста = "Value_" + КлючТеста;
		OperationId = "op_save_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");

		ЗначениеЭкранир = СтрЗаменить(ЗначениеТеста, "\", "\\");
		ЗначениеЭкранир = СтрЗаменить(ЗначениеЭкранир, """", "\""");
		DSL = "{""dsl_version"":2,""steps"":[{""action"":""SaveToStorage"",""key"":""" + КлючТеста + """,""data"":""" + ЗначениеЭкранир + """,""operation_id"":""" + OperationId + """}]}";

		Рез1 = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSL, СсылкаДиалога);
		Если НЕ Рез1.Успех Тогда
			Результат.Сообщение = "Первый SaveToStorage завершился с ошибкой: " + Рез1.Сообщение;
			Возврат Результат;
		КонецЕсли;

		Рез2 = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSL, СсылкаДиалога);
		Если НЕ Рез2.Успех Тогда
			Результат.Сообщение = "Повторный SaveToStorage завершился с ошибкой: " + Рез2.Сообщение;
			Возврат Результат;
		КонецЕсли;

		Если НЕ Рез2.Свойство("Результаты") ИЛИ Рез2.Результаты.Количество() = 0 Тогда
			Результат.Сообщение = "Во втором выполнении отсутствуют результаты шагов";
			Возврат Результат;
		КонецЕсли;

		РезультатШага = Рез2.Результаты[0];
		Если НЕ РезультатШага.Свойство("deduplicated_retry") ИЛИ НЕ РезультатШага.deduplicated_retry Тогда
			Результат.Сообщение = "Повторный вызов SaveToStorage с тем же operation_id должен возвращать deduplicated_retry=Истина";
			Возврат Результат;
		КонецЕсли;

		DSLЧтение = "{""dsl_version"":2,""steps"":[{""action"":""LoadFromStorage"",""key"":""" + КлючТеста + """}]}";
		РезЧтение = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSLЧтение, СсылкаДиалога);
		Если НЕ РезЧтение.Успех Тогда
			Результат.Сообщение = "LoadFromStorage после повторов не выполнен: " + РезЧтение.Сообщение;
			Возврат Результат;
		КонецЕсли;

		Если РезЧтение.Результаты.Количество() = 0 Тогда
			Результат.Сообщение = "LoadFromStorage не вернул результата шага";
			Возврат Результат;
		КонецЕсли;

		РезШагаЧтения = РезЧтение.Результаты[0];
		Если НЕ РезШагаЧтения.Свойство("Данные") ИЛИ Строка(РезШагаЧтения.Данные) <> ЗначениеТеста Тогда
			Результат.Сообщение = "После deduplicated retry в хранилище должно остаться исходное значение";
			Возврат Результат;
		КонецЕсли;

		ДанныеДиалога = ИИА_DialogStorage.ПолучитьДанныеДиалогаИзРегистра(СсылкаДиалога);
		Если ДанныеДиалога = Неопределено Тогда
			Результат.Сообщение = "Не удалось получить данные диалога из регистра";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ДанныеДиалога.Свойство("DSL_OperationJournal") Тогда
			Результат.Сообщение = "В данных диалога отсутствует DSL_OperationJournal";
			Возврат Результат;
		КонецЕсли;
		Если ДанныеДиалога.DSL_OperationJournal.Получить(OperationId) = Неопределено Тогда
			Результат.Сообщение = "DSL_OperationJournal не содержит запись по operation_id";
			Возврат Результат;
		КонецЕсли;

		Результат.Детали.Добавить("operation_id=" + OperationId);
		Результат.Детали.Добавить("Повторный вызов помечен как deduplicated_retry");
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: SaveToStorage безопасно дедуплицируется по operation_id";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест режима «только чтение» (Запрос1С): SetField должен быть запрещён.
//
Функция ТестРежимЗапрос1С() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		Результат.Детали.Добавить("Создан диалог типа Запрос1С");
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// RunQuery разрешён
		DSLЗапрос = "{""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ 1 КАК Н""}]}";
		РезЗапрос = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSLЗапрос, СсылкаДиалога);
		Результат.Детали.Добавить("RunQuery в Запрос1С: Успех=" + РезЗапрос.Успех);
		Если НЕ РезЗапрос.Успех Тогда
			Результат.Сообщение = "RunQuery должен быть разрешён: " + РезЗапрос.Сообщение;
			Возврат Результат;
		КонецЕсли;

		// CreateReference/SetField запрещены в Запрос1С
		DSLИзменение = "{""steps"":[{""action"":""CreateReference"",""object_name"":""Контрагенты""},{""action"":""SetField"",""field_name"":""Наименование"",""value"":""X""}]}";
		РезИзменение = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSLИзменение, СсылкаДиалога);
		Результат.Детали.Добавить("CreateReference+SetField в Запрос1С: Успех=" + РезИзменение.Успех + " (ожидается Ложь)");
		Если РезИзменение.Успех Тогда
			Результат.Сообщение = "Действия изменения должны быть запрещены в режиме Запрос1С";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: режим Запрос1С корректен";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Боевой тест: извлечение сущностей через ИИ (RAG экстрактор).
//
Функция ТестИзвлечьСущностиДляRAG() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		// Проверка 1: явные сущности в фразе
		Сущности = ИИА_Сервер.ИзвлечьСущностиДляRAG("Покажи реализацию товаров за апрель");
		Результат.Детали.Добавить("Сущности (1): '" + Сущности + "'");
		Если ПустаяСтрока(Сущности) Тогда
			Результат.Сообщение = "ИзвлечьСущностиДляRAG вернул пустую строку (проверьте API и настройки пользователя)";
			Возврат Результат;
		КонецЕсли;
		Если ВРег(Сущности) = "НЕТ" Тогда
			Результат.Сообщение = "ИИ вернул НЕТ — ожидались сущности (реализация, товары)";
			Возврат Результат;
		КонецЕсли;
		СущностиВРег = ВРег(Сущности);
		Если СтрНайти(СущностиВРег, "РЕАЛИЗАЦИ") = 0 И СтрНайти(СущностиВРег, "ТОВАР") = 0 Тогда
			Результат.Сообщение = "Сущности не содержат ожидаемых слов (реализация/товары): '" + Сущности + "'";
			Возврат Результат;
		КонецЕсли;

		// Проверка 2: фраза без явных имён объектов — экстрактор должен извлечь ключевые слова (продажи, категории)
		Сущности2 = ИИА_Сервер.ИзвлечьСущностиДляRAG("Проанализируй динамику продаж за последний месяц и выдели топ-3 растущих категории");
		Результат.Детали.Добавить("Сущности (2): '" + Сущности2 + "'");
		Если ПустаяСтрока(Сущности2) Или ВРег(Сущности2) = "НЕТ" Тогда
			Результат.Сообщение = "Экстрактор вернул НЕТ/пусто для 'динамика продаж' — ожидались ключевые слова (продажи, категории, реализация)";
			Возврат Результат;
		КонецЕсли;
		Сущности2ВРег = ВРег(Сущности2);
		СодержитОжидаемое = СтрНайти(Сущности2ВРег, "ПРОДАЖ") > 0 Или СтрНайти(Сущности2ВРег, "КАТЕГОРИ") > 0 Или СтрНайти(Сущности2ВРег, "РЕАЛИЗАЦ") > 0;
		Если НЕ СодержитОжидаемое Тогда
			Результат.Сообщение = "Сущности (динамика продаж) не содержат продаж/категорий/реализации: '" + Сущности2 + "'";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: извлечение сущностей для RAG";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Боевой тест: прямая генерация DSL через ИИ.
//
Функция ТестВызовИИГенерацияDSL() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		Промпт = "Сгенерируй DSL для создания контрагента с наименованием 'ТестИИ_DSL'. Ответ: только JSON {""dsl_version"":1,""steps"":[...]}.";
		ОтветИИ = ИИА_Сервер.ВызватьИИССистемнымСообщением(СсылкаДиалога, "Чат", Промпт, "", "dsl", 0.2, Ложь);

		DSL = "";
		Если ОтветИИ.Свойство("DSL") И НЕ ПустаяСтрока(ОтветИИ.DSL) Тогда
			DSL = ОтветИИ.DSL;
		ИначеЕсли ОтветИИ.Свойство("Текст") И НЕ ПустаяСтрока(ОтветИИ.Текст) Тогда
			DSL = ОтветИИ.Текст;
		КонецЕсли;

		Результат.Детали.Добавить("DSL длина: " + СтрДлина(DSL));
		Если ПустаяСтрока(DSL) Тогда
			Результат.Сообщение = "ИИ не вернул DSL (проверьте API). Ошибки: " + ?(ОтветИИ.Свойство("Ошибки"), ОтветИИ.Ошибки, "");
			Возврат Результат;
		КонецЕсли;

		Если СтрНайти(DSL, "CreateReference") = 0 Или СтрНайти(ВРег(DSL), "КОНТРАГЕНТ") = 0 Тогда
			Результат.Сообщение = "DSL не содержит CreateReference или Контрагент: " + Лев(DSL, 200);
			Возврат Результат;
		КонецЕсли;

		// Проверяем валидность — выполняем DSL
		РезDSL = ИИА_Сервер.ВыполнитьDSLСценарийБезСообщений(DSL, СсылкаДиалога);
		Результат.Детали.Добавить("Выполнение DSL: Успех=" + РезDSL.Успех);
		Если НЕ РезDSL.Успех Тогда
			Результат.Сообщение = "Сгенерированный DSL не выполнился: " + РезDSL.Сообщение;
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: генерация и выполнение DSL через ИИ";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Боевой тест: агент в режиме Запрос1С (только чтение).
//
Функция ТестАгентЗапрос1С() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Рез = ИИА_ДиалогCOM.СоздатьДиалогИВыполнитьАгентаСинхронно("Администратор", "Сколько записей в справочнике Контрагенты?", Перечисления.ИИА_ТипДиалога.Запрос1С);
		Результат.Детали.Добавить("Диалог: " + Рез.СсылкаДиалога);
		Результат.Детали.Добавить("Успех: " + Рез.Успех);
		Если НЕ ЗначениеЗаполнено(Рез.СсылкаДиалога) Тогда
			Результат.Сообщение = "Диалог не создан";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Рез.Успех;
		Результат.Сообщение = ?(Рез.Успех, "Тест пройден: Запрос1С выполнен", "Агент Запрос1С не завершился (проверьте лог)");
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Боевой тест: полный цикл агента — создание контрагента и проверка.
//
Функция ТестАгентСоздатьКонтрагента() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Наименование = "Тест_Агент_ИИ_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		Рез = ИИА_ДиалогCOM.СоздатьДиалогИВыполнитьАгентаСинхронно("Администратор", "Создай контрагента " + Наименование, Перечисления.ИИА_ТипДиалога.Агент);
		Результат.Детали.Добавить("Диалог: " + Рез.СсылкаДиалога);
		Результат.Детали.Добавить("Успех: " + Рез.Успех);
		Если НЕ ЗначениеЗаполнено(Рез.СсылкаДиалога) Тогда
			Результат.Сообщение = "Диалог не создан";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Рез.Успех Тогда
			Результат.Сообщение = "Агент не завершился успешно. Лог: " + Лев(Рез.Лог, 500);
			Возврат Результат;
		КонецЕсли;

		// Проверяем, что объект создан
		Диалог = Рез.СсылкаДиалога.ПолучитьОбъект();
		НайденОбъект = Ложь;
		Для Каждого СтрокаИзмененных Из Диалог.ИзмененныеОбъекты Цикл
			Если ЗначениеЗаполнено(СтрокаИзмененных.СсылкаНаОбъект) Тогда
				Попытка
					Объект = СтрокаИзмененных.СсылкаНаОбъект.ПолучитьОбъект();
					Если Объект <> Неопределено И СокрЛП(Строка(Объект.Наименование)) = Наименование Тогда
						НайденОбъект = Истина;
						Результат.Детали.Добавить("Создан: " + СтрокаИзмененных.СсылкаНаОбъект);
						Прервать;
					КонецЕсли;
				Исключение
					// Пропускаем при ошибке доступа к Наименование
				КонецПопытки;
			КонецЕсли;
		КонецЦикла;

		Если НЕ НайденОбъект Тогда
			Результат.Сообщение = "Контрагент '" + Наименование + "' не найден в ИзмененныеОбъекты";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: агент создал контрагента";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест СоздатьДиалогИВыполнитьАгентаСинхронно через COM.
//
Функция ТестСоздатьДиалогИВыполнитьАгентаСинхронно() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Рез = ИИА_ДиалогCOM.СоздатьДиалогИВыполнитьАгентаСинхронно("Администратор", "Создай контрагента Ашан", Перечисления.ИИА_ТипДиалога.Агент);
		Результат.Детали.Добавить("Диалог: " + Рез.СсылкаДиалога);
		Результат.Детали.Добавить("Успех: " + Рез.Успех);
		Если НЕ ЗначениеЗаполнено(Рез.СсылкаДиалога) Тогда
			Результат.Сообщение = "Диалог не создан";
			Возврат Результат;
		КонецЕсли;
		Если СтрДлина(Рез.Лог) = 0 Тогда
			Результат.Детали.Добавить("Лог пуст (возможно оркестратор не отработал)");
		КонецЕсли;
		Результат.Успех = Рез.Успех;
		Результат.Сообщение = ?(Рез.Успех, "Тест пройден: агент выполнен", "Агент не завершился успешно (проверьте лог)");
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест GetMetadata через DSL.
//
Функция ТестGetMetadata() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		DSL = "{""steps"":[{""action"":""GetMetadata""}]}";
		Рез = ИИА_DSL.ВыполнитьDSL(DSL, Неопределено, Ложь);
		Результат.Детали.Добавить("Выполнен GetMetadata");
		Если НЕ Рез.Успех Тогда
			Результат.Сообщение = "GetMetadata не выполнен: " + Рез.Сообщение;
			Возврат Результат;
		КонецЕсли;
		Если Рез.Результаты.Количество() = 0 Тогда
			Результат.Сообщение = "Нет результатов GetMetadata";
			Возврат Результат;
		КонецЕсли;
		РезШага = Рез.Результаты[0];
		Если НЕ РезШага.Свойство("Данные") ИЛИ РезШага.Данные = Неопределено Тогда
			Результат.Сообщение = "GetMetadata не вернул данные";
			Возврат Результат;
		КонецЕсли;
		Данные = РезШага.Данные;
		Если ТипЗнч(Данные) = Тип("Структура") И (Данные.Свойство("Справочники") ИЛИ Данные.Свойство("Документы")) Тогда
			Результат.Детали.Добавить("Получены Справочники/Документы");
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: GetMetadata";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест GetObjectFields для регистра накопления (поддержка регистров в GetObjectFields).
//
Функция ТестGetObjectFieldsРегистр() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СписокРегистров = ИИА_Метаданные.ПолучитьСписокРегистровНакопления("");
		Если СписокРегистров.Количество() = 0 Тогда
			Результат.Сообщение = "В конфигурации нет регистров накопления — тест пропущен";
			Результат.Успех = Истина;
			Возврат Результат;
		КонецЕсли;
		ИмяРегистра = СписокРегистров[0].Имя;
		DSL = "{""steps"":[{""action"":""GetObjectFields"",""object_type"":""РегистрНакопления"",""object_name"":""" + ИмяРегистра + """}]}";
		Рез = ИИА_DSL.ВыполнитьDSL(DSL, Неопределено, Ложь);
		Результат.Детали.Добавить("GetObjectFields РегистрНакопления." + ИмяРегистра + ": Успех=" + Рез.Успех);
		Если НЕ Рез.Успех Тогда
			Результат.Сообщение = "GetObjectFields для регистра не выполнен: " + Рез.Сообщение;
			Возврат Результат;
		КонецЕсли;
		Если Рез.Результаты.Количество() = 0 Тогда
			Результат.Сообщение = "Нет результатов GetObjectFields";
			Возврат Результат;
		КонецЕсли;
		РезШага = Рез.Результаты[0];
		Если НЕ РезШага.Свойство("Данные") ИЛИ РезШага.Данные = Неопределено Тогда
			Результат.Сообщение = "GetObjectFields не вернул данные";
			Возврат Результат;
		КонецЕсли;
		Данные = РезШага.Данные;
		Если ТипЗнч(Данные) = Тип("Структура") И Данные.Свойство("Реквизиты") Тогда
			Результат.Детали.Добавить("Получены реквизиты регистра");
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: GetObjectFields для РегистрНакопления";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест FindReferenceByName: создаём элемент, ищем по имени.
//
Функция ТестFindReferenceByName() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		УникальноеИмя = "Тест_FindRef_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		// Создаём элемент
		DSLСоздать = "{""steps"":[{""action"":""CreateReference"",""object_name"":""Контрагенты""},{""action"":""SetField"",""field_name"":""Наименование"",""value"":""" + УникальноеИмя + """},{""action"":""Write""}]}";
		РезСоздать = ИИА_DSL.ВыполнитьDSL(DSLСоздать, Неопределено, Ложь);
		Результат.Детали.Добавить("Создан элемент Контрагенты с именем " + УникальноеИмя + ": Успех=" + РезСоздать.Успех);
		Если НЕ РезСоздать.Успех Тогда
			Результат.Сообщение = "Не удалось создать элемент: " + РезСоздать.Сообщение;
			Возврат Результат;
		КонецЕсли;

		// Ищем по имени
		DSLНайти = "{""steps"":[{""action"":""FindReferenceByName"",""object_name"":""Контрагенты"",""name"":""" + УникальноеИмя + """}]}";
		РезНайти = ИИА_DSL.ВыполнитьDSL(DSLНайти, Неопределено, Ложь);
		Результат.Детали.Добавить("FindReferenceByName: Успех=" + РезНайти.Успех);
		Если НЕ РезНайти.Успех Тогда
			Результат.Сообщение = "FindReferenceByName не выполнен: " + РезНайти.Сообщение;
			Возврат Результат;
		КонецЕсли;
		Если РезНайти.Результаты.Количество() = 0 Тогда
			Результат.Сообщение = "FindReferenceByName не вернул результат";
			Возврат Результат;
		КонецЕсли;
		РезШага = РезНайти.Результаты[0];
		Если НЕ РезШага.Свойство("Данные") ИЛИ НЕ ЗначениеЗаполнено(РезШага.Данные) Тогда
			Результат.Сообщение = "FindReferenceByName не вернул ссылку (Данные)";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: FindReferenceByName";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест создания объекта через DSL: создаётся контрагент, проверяется что он добавлен в таб.часть
// ИзмененныеОбъекты диалога, создан ровно 1 объект с нужным наименованием.
//
Функция ТестСозданияОбъекта() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Наименование = "Тест_Создание_Объекта_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		Результат.Детали.Добавить("Наименование для создания: " + Наименование);

		// Создаём диалог
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("Создан диалог: " + СсылкаДиалога);

		// Создаём объект через DSL — ВыполнитьDSLСценарийССообщениями штатно вызывает ДобавитьИзмененныеОбъекты
		НаименованиеЭкранир = СтрЗаменить(Наименование, "\", "\\");
		НаименованиеЭкранир = СтрЗаменить(НаименованиеЭкранир, """", "\""");
		DSL = "{""steps"":[{""action"":""CreateReference"",""object_name"":""Контрагенты""},{""action"":""SetField"",""field_name"":""Наименование"",""value"":""" + НаименованиеЭкранир + """},{""action"":""Write""}]}";
		Рез = ИИА_Сервер.ВыполнитьDSLСценарийССообщениями(СсылкаДиалога, DSL);
		Результат.Детали.Добавить("DSL CreateReference+SetField+Write: Успех=" + Рез.Успех);
		Если НЕ Рез.Успех Тогда
			Результат.Сообщение = "Не удалось создать объект: " + Рез.Сообщение;
			Возврат Результат;
		КонецЕсли;

		// Проверяем, что объект добавлен в таб.часть ИзмененныеОбъекты диалога
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		Если Диалог = Неопределено Тогда
			Результат.Сообщение = "Не удалось получить объект диалога";
			Возврат Результат;
		КонецЕсли;

		КоличествоВИзмененных = 0;
		СсылкаСоздан = Неопределено;
		Для Каждого СтрокаИзмененных Из Диалог.ИзмененныеОбъекты Цикл
			Если ЗначениеЗаполнено(СтрокаИзмененных.СсылкаНаОбъект) Тогда
				КоличествоВИзмененных = КоличествоВИзмененных + 1;
				СсылкаСоздан = СтрокаИзмененных.СсылкаНаОбъект;
			КонецЕсли;
		КонецЦикла;

		Результат.Детали.Добавить("Строк в ИзмененныеОбъекты: " + КоличествоВИзмененных);
		Если КоличествоВИзмененных <> 1 Тогда
			Результат.Сообщение = "Ожидалась 1 запись в ИзмененныеОбъекты, найдено: " + КоличествоВИзмененных;
			Возврат Результат;
		КонецЕсли;

		// Проверяем, что создан ровно 1 объект с таким наименованием
		Запрос = Новый Запрос;
		Запрос.Текст =
		"ВЫБРАТЬ
		|	КОЛИЧЕСТВО(*) КАК Количество
		|ИЗ
		|	Справочник.Контрагенты КАК Элементы
		|ГДЕ
		|	Элементы.Наименование = &Наименование";
		Запрос.УстановитьПараметр("Наименование", Наименование);
		Выборка = Запрос.Выполнить().Выбрать();
		Выборка.Следующий();
		Количество = Выборка.Количество;
		Результат.Детали.Добавить("Найдено объектов с именем '" + Наименование + "': " + Количество);

		Если Количество <> 1 Тогда
			Результат.Сообщение = "Ожидался 1 объект с именем '" + Наименование + "', найдено: " + Количество;
			Возврат Результат;
		КонецЕсли;

		// Проверяем наименование у объекта из ИзмененныеОбъекты
		Если ЗначениеЗаполнено(СсылкаСоздан) Тогда
			ОбъектСоздан = СсылкаСоздан.ПолучитьОбъект();
			Если ОбъектСоздан <> Неопределено Тогда
				НаименованиеОбъекта = СокрЛП(Строка(ОбъектСоздан.Наименование));
				Если НаименованиеОбъекта <> Наименование Тогда
					Результат.Сообщение = "Наименование объекта не совпадает: '" + НаименованиеОбъекта + "' <> '" + Наименование + "'";
					Возврат Результат;
				КонецЕсли;
				Результат.Детали.Добавить("Наименование объекта совпадает");
			КонецЕсли;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: создан 1 объект, добавлен в ИзмененныеОбъекты, наименование '" + Наименование + "'";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест логирования диалога: ДобавитьЗаписьВЛогДиалога, ПолучитьЛогДиалога.
//
Функция ТестЛогированияДиалога() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Результат.Детали.Добавить("Создан диалог");
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		МеткаТест = "ТестЛог_" + Строка(Новый УникальныйИдентификатор());
		ЗаписьТест = МеткаТест;
		ИИА_Сервер.ДобавитьЗаписьВЛогДиалога(СсылкаДиалога, ЗаписьТест);
		Результат.Детали.Добавить("Добавлена запись в лог");

		Лог = ИИА_Сервер.ПолучитьЛогДиалога(СсылкаДиалога);
		Если СтрНайти(Лог, МеткаТест) = 0 Тогда
			Результат.Сообщение = "Добавленная запись не найдена в логе (искали '" + МеткаТест + "')";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("Запись найдена в логе");

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: логирование диалога";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест промпта дополнения плана: проверка структуры ПолучитьПромптДополненияПлана.
//
Функция ТестПромптаДополненияПлана() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ПланJSON = "[{""Задача"":""CreateReference"",""Выполнена"":true,""Результат"":""Создан""}]";
		ТекстЗадачи = "Добавь комментарий";

		// 1. Базовый вызов без диалога (без контекста ИзмененныеОбъекты)
		ПромптБезКонтекста = ИИА_Промты.ПолучитьПромптДополненияПлана(ТекстЗадачи, ПланJSON, Неопределено);
		Результат.Детали.Добавить("Промпт без контекста: длина " + СтрДлина(ПромптБезКонтекста));

		Если СтрНайти(ПромптБезКонтекста, "Новая задача пользователя:") = 0 Тогда
			Результат.Сообщение = "Промпт не содержит 'Новая задача пользователя:'";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ПромптБезКонтекста, ПланJSON) = 0 Тогда
			Результат.Сообщение = "Промпт не содержит ПланJSON";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(ПромптБезКонтекста), "КОНТРАКТ_ОТВЕТА") = 0
			ИЛИ СтрНайти(ВРег(ПромптБезКонтекста), "RESPONSE_MODE: PLAN") = 0 Тогда
			Результат.Сообщение = "Промпт не содержит контракт ответа для режима plan";
			Возврат Результат;
		КонецЕсли;

		// 2. Вызов с диалогом, имеющим ИзмененныеОбъекты
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// Добавляем объект в ИзмененныеОбъекты
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		НоваяСтрока = Диалог.ИзмененныеОбъекты.Добавить();
		НоваяСтрока.СсылкаНаОбъект = СсылкаДиалога;
		Диалог.Записать();

		ПромптСКонтекстом = ИИА_Промты.ПолучитьПромптДополненияПлана(ТекстЗадачи, ПланJSON, СсылкаДиалога);
		Результат.Детали.Добавить("Промпт с контекстом: длина " + СтрДлина(ПромптСКонтекстом));

		Если СтрНайти(ПромптСКонтекстом, "Контекст диалога") = 0 Тогда
			Результат.Сообщение = "Промпт с ИзмененныеОбъекты не содержит блок 'Контекст диалога'";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: промпт дополнения плана";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест логов после дополнения плана (режим холостого хода с mock).
//
Функция ТестЛоговПослеДополненияПлана() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("Создан диалог: " + СсылкаДиалога);

		// План с одной выполненной задачей
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		СтрокаПлана = Диалог.План.Добавить();
		СтрокаПлана.Задача = "CreateReference";
		СтрокаПлана.Выполнена = Истина;
		СтрокаПлана.Результат = "Создан";
		Диалог.Записать();

		// Mock-ответ ДО отправки (ОтправитьСообщениеСервера вызовет ДополнитьПлан при ПланЗавершен)
		// Формат: JSON массив строк (новые задачи)
		MockТекст = "[""SetField"",""Write""]";
		МассивMock = Новый Массив;
		МассивMock.Добавить(Новый Структура("Текст", MockТекст));
		ИИА_Сервер.УстановитьОчередьMockОтветов(СсылкаДиалога, МассивMock);
		Результат.Детали.Добавить("Установлена очередь mock-ответов");

		// Сообщение пользователя (при ПланЗавершен вызовет ДополнитьПлан с mock)
		ИИА_Сервер.ОтправитьСообщениеСервера(СсылкаДиалога, Перечисления.ИИА_ТипДиалога.Агент, "Добавь комментарий");
		Результат.Детали.Добавить("Добавлено сообщение пользователя");

		// Дополнить план
		УспехДополнения = ИИА_TypedPlan.ДополнитьПлан(СсылкаДиалога);
		Результат.Детали.Добавить("ДополнитьПлан: Успех=" + УспехДополнения);

		ИИА_Сервер.ОчиститьОчередьMockОтветов(СсылкаДиалога);

		Лог = ИИА_Сервер.ПолучитьЛогДиалога(СсылкаДиалога);
		// Префикс [План] убирается при записи в лог, остаётся текст после скобок
		Если СтрНайти(Лог, "Дополнение плана новой задачей") = 0 Тогда
			Результат.Сообщение = "Лог не содержит 'Дополнение плана новой задачей'";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Лог, "План дополнен") = 0 Тогда
			Результат.Сообщение = "Лог не содержит 'План дополнен'";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: логи после дополнения плана";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест дополнения плана с контекстом: создаём объект, добавляем в ИзмененныеОбъекты, дополняем план mock-ом.
//
Функция ТестДополнениеПланаСКонтекстомОбъекта() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Наименование = "Тест_Дополнение_Контекст_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// Создаём контрагента через DSL, добавляется в ИзмененныеОбъекты
		НаименованиеЭкранир = СтрЗаменить(Наименование, "\", "\\");
		НаименованиеЭкранир = СтрЗаменить(НаименованиеЭкранир, """", "\""");
		DSL = "{""steps"":[{""action"":""CreateReference"",""object_name"":""Контрагенты""},{""action"":""SetField"",""field_name"":""Наименование"",""value"":""" + НаименованиеЭкранир + """},{""action"":""Write""}]}";
		Рез = ИИА_Сервер.ВыполнитьDSLСценарийССообщениями(СсылкаДиалога, DSL);
		Если НЕ Рез.Успех Тогда
			Результат.Сообщение = "Не удалось создать объект: " + Рез.Сообщение;
			Возврат Результат;
		КонецЕсли;

		// План с выполненной задачей
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		СтрокаПлана = Диалог.План.Добавить();
		СтрокаПлана.Задача = "CreateReference";
		СтрокаПлана.Выполнена = Истина;
		СтрокаПлана.Результат = "Создан";
		Диалог.Записать();

		// Проверяем, что объект в ИзмененныеОбъекты
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		КоличествоВИзмененных = 0;
		Для Каждого СтрокаИзмененных Из Диалог.ИзмененныеОбъекты Цикл
			Если ЗначениеЗаполнено(СтрокаИзмененных.СсылкаНаОбъект) Тогда
				КоличествоВИзмененных = КоличествоВИзмененных + 1;
			КонецЕсли;
		КонецЦикла;
		Если КоличествоВИзмененных = 0 Тогда
			Результат.Сообщение = "ИзмененныеОбъекты пуст после создания";
			Возврат Результат;
		КонецЕсли;

		// Mock и дополнение
		MockТекст = "[""SetField"",""Write""]";
		МассивMock = Новый Массив;
		МассивMock.Добавить(Новый Структура("Текст", MockТекст));
		ИИА_Сервер.УстановитьОчередьMockОтветов(СсылкаДиалога, МассивMock);
		ИИА_Сервер.ОтправитьСообщениеСервера(СсылкаДиалога, Перечисления.ИИА_ТипДиалога.Агент, "заполни ему комментарий Тест");
		УспехДополнения = ИИА_TypedPlan.ДополнитьПлан(СсылкаДиалога);
		ИИА_Сервер.ОчиститьОчередьMockОтветов(СсылкаДиалога);
		Если НЕ УспехДополнения Тогда
			Результат.Сообщение = "ДополнитьПлан вернул Ложь";
			Возврат Результат;
		КонецЕсли;

		// Проверяем, что план дополнен
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		КоличествоЗадач = Диалог.План.Количество();
		Если КоличествоЗадач < 2 Тогда
			Результат.Сообщение = "План не дополнен: " + КоличествоЗадач + " задач";
			Возврат Результат;
		КонецЕсли;

		// Проверяем, что промпт содержал контекст (через повторный вызов ПолучитьПромптДополненияПлана — контекст уже есть)
		ПланJSON = "[{""Задача"":""CreateReference"",""Выполнена"":true,""Результат"":""Создан""}]";
		Промпт = ИИА_Промты.ПолучитьПромптДополненияПлана("заполни ему комментарий", ПланJSON, СсылкаДиалога);
		Если СтрНайти(Промпт, "Контекст диалога") = 0 Тогда
			Результат.Сообщение = "Промпт с ИзмененныеОбъекты не содержит 'Контекст диалога'";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: дополнение плана с контекстом объекта";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест контекста из регистра (ХранилищеЗначений) для дополнения плана.
//
Функция ТестКонтекстИзРегистраДляДополнения() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Наименование = "Тест_Регистр_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// Создаём контрагента и записываем в регистр DSL_СсылкаОбъекта
		НаименованиеЭкранир = СтрЗаменить(Наименование, "\", "\\");
		НаименованиеЭкранир = СтрЗаменить(НаименованиеЭкранир, """", "\""");
		DSL = "{""steps"":[{""action"":""CreateReference"",""object_name"":""Контрагенты""},{""action"":""SetField"",""field_name"":""Наименование"",""value"":""" + НаименованиеЭкранир + """},{""action"":""Write""}]}";
		Рез = ИИА_Сервер.ВыполнитьDSLСценарийССообщениями(СсылкаДиалога, DSL);
		Если НЕ Рез.Успех Тогда
			Результат.Сообщение = "Не удалось создать объект: " + Рез.Сообщение;
			Возврат Результат;
		КонецЕсли;

		// Промпт дополнения должен содержать контекст из регистра (Текущий объект в контексте DSL)
		ПланJSON = "[{""Задача"":""CreateReference"",""Выполнена"":true,""Результат"":""Создан""}]";
		Промпт = ИИА_Промты.ПолучитьПромптДополненияПлана("заполни ему комментарий", ПланJSON, СсылкаДиалога);

		// СформироватьКонтекстИзРегистраДиалога возвращает "Текущий объект в контексте DSL", СформироватьКонтекстИзмененныхОбъектов — "Контекст диалога"
		Если СтрНайти(Промпт, "Текущий объект в контексте DSL") > 0 ИЛИ СтрНайти(Промпт, "Контекст диалога") > 0 Тогда
			Результат.Детали.Добавить("Промпт содержит контекст из регистра или ИзмененныеОбъекты");
		Иначе
			Результат.Сообщение = "Промпт не содержит контекст из регистра или ИзмененныеОбъекты";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: контекст из регистра для дополнения";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест промпта создания плана с контекстом ИзмененныеОбъекты.
//
Функция ТестПромптаСозданияПланаСКонтекстом() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// Добавляем объект в ИзмененныеОбъекты
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		НоваяСтрока = Диалог.ИзмененныеОбъекты.Добавить();
		НоваяСтрока.СсылкаНаОбъект = СсылкаДиалога;
		Диалог.Записать();

		Промпт = ИИА_Промты.ПолучитьПромптСозданияПлана("заполни ему комментарий", СсылкаДиалога);

		Если СтрНайти(Промпт, "Контекст диалога") = 0 Тогда
			Результат.Сообщение = "Промпт создания плана не содержит 'Контекст диалога'";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Промпт, "Задача:") = 0 Тогда
			Результат.Сообщение = "Промпт не содержит 'Задача:'";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: промпт создания плана с контекстом";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест СобратьФактыПоследнихДействий.
//
Функция ТестСобратьФактыПоследнихДействий() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// Добавляем сообщение с dsl_system_result (имитация результата DSL)
		ТекстРезультата = "dsl_system_result success=true CreateReference Контрагенты";
		ИИА_Сервер.ДобавитьСообщениеВДиалог(СсылкаДиалога, Перечисления.ИИА_АвторСообщения.Система, Перечисления.ИИА_ТипСообщения.Текст, ТекстРезультата);

		Факты = ИИА_RuntimeФакты.СобратьФактыПоследнихДействий(СсылкаДиалога);

		Если ПустаяСтрока(Факты) Тогда
			Результат.Сообщение = "СобратьФактыПоследнихДействий вернул пустую строку";
			Возврат Результат;
		КонецЕсли;
		// Форматирование может дать "DSL Result: ok | ..." - проверяем наличие маркеров
		ФактыВРег = ВРег(Факты);
		Если СтрНайти(ФактыВРег, "DSL") = 0 И СтрНайти(ФактыВРег, "CREATEREFERENCE") = 0 Тогда
			Результат.Сообщение = "Факты не содержат ожидаемых данных (DSL или CreateReference)";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: СобратьФактыПоследнихДействий";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест авто-добавления get_metadata при ошибке «таблица не найдена».
// Проверяет: ДобавитьGetMetadataВПланПриТаблицаНеНайдена вставляет typed-step get_metadata перед первым невыполненным run_query.
//
Функция ТестАвтоГетМетадатаПриТаблицаНеНайдена() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		План = Новый Структура;
		План.Вставить("plan_version", 1);
		План.Вставить("goal", "Тест");
		Шаги = Новый Массив;
		Шаг = Новый Структура;
		Шаг.Вставить("id", "step_1");
		Шаг.Вставить("seq_no", 1);
		Шаг.Вставить("kind", "run_query");
		Шаг.Вставить("title", "Получить документ реализации");
		Шаг.Вставить("depends_on", Новый Массив);
		Input = Новый Структура;
		Input.Вставить("query", "ВЫБРАТЬ ПЕРВЫЕ 1 Документ.РеализацияТоваровУслуг.Ссылка КАК Ссылка");
		Шаг.Вставить("input", Input);
		Шаг.Вставить("status", "pending");
		Шаг.Вставить("result_summary", "");
		Шаг.Вставить("result_data", Неопределено);
		Шаги.Добавить(Шаг);
		План.Вставить("steps", Шаги);
		ИИА_TypedPlan.СохранитьTypedPlan(СсылкаДиалога, План);
		Результат.Детали.Добавить("Создан typed plan с run_query для РеализацияТоваровУслуг");

		// Вызываем ДобавитьGetMetadataВПланПриТаблицаНеНайдена
		Добавлен = ИИА_Оркестратор.ДобавитьGetMetadataВПланПриТаблицаНеНайдена(СсылкаДиалога, "Документ.РеализацияТоваровУслуг");
		Если НЕ Добавлен Тогда
			Результат.Сообщение = "ДобавитьGetMetadataВПланПриТаблицаНеНайдена вернул Ложь";
			Возврат Результат;
		КонецЕсли;

		// Проверяем, что в typed plan добавлен get_metadata перед run_query.
		// Нормализация typed plan также добавляет show_info после run_query, чтобы результат не терялся.
		План = ИИА_TypedPlan.ПолучитьTypedPlan(СсылкаДиалога);
		Если План.steps.Количество() <> 3 Тогда
			Результат.Сообщение = "Ожидалось 3 шага в typed plan, получено: " + Строка(План.steps.Количество());
			Возврат Результат;
		КонецЕсли;

		ПервыйШаг = План.steps[0];
		ВторойШаг = План.steps[1];
		ТретийШаг = План.steps[2];
		Если НРег(Строка(ПервыйШаг.kind)) <> "get_metadata" Тогда
			Результат.Сообщение = "Первый шаг должен быть get_metadata, получено: " + Строка(ПервыйШаг.kind);
			Возврат Результат;
		КонецЕсли;
		Если НЕ ПервыйШаг.input.Свойство("filter") ИЛИ СтрНайти(ВРег(Строка(ПервыйШаг.input.filter)), "РЕАЛИЗАЦИЯ") = 0 Тогда
			Результат.Сообщение = "get_metadata должен содержать фильтр Реализация";
			Возврат Результат;
		КонецЕсли;
		Если НРег(Строка(ВторойШаг.kind)) <> "run_query" Тогда
			Результат.Сообщение = "Второй шаг должен быть run_query, получено: " + Строка(ВторойШаг.kind);
			Возврат Результат;
		КонецЕсли;
		Если НРег(Строка(ТретийШаг.kind)) <> "show_info" Тогда
			Результат.Сообщение = "Третий шаг должен быть show_info, получено: " + Строка(ТретийШаг.kind);
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: авто-GetMetadata при таблица не найдена";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест релевантного фрагмента справки (П.3). Проверяет, что ПолучитьСправкуЗапросовПоОшибке возвращает целевой контент по типу ошибки.
//
Функция ТестСправкиЗапросовПоОшибке() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		// Ошибка с СУММА — должна добавить справку по агрегатным функциям
		СправкаСумма = ИИА_Сервер.ПолучитьСправкуЗапросовПоОшибке("Ошибка: СУММА не может быть применена");
		Если ПустаяСтрока(СправкаСумма) Тогда
			Результат.Сообщение = "Справка пуста для ошибки с СУММА";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(СправкаСумма), "СУММА") = 0 И СтрНайти(ВРег(СправкаСумма), "АГРЕГАТ") = 0 Тогда
			Результат.Сообщение = "Справка для СУММА должна содержать агрегатные функции";
			Возврат Результат;
		КонецЕсли;

		// Ошибка с JOIN — должна добавить справку по соединениям
		СправкаJoin = ИИА_Сервер.ПолучитьСправкуЗапросовПоОшибке("Ошибка JOIN в запросе");
		Если СтрНайти(ВРег(СправкаJoin), "СОЕДИНЕН") = 0 И СтрНайти(ВРег(СправкаJoin), "JOIN") = 0 Тогда
			Результат.Сообщение = "Справка для JOIN должна содержать операторы соединения";
			Возврат Результат;
		КонецЕсли;

		// Базовая справка всегда присутствует (основные конструкции)
		Если СтрНайти(ВРег(СправкаСумма), "ВЫБРАТЬ") = 0 Тогда
			Результат.Сообщение = "Справка должна содержать базовые конструкции (ВЫБРАТЬ)";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: справка по ошибке";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест правила GetObjectFields в промпте (П.5). Проверяет, что промпт содержит инструкцию про GetMetadata/GetObjectFields.
//
Функция ТестПромптаGetObjectFields() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Промпт = ИИА_Промты.ПолучитьПромптГенерацииСледующегоШага("{""plan_version"":1,""goal"":""Тест"",""steps"":[{""id"":""step_1"",""kind"":""run_query"",""title"":""Тестовый запрос"",""depends_on"":[],""input"":{}}]}", "{""id"":""step_1"",""kind"":""run_query"",""title"":""Тестовый запрос"",""depends_on"":[],""input"":{}}", "Факты", "", "");
		Если СтрНайти(ВРег(Промпт), "GETMETADATA") = 0 ИЛИ СтрНайти(ВРег(Промпт), "GETOBJECTFIELDS") = 0 Тогда
			Результат.Сообщение = "Промпт должен содержать правило GetMetadata/GetObjectFields";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(Промпт), "ОБЯЗАТЕЛЬНО") = 0 Тогда
			Результат.Сообщение = "Промпт должен содержать ОБЯЗАТЕЛЬНО для GetObjectFields";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: правило GetObjectFields в промпте";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест сбора неудачных объектов из фактов (П.6). Проверяет СобратьНеудачныеОбъектыИзФактов.
//
Функция ТестСобратьНеудачныеОбъектыИзФактов() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ТекстФактов = "Таблица 'Документ.РеализацияТоваровУслуг' не найдена в метаданных. " +
			"Поле не найдено ""Контрагенты.ЮрФизЛицо"".";
		Список = ИИА_RuntimeФакты.СобратьНеудачныеОбъектыИзФактов(ТекстФактов);
		Если ПустаяСтрока(Список) Тогда
			Результат.Сообщение = "Список неудачных объектов пуст";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(Список), "РЕАЛИЗАЦИЯТОВАРОВУСЛУГ") = 0 Тогда
			Результат.Сообщение = "Список должен содержать РеализацияТоваровУслуг, получено: " + Список;
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(Список), "КОНТРАГЕНТЫ") = 0 Тогда
			Результат.Сообщение = "Список должен содержать Контрагенты (из Поле не найдено), получено: " + Список;
			Возврат Результат;
		КонецЕсли;

		// Пустой ввод
		Пустой = ИИА_RuntimeФакты.СобратьНеудачныеОбъектыИзФактов("");
		Если НЕ ПустаяСтрока(Пустой) Тогда
			Результат.Сообщение = "Для пустого ввода должен вернуться пустой список";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: СобратьНеудачныеОбъектыИзФактов";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест ключа последней ошибки из фактов (П.7). Проверяет ПолучитьКлючПоследнейОшибкиИзФактов.
//
Функция ТестПолучитьКлючПоследнейОшибкиИзФактов() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		// Факты с успехом — ключ пустой
		ФактыУспех = "dsl_system_result ""success"":true";
		КлючУспех = ИИА_RuntimeФакты.ПолучитьКлючПоследнейОшибкиИзФактов(ФактыУспех);
		Если НЕ ПустаяСтрока(КлючУспех) Тогда
			Результат.Сообщение = "При успехе ключ должен быть пустым, получено: " + КлючУспех;
			Возврат Результат;
		КонецЕсли;

		// Факты с ошибкой — через диалог (как в реальном сценарии)
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;
		ТекстОшибки = "{""kind"":""dsl_system_result"",""success"":false,""error"":""Таблица не найдена""}";
		ИИА_Сервер.ДобавитьСообщениеВДиалог(СсылкаДиалога, Перечисления.ИИА_АвторСообщения.Система, Перечисления.ИИА_ТипСообщения.Текст, ТекстОшибки);
		Факты = ИИА_RuntimeФакты.СобратьФактыПоследнихДействий(СсылкаДиалога);
		КлючОшибка = ИИА_RuntimeФакты.ПолучитьКлючПоследнейОшибкиИзФактов(Факты);
		Если ПустаяСтрока(КлючОшибка) Тогда
			Результат.Сообщение = "При ошибке в фактах ключ не должен быть пустым. Факты: " + Лев(Факты, 300);
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(КлючОшибка), "RUNQUERY") = 0 Тогда
			Результат.Сообщение = "Ключ должен начинаться с RunQuery, получено: " + КлючОшибка;
			Возврат Результат;
		КонецЕсли;

		// Пустой ввод
		КлючПусто = ИИА_RuntimeФакты.ПолучитьКлючПоследнейОшибкиИзФактов("");
		Если НЕ ПустаяСтрока(КлючПусто) Тогда
			Результат.Сообщение = "Для пустого ввода ключ должен быть пустым";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: ПолучитьКлючПоследнейОшибкиИзФактов";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест создания объекта и последующего изменения через контекст регистра.
//
Функция ТестСозданиеИИзменениеОбъекта() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Наименование = "Тест_Создание_Изменение_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		КомментарийТест = "Комментарий из теста";
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// 1. Создаём контрагента (DSL пишет DSL_СсылкаОбъекта в регистр)
		НаименованиеЭкранир = СтрЗаменить(Наименование, "\", "\\");
		НаименованиеЭкранир = СтрЗаменить(НаименованиеЭкранир, """", "\""");
		DSL1 = "{""steps"":[{""action"":""CreateReference"",""object_name"":""Контрагенты""},{""action"":""SetField"",""field_name"":""Наименование"",""value"":""" + НаименованиеЭкранир + """},{""action"":""Write""}]}";
		Рез1 = ИИА_Сервер.ВыполнитьDSLСценарийССообщениями(СсылкаДиалога, DSL1);
		Если НЕ Рез1.Успех Тогда
			Результат.Сообщение = "Не удалось создать объект: " + Рез1.Сообщение;
			Возврат Результат;
		КонецЕсли;

		// 2. Изменяем через контекст регистра (SetField + Write без CreateReference)
		КомментарийЭкранир = СтрЗаменить(КомментарийТест, "\", "\\");
		КомментарийЭкранир = СтрЗаменить(КомментарийЭкранир, """", "\""");
		DSL2 = "{""steps"":[{""action"":""SetField"",""field_name"":""Комментарий"",""value"":""" + КомментарийЭкранир + """},{""action"":""Write""}]}";
		Рез2 = ИИА_Сервер.ВыполнитьDSLСценарийССообщениями(СсылкаДиалога, DSL2);
		Если НЕ Рез2.Успех Тогда
			Результат.Сообщение = "Не удалось изменить объект: " + Рез2.Сообщение + " (проверьте наличие реквизита Комментарий у Контрагенты)";
			Возврат Результат;
		КонецЕсли;

		// 3. Проверяем, что объект создан и комментарий установлен
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		СсылкаСоздан = Неопределено;
		Для Каждого СтрокаИзмененных Из Диалог.ИзмененныеОбъекты Цикл
			Если ЗначениеЗаполнено(СтрокаИзмененных.СсылкаНаОбъект) Тогда
				СсылкаСоздан = СтрокаИзмененных.СсылкаНаОбъект;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		Если НЕ ЗначениеЗаполнено(СсылкаСоздан) Тогда
			Результат.Сообщение = "Не найден объект в ИзмененныеОбъекты";
			Возврат Результат;
		КонецЕсли;

		Объект = СсылкаСоздан.ПолучитьОбъект();
		Если Объект = Неопределено Тогда
			Результат.Сообщение = "Не удалось получить объект";
			Возврат Результат;
		КонецЕсли;

		Попытка
			КомментарийФакт = СокрЛП(Строка(Объект.Комментарий));
		Исключение
			Результат.Сообщение = "Реквизит Комментарий отсутствует у Контрагенты";
			Возврат Результат;
		КонецПопытки;

		Если КомментарийФакт <> КомментарийТест Тогда
			Результат.Сообщение = "Комментарий не совпадает: '" + КомментарийФакт + "' <> '" + КомментарийТест + "'";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: создание и изменение объекта через контекст";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест полного цикла агента в режиме холостого хода (mock для плана, DSL, обновления плана, проверки, summary).
//
Функция ТестПолныйЦиклАгентаХолостойХод() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Наименование = "Тест_ПолныйЦикл_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		НаименованиеЭкранир = СтрЗаменить(Наименование, "\", "\\");
		НаименованиеЭкранир = СтрЗаменить(НаименованиеЭкранир, """", "\""");
		DSLШаг1 = "{""steps"":[{""action"":""CreateReference"",""object_name"":""Контрагенты""},{""action"":""SetField"",""field_name"":""Наименование"",""value"":""" + НаименованиеЭкранир + """},{""action"":""Write""}]}";
		ПланОбновленный = "[{""Задача"":""CreateReference"",""Выполнена"":true,""Результат"":""Создан""},{""Задача"":""SetField"",""Выполнена"":true,""Результат"":""Установлено""},{""Задача"":""Write"",""Выполнена"":true,""Результат"":""Записан""}]";

		// Очередь mock: 1) план, 2) DSL, 3) обновление плана, 4) проверка (dsl), 5) summary (text)
		МассивMock = Новый Массив;
		МассивMock.Добавить(Новый Структура("Текст", "[""CreateReference"",""SetField"",""Write""]"));
		МассивMock.Добавить(Новый Структура("Текст, DSL", DSLШаг1, DSLШаг1));
		МассивMock.Добавить(Новый Структура("Текст", ПланОбновленный));
		МассивMock.Добавить(Новый Структура("Текст", "Да"));
		МассивMock.Добавить(Новый Структура("Текст", "Создан контрагент " + Наименование));
		ИИА_Сервер.УстановитьОчередьMockОтветов(СсылкаДиалога, МассивMock);

		ИИА_Сервер.ОтправитьСообщениеСервера(СсылкаДиалога, Перечисления.ИИА_ТипДиалога.Агент, "Создай контрагента " + Наименование);
		ИИА_Сервер.УстановитьОркестраторВключен(СсылкаДиалога, Истина);
		НачальныеТокены = ИИА_Сервер.ПолучитьОбщееКоличествоТокенов(СсылкаДиалога);
		ИИА_Оркестратор.ВыполнитьЦикл(СсылкаДиалога, НачальныеТокены);

		ИИА_Сервер.ОчиститьОчередьMockОтветов(СсылкаДиалога);

		// Проверяем завершение
		Если ИИА_Сервер.ОркестраторВключенДляДиалога(СсылкаДиалога) Тогда
			Результат.Сообщение = "Оркестратор не завершился";
			Возврат Результат;
		КонецЕсли;

		// Проверяем, что объект создан
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		КоличествоВИзмененных = 0;
		Для Каждого СтрокаИзмененных Из Диалог.ИзмененныеОбъекты Цикл
			Если ЗначениеЗаполнено(СтрокаИзмененных.СсылкаНаОбъект) Тогда
				КоличествоВИзмененных = КоличествоВИзмененных + 1;
			КонецЕсли;
		КонецЦикла;
		Если КоличествоВИзмененных = 0 Тогда
			Результат.Сообщение = "ИзмененныеОбъекты пуст после цикла";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: полный цикл агента в холостом ходу";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Бесплатный тест на Лимит токенов на запуск. Использует mock — без вызова ИИ.
// Проверяет: при превышении лимита оркестратор останавливается и отправляет уведомление ПревышенЛимитТокенов.
//
Функция ТестЛимитТокеновНаЗапуск() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ЛимитБыло = Неопределено;
	Пользователь = "Администратор";
	Попытка
		НаборЗаписей = РегистрыСведений.ИИА_НастройкиПользователей.СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.Пользователь.Установить(Пользователь);
		НаборЗаписей.Прочитать();
		Если НаборЗаписей.Количество() = 0 Тогда
			Запись = НаборЗаписей.Добавить();
			Запись.Пользователь = Пользователь;
			ЛимитБыло = 0;
		Иначе
			Запись = НаборЗаписей[0];
			ЛимитБыло = Запись.ЛимитТокеновНаЗапуск;
		КонецЕсли;
		Запись.ЛимитТокеновНаЗапуск = 100;
		НаборЗаписей.Записать();
		Результат.Детали.Добавить("Лимит установлен: 100");

		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог(Пользователь, Перечисления.ИИА_ТипДиалога.Агент);
		Если НЕ ЗначениеЗаполнено(СсылкаДиалога) Тогда
			Результат.Сообщение = "Не удалось создать диалог";
			Возврат Результат;
		КонецЕсли;

		// Mock: первый ответ (планирование) с Usage 150 > лимит 100
		МассивMock = Новый Массив;
		МассивMock.Добавить(Новый Структура("Текст, Usage", "[""CreateReference"",""SetField"",""Write""]", Новый Структура("TotalTokens", 150)));
		ИИА_Сервер.УстановитьОчередьMockОтветов(СсылкаДиалога, МассивMock);

		ИИА_Сервер.ОтправитьСообщениеСервера(СсылкаДиалога, Перечисления.ИИА_ТипДиалога.Агент, "Создай контрагента Тест_Лимит");
		ИИА_Сервер.УстановитьОркестраторВключен(СсылкаДиалога, Истина);
		НачальныеТокены = ИИА_Сервер.ПолучитьОбщееКоличествоТокенов(СсылкаДиалога);
		ИИА_Оркестратор.ВыполнитьЦикл(СсылкаДиалога, НачальныеТокены);

		ИИА_Сервер.ОчиститьОчередьMockОтветов(СсылкаДиалога);

		Если ИИА_Сервер.ОркестраторВключенДляДиалога(СсылкаДиалога) Тогда
			Результат.Сообщение = "Оркестратор не остановился при превышении лимита";
			Возврат Результат;
		КонецЕсли;

		// Проверяем, что в логе есть запись о превышении лимита
		Диалог = СсылкаДиалога.ПолучитьОбъект();
		НайденоПревышение = Ложь;
		Для Каждого СтрокаСообщения Из Диалог.Сообщения Цикл
			Если СтрНайти(ВРег(СтрокаСообщения.Статус), "ЛИМИТ") > 0 Или СтрНайти(ВРег(СтрокаСообщения.Текст), "ЛИМИТ") > 0 Тогда
				НайденоПревышение = Истина;
				Результат.Детали.Добавить("Статус: " + СтрокаСообщения.Статус);
				Прервать;
			КонецЕсли;
		КонецЦикла;
		// Проверяем лог диалога
		Если НЕ НайденоПревышение Тогда
			ЛогДиалога = ИИА_Сервер.ПолучитьЛогДиалога(СсылкаДиалога);
			Если СтрНайти(ВРег(ЛогДиалога), "ПРЕВЫШЕН") > 0 Или СтрНайти(ВРег(ЛогДиалога), "ЛИМИТ") > 0 Тогда
				НайденоПревышение = Истина;
			КонецЕсли;
		КонецЕсли;
		Если НЕ НайденоПревышение Тогда
			Результат.Сообщение = "В логе/статусе не найдена запись о превышении лимита";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: лимит токенов на запуск сработал";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	// Восстановление лимита
	Если ЛимитБыло <> Неопределено Тогда
		Попытка
			НаборЗаписей = РегистрыСведений.ИИА_НастройкиПользователей.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.Пользователь.Установить(Пользователь);
			НаборЗаписей.Прочитать();
			Если НаборЗаписей.Количество() > 0 Тогда
				Запись = НаборЗаписей[0];
				Запись.ЛимитТокеновНаЗапуск = ЛимитБыло;
				НаборЗаписей.Записать();
			КонецЕсли;
		Исключение
			Результат.Детали.Добавить("Не удалось восстановить лимит: " + ОписаниеОшибки());
		КонецПопытки;
	КонецЕсли;
	Возврат Результат;
КонецФункции

// Тест качества RAG: нормализация и токенизация текста.
//
Функция ТестRAGНормализацияТекста() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		// РазрезатьCamelCase
		Разрез = ИИА_RAG_Текст.РазрезатьCamelCase("РеализацияТоваров");
		Если Разрез <> "Реализация Товаров" Тогда
			Результат.Сообщение = "РазрезатьCamelCase: ожидалось 'Реализация Товаров', получено '" + Разрез + "'";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("РазрезатьCamelCase: OK");

		// Нормализовать
		Норм = ИИА_RAG_Текст.Нормализовать("Реализация Товаров и Услуг");
		Если СтрНайти(Норм, "реализация") = 0 Или СтрНайти(Норм, "товаров") = 0 Тогда
			Результат.Сообщение = "Нормализовать: ожидался нижний регистр, получено '" + Норм + "'";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("Нормализовать: OK");

		// Токенизация (стоп-слова отфильтрованы)
		СтопСлова = ИИА_RAG_Настройки.ПолучитьСтопСлова();
		Токены = ИИА_RAG_Текст.Токенизировать("контрагент и накладная", СтопСлова);
		Если Токены.Найти("контрагент") = Неопределено Или Токены.Найти("накладная") = Неопределено Тогда
			Результат.Сообщение = "Токенизировать: ожидались 'контрагент', 'накладная', получено " + СтрСоединить(Токены, ", ");
			Возврат Результат;
		КонецЕсли;
		Если Токены.Найти("и") <> Неопределено Тогда
			Результат.Сообщение = "Токенизировать: стоп-слово 'и' не должно попасть в токены";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("Токенизировать: OK");

		// Стеммирование
		Стем = ИИА_RAG_Текст.Стеммировать("реализация");
		Если ПустаяСтрока(Стем) Или СтрДлина(Стем) < 4 Тогда
			Результат.Сообщение = "Стеммировать: ожидалась основа слова, получено '" + Стем + "'";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("Стеммировать: OK");

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: RAG нормализация текста";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест качества RAG: поиск по запросу. Индекс должен быть построен заранее (форма RAG).
// Примечание: run_tests.py по умолчанию выполняет UpdateDBCfg перед тестами — это может очищать
// данные расширения. Запускайте с --skip-update, чтобы сохранить индекс.
//
Функция ТестRAGПоискПоЗапросу() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		РезультатыПоиска = ИИА_RAG_Поиск.ВыполнитьПоиск("Контрагенты", 10);
		Если РезультатыПоиска.Количество() = 0 Тогда
			КолЧанков = ПолучитьКоличествоЗаписейРегистра("ИИА_Чанки");
			КолТокенов = ПолучитьКоличествоЗаписейРегистра("ИИА_ТокенИндекс");
			Результат.Успех = Истина;
			Результат.Сообщение = "Тест пропущен: поиск не вернул результатов";
			Результат.Детали.Добавить("ИИА_Чанки: " + Формат(КолЧанков, "ЧГ=0") + " записей, ИИА_ТокенИндекс: " + Формат(КолТокенов, "ЧГ=0"));
			Если КолЧанков > 0 И КолТокенов > 0 Тогда
				Результат.Детали.Добавить("Индекс есть в базе, но поиск не нашёл — проверьте логику (токены, IDF)");
			Иначе
				Результат.Детали.Добавить("Индекс пуст. run_tests.py по умолчанию выполняет UpdateDBCfg — он очищает данные расширения. Запустите: python run_tests.py --skip-update");
			КонецЕсли;
			Возврат Результат;
		КонецЕсли;

		НайденКонтрагенты = Ложь;
		Для Каждого СтрокаРез Из РезультатыПоиска Цикл
			Если СтрНайти(ВРег(СтрокаРез.Имя), "КОНТРАГЕНТ") > 0 Тогда
				НайденКонтрагенты = Истина;
				Результат.Детали.Добавить("Найден: " + СтрокаРез.Тип + "." + СтрокаРез.Имя + " (Score=" + СтрокаРез.Score + ")");
				Прервать;
			КонецЕсли;
		КонецЦикла;

		Если НЕ НайденКонтрагенты Тогда
			Результат.Сообщение = "В топ-10 по запросу 'Контрагенты' нет справочника Контрагенты. Топ: ";
			Для Индекс = 0 По Мин(4, РезультатыПоиска.Количество() - 1) Цикл
				СтрокаРез = РезультатыПоиска[Индекс];
				Результат.Сообщение = Результат.Сообщение + СтрокаРез.Имя + "; ";
			КонецЦикла;
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: RAG поиск по запросу";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест ядра метаданных: ОпределитьИдКонфигурации и ОбъектВЯдре.
// Бесплатный тест — без вызова ИИ и без доступа к базе.
//
Функция ТестЯдроМетаданных() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		// 1. Определение конфигурации по имени
		Если ИИА_ЯдроМетаданных.ОпределитьИдКонфигурации("Управление торговлей 11") <> "UT" Тогда
			Результат.Сообщение = "УТ: ожидался UT";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОпределитьИдКонфигурации УТ: OK");

		Если ИИА_ЯдроМетаданных.ОпределитьИдКонфигурации("ERP 2") <> "ERP" Тогда
			Результат.Сообщение = "ERP: ожидался ERP";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОпределитьИдКонфигурации ERP: OK");

		Если ИИА_ЯдроМетаданных.ОпределитьИдКонфигурации("Розница 2") <> "Roznica" Тогда
			Результат.Сообщение = "Розница: ожидался Roznica";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОпределитьИдКонфигурации Розница: OK");

		Если ИИА_ЯдроМетаданных.ОпределитьИдКонфигурации("Бухгалтерия предприятия 3.0") <> "BP" Тогда
			Результат.Сообщение = "БП: ожидался BP";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОпределитьИдКонфигурации БП: OK");

		Если ИИА_ЯдроМетаданных.ОпределитьИдКонфигурации("УправлениеНебольшойФирмой") <> "UNF" Тогда
			Результат.Сообщение = "УНФ: ожидался UNF";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОпределитьИдКонфигурации УНФ: OK");

		Если ИИА_ЯдроМетаданных.ОпределитьИдКонфигурации("КонфигурацияТест") <> "Test" Тогда
			Результат.Сообщение = "Test: ожидался Test";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОпределитьИдКонфигурации Test: OK");

		Если ИИА_ЯдроМетаданных.ОпределитьИдКонфигурации("НеизвестнаяКонфигурация") <> "Unknown" Тогда
			Результат.Сообщение = "Unknown: ожидался Unknown";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОпределитьИдКонфигурации Unknown: OK");

		// 2. ОбъектВЯдре для УНФ
		Если НЕ ИИА_ЯдроМетаданных.ОбъектВЯдре("Document", "ПриходнаяНакладная", "УправлениеНебольшойФирмой") Тогда
			Результат.Сообщение = "УНФ: ПриходнаяНакладная должна быть в ядре";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ИИА_ЯдроМетаданных.ОбъектВЯдре("Document", "ЧекККМ", "УправлениеНебольшойФирмой") Тогда
			Результат.Сообщение = "УНФ: ЧекККМ должна быть в ядре";
			Возврат Результат;
		КонецЕсли;
		Если ИИА_ЯдроМетаданных.ОбъектВЯдре("Document", "РеализацияТоваровУслуг", "УправлениеНебольшойФирмой") Тогда
			Результат.Сообщение = "УНФ: РеализацияТоваровУслуг не в ядре УНФ";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОбъектВЯдре УНФ: OK");

		// 3. ОбъектВЯдре для УТ
		Если НЕ ИИА_ЯдроМетаданных.ОбъектВЯдре("Document", "РеализацияТоваровУслуг", "Управление торговлей") Тогда
			Результат.Сообщение = "УТ: РеализацияТоваровУслуг должна быть в ядре";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ИИА_ЯдроМетаданных.ОбъектВЯдре("AccumReg", "ТоварыНаСкладах", "Управление торговлей") Тогда
			Результат.Сообщение = "УТ: ТоварыНаСкладах должен быть в ядре";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОбъектВЯдре УТ: OK");

		// 4. ОбъектВЯдре для Unknown — общие объекты
		Если НЕ ИИА_ЯдроМетаданных.ОбъектВЯдре("Document", "ЧекККМ", "КастомнаяКонфигурация") Тогда
			Результат.Сообщение = "Unknown: ЧекККМ должна быть в общем ядре";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("ОбъектВЯдре Unknown: OK");

		// 5. Пустое имя — не в ядре
		Если ИИА_ЯдроМетаданных.ОбъектВЯдре("Document", "", "УНФ") Тогда
			Результат.Сообщение = "Пустое имя не должно быть в ядре";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("Пустое имя: OK");

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: ядро метаданных";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест ядра метаданных для текущей базы УправлениеНебольшойФирмой.
// Проверяет: Метаданные.Имя распознаётся как УНФ, объекты ядра УНФ входят в ядро.
// Если база не УНФ — тест пропускается (успех).
//
Функция ТестЯдроМетаданныхУНФТекущаяБаза() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ИмяКонфигурации = Метаданные.Имя;
		ИдКонфигурации = ИИА_ЯдроМетаданных.ОпределитьИдКонфигурации(ИмяКонфигурации);

		Если ИдКонфигурации <> "UNF" Тогда
			Результат.Успех = Истина;
			Результат.Сообщение = "Пропущен: конфигурация '" + ИмяКонфигурации + "' не УНФ (ядро: " + ИдКонфигурации + ")";
			Результат.Детали.Добавить("Тест применим только к базе УправлениеНебольшойФирмой");
			Возврат Результат;
		КонецЕсли;

		Результат.Детали.Добавить("Конфигурация: " + ИмяКонфигурации + " -> UNF");

		// Объекты ядра УНФ, которые должны быть в базе
		ОбъектыУНФ = Новый Массив;
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "Document", "ПриходнаяНакладная"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "Document", "РасходнаяНакладная"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "Document", "ЧекККМ"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "Document", "ЧекККМВозврат"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "Document", "СчетНаОплату"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "Document", "ЗаказПокупателя"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "Document", "ЗаказПоставщику"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "Document", "ОтчетОРозничныхПродажах"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "Catalog", "Номенклатура"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "InfoReg", "ЦеныНоменклатуры"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "AccumReg", "Запасы"));
		ОбъектыУНФ.Добавить(Новый Структура("Тип, Имя", "AccumReg", "ЗапасыНаСкладах"));

		Для Каждого Объект Из ОбъектыУНФ Цикл
			Если НЕ ИИА_ЯдроМетаданных.ОбъектВЯдре(Объект.Тип, Объект.Имя, ИмяКонфигурации) Тогда
				Результат.Сообщение = "УНФ: " + Объект.Тип + "." + Объект.Имя + " должна быть в ядре";
				Возврат Результат;
			КонецЕсли;
			Результат.Детали.Добавить(Объект.Тип + "." + Объект.Имя + " в ядре: OK");
		КонецЦикла;

		// Объект не из ядра УНФ
		Если ИИА_ЯдроМетаданных.ОбъектВЯдре("Document", "РеализацияТоваровУслуг", ИмяКонфигурации) Тогда
			Результат.Сообщение = "УНФ: РеализацияТоваровУслуг не должна быть в ядре";
			Возврат Результат;
		КонецЕсли;
		Результат.Детали.Добавить("РеализацияТоваровУслуг не в ядре УНФ: OK");

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: ядро УНФ для текущей базы";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест качества RAG: поиск по синонимам (реализация -> расходная/накладная). Индекс должен быть построен заранее.
//
Функция ТестRAGПоискСинонимов() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		РезультатыПоиска = ИИА_RAG_Поиск.ВыполнитьПоиск("реализация товаров", 15);

		Если РезультатыПоиска.Количество() = 0 Тогда
			КолЧанков = ПолучитьКоличествоЗаписейРегистра("ИИА_Чанки");
			КолТокенов = ПолучитьКоличествоЗаписейРегистра("ИИА_ТокенИндекс");
			Результат.Успех = Истина;
			Результат.Сообщение = "Тест пропущен: поиск не вернул результатов";
			Результат.Детали.Добавить("ИИА_Чанки: " + Формат(КолЧанков, "ЧГ=0") + ", ИИА_ТокенИндекс: " + Формат(КолТокенов, "ЧГ=0"));
			Если КолЧанков = 0 И КолТокенов = 0 Тогда
				Результат.Детали.Добавить("Индекс пуст. Запустите с --skip-update, перестройте индекс в форме RAG");
			ИначеЕсли КолЧанков > 0 И КолТокенов > 0 Тогда
				Результат.Детали.Добавить("Индекс есть, но поиск не нашёл — проверьте логику");
			КонецЕсли;
			Возврат Результат;
		КонецЕсли;

		// Ожидаем документы с реализация/расходная/накладная в имени или синониме
		НайденРелевантный = Ложь;
		Для Каждого СтрокаРез Из РезультатыПоиска Цикл
			ИмяВРег = ВРег(СтрокаРез.Имя);
			СинВРег = ВРег(СтрокаРез.Синоним);
			Если СтрНайти(ИмяВРег, "РЕАЛИЗАЦИ") > 0 Или СтрНайти(ИмяВРег, "РАСХОДН") > 0 Или СтрНайти(ИмяВРег, "НАКЛАДН") > 0
				Или СтрНайти(СинВРег, "РЕАЛИЗАЦИ") > 0 Или СтрНайти(СинВРег, "РАСХОДН") > 0 Или СтрНайти(СинВРег, "НАКЛАДН") > 0 Тогда
				НайденРелевантный = Истина;
				Результат.Детали.Добавить("Синоним сработал: " + СтрокаРез.Тип + "." + СтрокаРез.Имя + " (" + СтрокаРез.Синоним + ")");
				Прервать;
			КонецЕсли;
		КонецЦикла;

		Если НЕ НайденРелевантный Тогда
			// Конфигурация может не содержать документов реализации — проверяем хотя бы релевантность по "товар"
			Для Каждого СтрокаРез Из РезультатыПоиска Цикл
				Если СтрНайти(ВРег(СтрокаРез.Имя + СтрокаРез.Синоним), "ТОВАР") > 0 Тогда
					НайденРелевантный = Истина;
					Результат.Детали.Добавить("Найден объект с 'товар': " + СтрокаРез.Имя);
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;

		Если НЕ НайденРелевантный Тогда
			Результат.Сообщение = "Поиск по синониму 'реализация' не нашёл релевантных документов. Топ: ";
			Для Индекс = 0 По Мин(3, РезультатыПоиска.Количество() - 1) Цикл
				СтрокаРез = РезультатыПоиска[Индекс];
				Результат.Сообщение = Результат.Сообщение + СтрокаРез.Имя + "; ";
			КонецЦикла;
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: RAG поиск по синонимам";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

// Тест RAG: объекты из ядра должны попадать в топ результатов для типовых запросов.
// Покрывает: ЗапасыНаСкладах, Контрагенты, Номенклатура, РасходнаяНакладная и др.
// Индекс должен быть построен (форма RAG). Если конфигурация не УНФ — тест пропускается.
//
Функция ТестRAGЯдроПоиск() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ИмяКонфигурации = Метаданные.Имя;
		ИдКонфигурации = ИИА_ЯдроМетаданных.ОпределитьИдКонфигурации(ИмяКонфигурации);

		Если ИдКонфигурации <> "UNF" Тогда
			Результат.Успех = Истина;
			Результат.Сообщение = "Пропущен: конфигурация '" + ИмяКонфигурации + "' не УНФ (ядро: " + ИдКонфигурации + ")";
			Результат.Детали.Добавить("Тест RAG-ядро применим к базе УправлениеНебольшойФирмой");
			Возврат Результат;
		КонецЕсли;

		// Запрос -> ожидаемые объекты (хотя бы один в топ-10)
		Проверки = Новый Массив;
		Проверки.Добавить(Новый Структура("Запрос, ОжидаемыеИмена", "остатки склад", "ЗапасыНаСкладах"));
		Проверки.Добавить(Новый Структура("Запрос, ОжидаемыеИмена", "запасы склад", "ЗапасыНаСкладах"));
		Проверки.Добавить(Новый Структура("Запрос, ОжидаемыеИмена", "контрагенты", "Контрагенты,КонтрагентыМП"));
		Проверки.Добавить(Новый Структура("Запрос, ОжидаемыеИмена", "номенклатура", "Номенклатура"));
		Проверки.Добавить(Новый Структура("Запрос, ОжидаемыеИмена", "реализация товары", "РасходнаяНакладная,ПриходнаяНакладная"));
		Проверки.Добавить(Новый Структура("Запрос, ОжидаемыеИмена", "заказы", "ЗаказПокупателя,ЗаказПоставщику,ЗаказыПокупателей"));

		Для Каждого Проверка Из Проверки Цикл
			РезультатыПоиска = ИИА_RAG_Поиск.ВыполнитьПоиск(Проверка.Запрос, 10);
			Если РезультатыПоиска.Количество() = 0 Тогда
				Результат.Сообщение = "Запрос '" + Проверка.Запрос + "': индекс пуст или поиск не вернул результатов";
				Результат.Детали.Добавить("Перестройте индекс в форме RAG");
				Возврат Результат;
			КонецЕсли;

			ОжидаемыеИмена = СтрРазделить(Проверка.ОжидаемыеИмена, ",", Ложь);
			НайденОжидаемый = Ложь;
			Для Каждого СтрокаРез Из РезультатыПоиска Цикл
				Для Каждого ОжидИмя Из ОжидаемыеИмена Цикл
					Если СтрокаРез.Имя = СокрЛП(ОжидИмя) Тогда
						НайденОжидаемый = Истина;
						Результат.Детали.Добавить("'" + Проверка.Запрос + "': " + СтрокаРез.Тип + "." + СтрокаРез.Имя + " в топе (Rank=" + СтрокаРез.Rank + ")");
						Прервать;
					КонецЕсли;
				КонецЦикла;
				Если НайденОжидаемый Тогда
					Прервать;
				КонецЕсли;
			КонецЦикла;

			Если НЕ НайденОжидаемый Тогда
				Результат.Сообщение = "Запрос '" + Проверка.Запрос + "': ожидался " + Проверка.ОжидаемыеИмена + " в топ-10";
				ТопИмена = Новый Массив;
				Для Индекс = 0 По Мин(4, РезультатыПоиска.Количество() - 1) Цикл
					ТопИмена.Добавить(РезультатыПоиска[Индекс].Имя);
				КонецЦикла;
				Результат.Детали.Добавить("Топ: " + СтрСоединить(ТопИмена, "; "));
				Возврат Результат;
			КонецЕсли;
		КонецЦикла;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: RAG возвращает объекты ядра для типовых запросов";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактDSL() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Реестр = ИИА_DSLКонтракт.ПолучитьРеестрКонтрактовDSL();
		Если Реестр = Неопределено Или Реестр.Количество() = 0 Тогда
			Результат.Сообщение = "Реестр DSL-контрактов пуст";
			Возврат Результат;
		КонецЕсли;
		КонтрактRunQuery = Реестр.Получить("RunQuery");
		Если КонтрактRunQuery = Неопределено Тогда
			Результат.Сообщение = "В реестре нет контракта RunQuery";
			Возврат Результат;
		КонецЕсли;
		Если НЕ КонтрактRunQuery.Свойство("capability") Или КонтрактRunQuery.capability <> "data.read" Тогда
			Результат.Сообщение = "Контракт RunQuery должен требовать capability=data.read";
			Возврат Результат;
		КонецЕсли;
		Если НЕ КонтрактRunQuery.Свойство("supports_safe_retries") Или НЕ КонтрактRunQuery.supports_safe_retries Тогда
			Результат.Сообщение = "Контракт RunQuery должен поддерживать safe retries";
			Возврат Результат;
		КонецЕсли;
		КонтрактWrite = Реестр.Получить("Write");
		Если КонтрактWrite = Неопределено Тогда
			Результат.Сообщение = "В реестре нет контракта Write";
			Возврат Результат;
		КонецЕсли;
		Если НЕ КонтрактWrite.Свойство("supports_safe_retries") Или КонтрактWrite.supports_safe_retries Тогда
			Результат.Сообщение = "Контракт Write не должен считаться безопасным для повторов без operation_id";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: DSL-контракт валиден";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестПромптТребуетOperationId() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ПромптШага = ИИА_Промты.ПолучитьПромптГенерацииСледующегоШага("{""plan_version"":1,""goal"":""Тест"",""steps"":[{""id"":""step_1"",""kind"":""create_reference"",""title"":""Создать контрагента"",""depends_on"":[],""input"":{""object_name"":""Контрагенты"",""name"":""Тест""}},{""id"":""step_2"",""kind"":""write_object"",""title"":""Записать объект"",""depends_on"":[""step_1""],""input"":{}}]}", "{""id"":""step_2"",""kind"":""write_object"",""title"":""Записать объект"",""depends_on"":[""step_1""],""input"":{}}", "Факты", "", "");
		ПромптПроверки = ИИА_Промты.ПолучитьПромптExecutorПроверкиDSL("{""dsl_version"":2,""steps"":[{""action"":""Write""}]}", "");
		ПромптИсправления = ИИА_Промты.ПолучитьПромптИсправленияDSL("write_failed", "{""dsl_version"":2,""steps"":[{""action"":""SaveToStorage"",""key"":""x"",""data"":""y""}]}", "");

		Если СтрНайти(ПромптШага, "operation_id") = 0 Тогда
			Результат.Сообщение = "Промпт генерации шага должен требовать operation_id";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ПромптШага, "CreateReference") = 0
			ИЛИ СтрНайти(ПромптШага, "Write") = 0
			ИЛИ СтрНайти(ПромптШага, "SaveToStorage") = 0
			ИЛИ СтрНайти(ПромптШага, "ForEach") = 0 Тогда
			Результат.Сообщение = "Промпт генерации шага должен перечислять write-side действия, требующие operation_id";
			Возврат Результат;
		КонецЕсли;

		Если СтрНайти(ПромптПроверки, "operation_id") = 0 Тогда
			Результат.Сообщение = "Промпт проверки DSL должен требовать operation_id для неидемпотентных действий";
			Возврат Результат;
		КонецЕсли;

		Если СтрНайти(ПромптИсправления, "operation_id") = 0 Тогда
			Результат.Сообщение = "Промпт исправления DSL должен сохранять или добавлять operation_id";
			Возврат Результат;
		КонецЕсли;

		Если СтрНайти(ПромптИсправления, "не меняй его operation_id") = 0 Тогда
			Результат.Сообщение = "Промпт исправления DSL должен запрещать менять operation_id при повторе";
			Возврат Результат;
		КонецЕсли;

		Результат.Детали.Добавить("Промпт генерации шага требует operation_id");
		Результат.Детали.Добавить("Промпт проверки DSL требует operation_id");
		Результат.Детали.Добавить("Промпт исправления DSL сохраняет operation_id");
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: промпты требуют и сохраняют operation_id";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактПровайдера() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Контракт = ИИА_Провайдеры.ПолучитьКонтрактПровайдера();
		Если Контракт = Неопределено Тогда
			Результат.Сообщение = "Контракт провайдера не получен";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Контракт.Свойство("messages")
			ИЛИ НЕ Контракт.Свойство("temperature")
			ИЛИ НЕ Контракт.Свойство("tool_choice")
			ИЛИ НЕ Контракт.Свойство("response_format") Тогда
			Результат.Сообщение = "Контракт провайдера не содержит обязательные ключи";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: контракт провайдера валиден";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестСтратегияОтветаПровайдера() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ПараметрыИИ = Новый Структура;
		ПараметрыИИ.Вставить("ResponseFormat", Новый Структура("type", "json_object"));

		СтратегияDSL = ИИА_Провайдеры.ПостроитьСтратегиюОтвета(ПараметрыИИ, "dsl");
		Если НЕ СтратегияDSL.UseBuiltInToolCall Тогда
			Результат.Сообщение = "Для dsl должен использоваться встроенный tool-calling";
			Возврат Результат;
		КонецЕсли;
		Если СтратегияDSL.UseJsonMode Тогда
			Результат.Сообщение = "Для dsl нельзя одновременно включать json-mode";
			Возврат Результат;
		КонецЕсли;
		Если СтратегияDSL.AllowAttachmentTools Тогда
			Результат.Сообщение = "Для dsl не должны подмешиваться tools вложений";
			Возврат Результат;
		КонецЕсли;

		СтратегияText = ИИА_Провайдеры.ПостроитьСтратегиюОтвета(Новый Структура, "text");
		Если СтратегияText.UseBuiltInToolCall Тогда
			Результат.Сообщение = "Для text не должен включаться встроенный tool-calling";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СтратегияText.AllowAttachmentTools Тогда
			Результат.Сообщение = "Для text должны быть разрешены tools вложений";
			Возврат Результат;
		КонецЕсли;

		СтратегияJson = ИИА_Провайдеры.ПостроитьСтратегиюОтвета(ПараметрыИИ, "json_object");
		Если НЕ СтратегияJson.UseJsonMode Тогда
			Результат.Сообщение = "Для json_object должен включаться json-mode";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: стратегия ответа провайдера разделяет tool/json/text режимы";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестDSLТребуетНативныйToolCalling() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ПараметрыИИ = Новый Структура;
		ПараметрыИИ.Вставить("ExpectedResponseFormat", "dsl");
		Распознанный = ИИА_Провайдеры.РаспознатьОтветИИ("{""dsl_version"":2,""steps"":[{""action"":""ShowInfo"",""message"":""ok""}]}", "Чат", ПараметрыИИ);
		Если Распознанный.ТипОтвета <> "ОшибкаКонтракта" Тогда
			Результат.Сообщение = "В dsl-режиме текстовый fallback должен быть отключен";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Распознанный.Текст, "native tool-calling") = 0 Тогда
			Результат.Сообщение = "Ошибка контракта должна явно требовать native tool-calling";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: dsl-режим требует native tool-calling";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестПредставлениеОшибкиИнструмента() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Ошибка = ИИА_Провайдеры.СформироватьОшибкуИнструмента("connection_error", "network", "Проверьте сеть и URL провайдера.", "SSL handshake timeout");
		ТекстОшибки = ИИА_Провайдеры.ПредставлениеОшибкиИнструмента(Ошибка);
		Если СтрНайти(ТекстОшибки, "connection_error") = 0 Тогда
			Результат.Сообщение = "Представление ошибки должно содержать code";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ТекстОшибки, "network") = 0 Тогда
			Результат.Сообщение = "Представление ошибки должно содержать category";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ТекстОшибки, "SSL handshake timeout") = 0 Тогда
			Результат.Сообщение = "Представление ошибки должно содержать details";
			Возврат Результат;
		КонецЕсли;
		Если ТекстОшибки = "Структура" Тогда
			Результат.Сообщение = "Представление ошибки не должно схлопываться в 'Структура'";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: ошибка инструмента сериализуется в читаемый текст";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестСписокМоделейOpenAI() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Модели = ИИА_GitsellСервер.Gitsell_СписокМоделей();
		Если Модели = Неопределено ИЛИ Модели.Количество() = 0 Тогда
			Результат.Сообщение = "Список моделей пуст";
			Возврат Результат;
		КонецЕсли;
		ОжидаемыеМодели = Новый Массив;
		ОжидаемыеМодели.Добавить("gpt-4.1");
		ОжидаемыеМодели.Добавить("gpt-4.1-mini");
		ОжидаемыеМодели.Добавить("gpt-4.1-nano");
		ОжидаемыеМодели.Добавить("gpt-4o");
		ОжидаемыеМодели.Добавить("gpt-4o-mini");
		ОжидаемыеМодели.Добавить("gpt-5.4");
		ОжидаемыеМодели.Добавить("gpt-5.4-mini");
		ОжидаемыеМодели.Добавить("gpt-5.4-nano");
		ОжидаемыеМодели.Добавить("gpt-5.4-thinking");

		Для Каждого ИмяМодели Из ОжидаемыеМодели Цикл
			Найдена = Ложь;
			Для Каждого Модель Из Модели Цикл
				Если Модель.Свойство("id") И Строка(Модель.id) = ИмяМодели Тогда
					Найдена = Истина;
					Прервать;
				КонецЕсли;
			КонецЦикла;
			Если НЕ Найдена Тогда
				Результат.Сообщение = "В списке моделей отсутствует " + ИмяМодели;
				Возврат Результат;
			КонецЕсли;
		КонецЦикла;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: список OpenAI моделей обновлен";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестCapabilityПроверка() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Шаг = Новый Структура("action", "CreateReference");
		Контекст = Новый Структура;
		Контекст.Вставить("DSL_Capabilities", Новый Массив);
		Контекст.DSL_Capabilities.Добавить("data.read");
		Проверка = ИИА_DSLКонтракт.ПроверитьCapabilityШага(Шаг, Контекст);
		Если Проверка.Успех Тогда
			Результат.Сообщение = "Проверка capability должна блокировать CreateReference без data.write.reference";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: capability-проверка работает";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактПромптовDSLВерсия() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ТекущаяВерсияDSL = ИИА_DSLКонтракт.ПолучитьВерсииDSLКонтракта().Текущая;
		Промпт = ИИА_Промты.ПолучитьПромптГенерацииСледующегоШага("{""plan_version"":1,""goal"":""Тест"",""steps"":[{""id"":""step_1"",""kind"":""run_query"",""title"":""Тестовый запрос"",""depends_on"":[],""input"":{}}]}", "{""id"":""step_1"",""kind"":""run_query"",""title"":""Тестовый запрос"",""depends_on"":[],""input"":{}}", "Факты", "", "");
		МаркерВерсии = """dsl_version"":" + Строка(ТекущаяВерсияDSL);
		МаркерВерсииТекст = "dsl_version=" + Строка(ТекущаяВерсияDSL);
		Если СтрНайти(Промпт, МаркерВерсии) = 0 И СтрНайти(Промпт, МаркерВерсииТекст) = 0 Тогда
			Результат.Сообщение = "Промпт должен требовать текущую версию DSL: " + Строка(ТекущаяВерсияDSL);
			Возврат Результат;
		КонецЕсли;
		Если ТекущаяВерсияDSL <> 1 И СтрНайти(Промпт, """dsl_version"":1") > 0 Тогда
			Результат.Сообщение = "В промпте найден устаревший dsl_version=1 при текущей версии " + Строка(ТекущаяВерсияDSL);
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: промпт использует актуальную версию DSL";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактПромптовДействия() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Схемы = ИИА_Промты.СформироватьБлокСхемДействийDSL();
		Если ПустаяСтрока(Схемы) Тогда
			Результат.Сообщение = "Схемы действий DSL в промптах пусты";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(Схемы), "DELETE{") > 0 Тогда
			Результат.Сообщение = "В схемах промптов найдено невалидное действие Delete";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(Схемы), "GETMETADATA{FILTER?}") = 0 Тогда
			Результат.Сообщение = "Схемы должны содержать GetMetadata{filter?}";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: схемы действий в промптах валидны";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактПромптовСекции() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ПустаяИстория = Новый ТаблицаЗначений;
		ПустаяИстория.Колонки.Добавить("Автор");
		ПустаяИстория.Колонки.Добавить("ТипСообщения");
		ПустаяИстория.Колонки.Добавить("Текст");
		ПустаяИстория.Колонки.Добавить("ТекстКода");
		ПакетПромпта = ИИА_Промты.СформироватьПромпт(Перечисления.ИИА_ТипДиалога.Агент, "Чат", "тест", ПустаяИстория, "", Неопределено, Ложь);
		СистемныйПромпт = ПакетПромпта.Сообщения[0].content;
		Если СтрНайти(ВРег(СистемныйПромпт), "PROMPT-VERSION:") = 0 Тогда
			Результат.Сообщение = "В системном промпте отсутствует секция Prompt-Version";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(СистемныйПромпт), "КОНТРАКТ_ОТВЕТА") = 0 Тогда
			Результат.Сообщение = "В системном промпте отсутствует секция контракта ответа";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(СистемныйПромпт), "НИКАКОГО ТЕКСТА ВНЕ JSON") = 0 Тогда
			Результат.Сообщение = "В системном промпте отсутствует явный запрет текста вне JSON";
			Возврат Результат;
		КонецЕсли;
		ПромптШага = ИИА_Промты.ПолучитьПромптГенерацииСледующегоШага("{""plan_version"":1,""goal"":""Тест"",""steps"":[{""id"":""step_1"",""kind"":""run_query"",""title"":""Тест"",""depends_on"":[],""input"":{}}]}", "{""id"":""step_1"",""kind"":""run_query"",""title"":""Тест"",""depends_on"":[],""input"":{}}", "Факты", "", "");
		Если СтрНайти(ВРег(ПромптШага), "RESPONSE_MODE: NEXT_STEP_DSL") = 0 Тогда
			Результат.Сообщение = "Промпт генерации шага должен содержать response_mode=next_step_dsl";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: обязательные секции промптов присутствуют";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактStateMachine() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(Неопределено);
		Если Состояние = Неопределено Тогда
			Результат.Сообщение = "Состояние оркестратора не инициализировано";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Состояние.Свойство("ТекущаяСтадия") Тогда
			Результат.Сообщение = "Контракт состояния должен содержать ТекущаяСтадия";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: контракт state machine доступен";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция НайтиЗначениеВМассиве(МассивЗначений, ОжидаемоеЗначение)
	Если ТипЗнч(МассивЗначений) <> Тип("Массив") Тогда
		Возврат Ложь;
	КонецЕсли;
	Для Каждого ЗначениеМассива Из МассивЗначений Цикл
		Если СокрЛП(Строка(ЗначениеМассива)) = СокрЛП(Строка(ОжидаемоеЗначение)) Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	Возврат Ложь;
КонецФункции

Функция ТестКонтрактSubgraphResult() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СубграфРезультат = ИИА_RuntimeКонтракты.СоздатьРезультатПодграфа("TestSubgraph", Истина, "Test->Next", Неопределено, Ложь, "continue");
		Если СубграфРезультат = Неопределено Тогда
			Результат.Сообщение = "Subgraph result не создан";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СубграфРезультат.Свойство("SubgraphName") Или СубграфРезультат.SubgraphName <> "TestSubgraph" Тогда
			Результат.Сообщение = "Subgraph result должен содержать SubgraphName";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СубграфРезультат.Свойство("Transition") Или СубграфРезультат.Transition <> "Test->Next" Тогда
			Результат.Сообщение = "Subgraph result должен содержать Transition";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СубграфРезультат.Свойство("ApprovalPending") Тогда
			Результат.Сообщение = "Subgraph result должен содержать ApprovalPending";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СубграфРезультат.Свойство("Decision") Тогда
			Результат.Сообщение = "Subgraph result должен содержать Decision";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СубграфРезультат.Свойство("Details") Тогда
			Результат.Сообщение = "Subgraph result должен содержать Details";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СубграфРезультат.Свойство("NextStage") Тогда
			Результат.Сообщение = "Subgraph result должен содержать NextStage";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СубграфРезультат.Свойство("Reason") Тогда
			Результат.Сообщение = "Subgraph result должен содержать Reason";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СубграфРезультат.Свойство("Policy") Тогда
			Результат.Сообщение = "Subgraph result должен содержать Policy";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СубграфРезультат.Свойство("StepId") Тогда
			Результат.Сообщение = "Subgraph result должен содержать StepId";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: контракт SubgraphResult валиден";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактTransitionRegistry() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Реестр = ИИА_RuntimeКонтракты.ПолучитьРеестрTransitionContract();
		Если Реестр = Неопределено Тогда
			Результат.Сообщение = "Transition registry не создан";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Реестр.Свойство("Decisions") Или НЕ Реестр.Свойство("Reasons") Или НЕ Реестр.Свойство("Policies") Тогда
			Результат.Сообщение = "Transition registry должен содержать Decisions/Reasons/Policies";
			Возврат Результат;
		КонецЕсли;
		Если НЕ НайтиЗначениеВМассиве(Реестр.Decisions, "continue")
			ИЛИ НЕ НайтиЗначениеВМассиве(Реестр.Decisions, "await_approval")
			ИЛИ НЕ НайтиЗначениеВМассиве(Реестр.Decisions, "replan") Тогда
			Результат.Сообщение = "Transition registry должен содержать базовые decisions";
			Возврат Результат;
		КонецЕсли;
		Если НЕ НайтиЗначениеВМассиве(Реестр.Reasons, "plan_ready")
			ИЛИ НЕ НайтиЗначениеВМассиве(Реестр.Reasons, "approval_required")
			ИЛИ НЕ НайтиЗначениеВМассиве(Реестр.Reasons, "resume_same_operation_id") Тогда
			Результат.Сообщение = "Transition registry должен содержать базовые reasons";
			Возврат Результат;
		КонецЕсли;
		Если НЕ НайтиЗначениеВМассиве(Реестр.Policies, "safe-path")
			ИЛИ НЕ НайтиЗначениеВМассиве(Реестр.Policies, "fast-path")
			ИЛИ НЕ НайтиЗначениеВМассиве(Реестр.Policies, "write_retry_same_operation_policy") Тогда
			Результат.Сообщение = "Transition registry должен содержать базовые policies";
			Возврат Результат;
		КонецЕсли;

		СубграфРезультат = ИИА_RuntimeКонтракты.СоздатьРезультатПодграфа("TestSubgraph", Истина, "Test->Next", Неопределено, Ложь, "unexpected_decision", Неопределено, "Execute", "unexpected_reason", "unexpected_policy", "step_1");
		Если СубграфРезультат.Decision <> "" Или СубграфРезультат.Reason <> "" Или СубграфРезультат.Policy <> "" Тогда
			Результат.Сообщение = "SubgraphResult должен отбрасывать значения вне transition registry";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: transition registry и нормализация контракта валидны";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактRuntimeState() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(Неопределено);
		Если НЕ Состояние.Свойство("execution_policy") Тогда
			Результат.Сообщение = "Runtime state должен содержать execution_policy";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Состояние.Свойство("active_subgraph") Тогда
			Результат.Сообщение = "Runtime state должен содержать active_subgraph";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Состояние.Свойство("subgraph_history") Тогда
			Результат.Сообщение = "Runtime state должен содержать subgraph_history";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Состояние.Свойство("decision_log") Тогда
			Результат.Сообщение = "Runtime state должен содержать decision_log";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Состояние.Свойство("active_step_id") Тогда
			Результат.Сообщение = "Runtime state должен содержать active_step_id";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Состояние.Свойство("thread_id") Тогда
			Результат.Сообщение = "Runtime state должен содержать thread_id";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: runtime state содержит новые contract fields";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактDecisionLogRecord() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "execution_policy", "safe-path", "PlanningSubgraph", Новый Структура("source,step_id,policy,reason", "planning", "step_1", "planner_policy", "next_step_generation"));
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если ТипЗнч(Состояние.decision_log) <> Тип("Массив") Или Состояние.decision_log.Количество() = 0 Тогда
			Результат.Сообщение = "Decision log должен содержать запись";
			Возврат Результат;
		КонецЕсли;
		ЗаписьРешения = Состояние.decision_log[Состояние.decision_log.Количество() - 1];
		Если НЕ ЗаписьРешения.Свойство("kind") Или НЕ ЗаписьРешения.Свойство("decision") Тогда
			Результат.Сообщение = "Decision record должен содержать kind и decision";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ЗаписьРешения.Свойство("source") Или ЗаписьРешения.source <> "planning" Тогда
			Результат.Сообщение = "Decision record должен содержать stable field source";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ЗаписьРешения.Свойство("step_id") Или ЗаписьРешения.step_id <> "step_1" Тогда
			Результат.Сообщение = "Decision record должен содержать stable field step_id";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ЗаписьРешения.Свойство("policy") Или ЗаписьРешения.policy <> "planner_policy" Тогда
			Результат.Сообщение = "Decision record должен содержать stable field policy";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ЗаписьРешения.Свойство("reason") Или ЗаписьРешения.reason <> "next_step_generation" Тогда
			Результат.Сообщение = "Decision record должен содержать stable field reason";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: decision log record имеет стабильный контракт";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактDecisionLogPolicyRegistry() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "unknown_kind", "anything", "PlanningSubgraph");
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "approval", "unexpected_status", "ReadQuerySubgraph");
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "next_step_selected", "step_42", "PlanningSubgraph", Новый Структура("source,step_id,reason", "planning", "step_42", "planner_generated"));

		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если ТипЗнч(Состояние.decision_log) <> Тип("Массив") Тогда
			Результат.Сообщение = "Decision log должен оставаться массивом";
			Возврат Результат;
		КонецЕсли;
		Если Состояние.decision_log.Количество() <> 1 Тогда
			Результат.Сообщение = "Policy registry должен отбрасывать invalid kind и invalid fixed decision";
			Возврат Результат;
		КонецЕсли;
		ЗаписьРешения = Состояние.decision_log[0];
		Если ЗаписьРешения.kind <> "next_step_selected" Или ЗаписьРешения.decision <> "step_42" Тогда
			Результат.Сообщение = "Policy registry должен пропускать допустимый freeform decision";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: decision log policy registry валидирует записи на входе";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактTracePolicyRegistry() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ЗафиксироватьАктивныйПодграф(СсылкаДиалога, "PlanningSubgraph", "unknown_subgraph_decision");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "unknown_checkpoint_type", "Plan");

		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если ТипЗнч(Состояние.subgraph_history) <> Тип("Массив") Или Состояние.subgraph_history.Количество() <> 1 Тогда
			Результат.Сообщение = "Subgraph history должен сохранять запись";
			Возврат Результат;
		КонецЕсли;
		ЗаписьПодграфа = Состояние.subgraph_history[0];
		Если ЗаписьПодграфа.Свойство("decision") И НЕ ПустаяСтрока(Строка(ЗаписьПодграфа.decision)) Тогда
			Результат.Сообщение = "Trace policy registry должен очищать invalid subgraph decision";
			Возврат Результат;
		КонецЕсли;

		ИсторияЧекпоинтов = ИИА_Сервер.ПолучитьИсториюЧекпоинтовОркестратора(СсылкаДиалога, 10);
		Если ТипЗнч(ИсторияЧекпоинтов) <> Тип("Массив") Тогда
			Результат.Сообщение = "Checkpoint history должен оставаться массивом";
			Возврат Результат;
		КонецЕсли;
		Если ИсторияЧекпоинтов.Количество() <> 0 Тогда
			Результат.Сообщение = "Trace policy registry не должен сохранять invalid checkpoint type";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: trace policy registry валидирует subgraph/checkpoint события";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестDecisionTimeline() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "execution_policy", "safe-path", "PlanningSubgraph", Новый Структура("source,step_id,policy,reason", "planning", "step_1", "planner_policy", "next_step_generation"));
		Таймлайн = ИИА_Сервер.ПолучитьDecisionTimeline(СсылкаДиалога, 10);
		Если ПустаяСтрока(Таймлайн) Тогда
			Результат.Сообщение = "Decision timeline не должен быть пустым";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Таймлайн, "execution_policy -> safe-path") = 0 Тогда
			Результат.Сообщение = "Decision timeline должен содержать kind и decision";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Таймлайн, "source=planning") = 0 Тогда
			Результат.Сообщение = "Decision timeline должен содержать source";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Таймлайн, "step=step_1") = 0 Тогда
			Результат.Сообщение = "Decision timeline должен содержать step_id";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: decision timeline форматируется для UI";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактRuntimeTrace() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);
		ИИА_Сервер.УстановитьThreadId(СсылкаДиалога, "thread_test");
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Plan", "test_transition", Ложь);
		ИИА_Сервер.ЗафиксироватьАктивныйПодграф(СсылкаДиалога, "PlanningSubgraph", "next_step_generation", Новый Структура("active_step_id", "step_1"));
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "execution_policy", "safe-path", "PlanningSubgraph", Новый Структура("source,step_id,policy,reason", "planning", "step_1", "planner_policy", "next_step_generation"));
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "typed_plan_saved", "Plan", Новый Структура("step_id", "step_1"), "test_transition");

		Trace = ИИА_Сервер.ПолучитьRuntimeTrace(СсылкаДиалога, 50);
		Если ТипЗнч(Trace) <> Тип("Массив") Или Trace.Количество() = 0 Тогда
			Результат.Сообщение = "Runtime trace должен возвращать массив событий";
			Возврат Результат;
		КонецЕсли;

		НайденDecision = Ложь;
		НайденCheckpoint = Ложь;
		Для Каждого Событие Из Trace Цикл
			Если ТипЗнч(Событие) <> Тип("Структура") Тогда
				Результат.Сообщение = "Runtime trace должен содержать структуры";
				Возврат Результат;
			КонецЕсли;
			Если НЕ Событие.Свойство("timestamp")
				ИЛИ НЕ Событие.Свойство("trace_type")
				ИЛИ НЕ Событие.Свойство("source")
				ИЛИ НЕ Событие.Свойство("trace_id")
				ИЛИ НЕ Событие.Свойство("thread_id") Тогда
				Результат.Сообщение = "Runtime trace event должен содержать базовые contract fields";
				Возврат Результат;
			КонецЕсли;
			Если Событие.trace_type = "decision_log" И Событие.kind = "execution_policy" Тогда
				НайденDecision = Истина;
				Если Событие.step_id <> "step_1" Или Событие.policy <> "planner_policy" Тогда
					Результат.Сообщение = "Decision event должен сохранять stable fields step_id/policy";
					Возврат Результат;
				КонецЕсли;
			КонецЕсли;
			Если Событие.trace_type = "checkpoint" И Событие.kind = "typed_plan_saved" Тогда
				НайденCheckpoint = Истина;
			КонецЕсли;
		КонецЦикла;

		Если НЕ НайденDecision Тогда
			Результат.Сообщение = "Runtime trace должен включать decision_log events";
			Возврат Результат;
		КонецЕсли;
		Если НЕ НайденCheckpoint Тогда
			Результат.Сообщение = "Runtime trace должен включать checkpoint events";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: runtime trace возвращает нормализованные события";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактCheckpointHistory() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);
		ИИА_Сервер.УстановитьThreadId(СсылкаДиалога, "thread_checkpoint_test");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "typed_plan_saved", "Plan", Новый Структура("step_id,reason", "step_1", "checkpoint_one"), "Plan->Execute");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "execute_subgraph_entered", "Execute", Новый Структура("step_id,policy_id,reason", "step_2", "safe-path", "checkpoint_two"), "Execute->Validate");

		История = ИИА_Сервер.ПолучитьИсториюЧекпоинтовОркестратора(СсылкаДиалога, 10);
		Если ТипЗнч(История) <> Тип("Массив") Или История.Количество() <> 2 Тогда
			Результат.Сообщение = "Checkpoint history должен возвращать нормализованный массив";
			Возврат Результат;
		КонецЕсли;

		Последний = История[История.Количество() - 1];
		Если НЕ Последний.Свойство("checkpoint_index")
			Или НЕ Последний.Свойство("is_latest")
			Или НЕ Последний.Свойство("thread_id")
			Или НЕ Последний.Свойство("step_id")
			Или НЕ Последний.Свойство("reason") Тогда
			Результат.Сообщение = "Checkpoint history должен содержать contract fields";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Последний.is_latest Или Последний.step_id <> "step_2" Или Последний.thread_id <> "thread_checkpoint_test" Тогда
			Результат.Сообщение = "Checkpoint history должен корректно нормализовать latest/thread/step";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: checkpoint history возвращает нормализованные записи";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактCheckpointDiff() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "typed_plan_saved", "Plan", Новый Структура("step_id,reason", "step_1", "checkpoint_one"), "Plan->Execute");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "execute_subgraph_entered", "Execute", Новый Структура("step_id,reason", "step_2", "checkpoint_two"), "Execute->Validate");

		Diff = ИИА_Сервер.ПолучитьDiffЧекпоинтовОркестратора(СсылкаДиалога, 0, 1);
		Если Diff = Неопределено
			Или НЕ Diff.Свойство("before")
			Или НЕ Diff.Свойство("after")
			Или НЕ Diff.Свойство("changed_fields") Тогда
			Результат.Сообщение = "Checkpoint diff должен возвращать before/after/changed_fields";
			Возврат Результат;
		КонецЕсли;
		Если ТипЗнч(Diff.changed_fields) <> Тип("Массив") Или Diff.changed_fields.Количество() = 0 Тогда
			Результат.Сообщение = "Checkpoint diff должен находить измененные поля";
			Возврат Результат;
		КонецЕсли;

		НайденStage = Ложь;
		Для Каждого Изменение Из Diff.changed_fields Цикл
			Если Изменение.Свойство("field") И Изменение.field = "stage" Тогда
				НайденStage = Истина;
			КонецЕсли;
		КонецЦикла;
		Если НЕ НайденStage Тогда
			Результат.Сообщение = "Checkpoint diff должен фиксировать изменение stage";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: checkpoint diff возвращает измененные поля";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестResumeFromCheckpoint() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "typed_plan_saved", "Plan", Новый Структура("step_id,execution_policy,subgraph", "step_restore", "safe-path", "PlanningSubgraph"), "Plan->Execute");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "recovery_decision_abort", "Summarize", Новый Структура("step_id", "step_other"), "Validate->Summarize");
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Summarize", "test_changed", Ложь);
		ИИА_Сервер.УстановитьActiveStepId(СсылкаДиалога, "step_changed");

		Если НЕ ИИА_Оркестратор.ВозобновитьСЧекпоинта(СсылкаДиалога, 0, Ложь) Тогда
			Результат.Сообщение = "Resume from checkpoint должен отрабатывать без автозапуска";
			Возврат Результат;
		КонецЕсли;

		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если Состояние = Неопределено
			Или Состояние.ТекущаяСтадия <> "Plan"
			Или Состояние.ПоследнийПереход <> "checkpoint_resume" Тогда
			Результат.Сообщение = "Resume from checkpoint должен восстанавливать stage и переход";
			Возврат Результат;
		КонецЕсли;
		Если Состояние.active_step_id <> "step_restore" Или Состояние.execution_policy <> "safe-path" Тогда
			Результат.Сообщение = "Resume from checkpoint должен восстанавливать active_step_id и execution_policy";
			Возврат Результат;
		КонецЕсли;

		Если ТипЗнч(Состояние.decision_log) <> Тип("Массив") Или Состояние.decision_log.Количество() = 0 Тогда
			Результат.Сообщение = "Resume from checkpoint должен писать decision_log";
			Возврат Результат;
		КонецЕсли;
		ПоследнееРешение = Состояние.decision_log[Состояние.decision_log.Количество() - 1];
		Если НЕ ПоследнееРешение.Свойство("kind")
			Или ПоследнееРешение.kind <> "checkpoint_resume"
			Или НЕ ПоследнееРешение.Свойство("decision")
			Или ПоследнееРешение.decision <> "requested" Тогда
			Результат.Сообщение = "Resume from checkpoint должен фиксировать checkpoint_resume решение";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: resume from checkpoint восстанавливает runtime state";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактМетрикОркестратора() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "approval", "requested", "ReadQuerySubgraph", Новый Структура("source,step_id,reason", "approval", "step_1", "approval_required"));
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "approval", "approved", "ReadQuerySubgraph", Новый Структура("source,step_id,reason", "approval", "step_1", "approval_approved"));
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "recovery_policy", "resume_same_operation_id", "RecoverySubgraph", Новый Структура("source,policy_id,reason", "recovery", "write_retry_same_operation_policy", "resume_same_operation_id"));
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "recovery_policy", "abort_unsafe_write_retry", "RecoverySubgraph", Новый Структура("source,policy_id,reason", "recovery", "write_retry_missing_operation_id_policy", "abort_unsafe_write_retry"));
		ИИА_Сервер.ЗафиксироватьАктивныйПодграф(СсылкаДиалога, "PlanningSubgraph", "enter");
		ИИА_Сервер.ЗафиксироватьАктивныйПодграф(СсылкаДиалога, "ReadQuerySubgraph", "selected");
		ИИА_Сервер.ЗафиксироватьАктивныйПодграф(СсылкаДиалога, "ReadQuerySubgraph", "completed");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "typed_plan_saved", "Plan");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "execute_subgraph_entered", "Execute");

		Метрики = ИИА_Сервер.ПолучитьМетрикиОркестратора(СсылкаДиалога);
		Если Метрики = Неопределено Тогда
			Результат.Сообщение = "Метрики оркестратора должны возвращаться";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Метрики.Свойство("approval_rate")
			Или НЕ Метрики.Свойство("recovery_success_rate")
			Или НЕ Метрики.Свойство("top_failure_reasons")
			Или НЕ Метрики.Свойство("execution_path_frequency") Тогда
			Результат.Сообщение = "Метрики оркестратора должны содержать contract fields";
			Возврат Результат;
		КонецЕсли;
		Если Метрики.approval_requested <> 1 Или Метрики.approval_approved <> 1 Тогда
			Результат.Сообщение = "Approval metrics должны корректно считать requested/approved";
			Возврат Результат;
		КонецЕсли;
		Если Метрики.recovery_resume_count <> 1 Или Метрики.recovery_abort_count <> 1 Тогда
			Результат.Сообщение = "Recovery metrics должны корректно считать resume/abort";
			Возврат Результат;
		КонецЕсли;
		Если Метрики.checkpoint_count < 2 Или Метрики.decision_count < 4 Тогда
			Результат.Сообщение = "Метрики должны возвращать агрегаты по checkpoint/decision";
			Возврат Результат;
		КонецЕсли;
		Если ТипЗнч(Метрики.execution_path_frequency) <> Тип("Массив") Или Метрики.execution_path_frequency.Количество() = 0 Тогда
			Результат.Сообщение = "Метрики должны возвращать execution_path_frequency";
			Возврат Результат;
		КонецЕсли;
		Если ТипЗнч(Метрики.top_failure_reasons) <> Тип("Массив") Или Метрики.top_failure_reasons.Количество() = 0 Тогда
			Результат.Сообщение = "Метрики должны возвращать top_failure_reasons";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: метрики оркестратора агрегируются из runtime state";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактМетрикОркестратораV2() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "recovery_policy", "resume_same_operation_id", "RecoverySubgraph", Новый Структура("source,policy_id,reason", "recovery", "write_retry_same_operation_policy", "resume_same_operation_id"));
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "recovery_policy", "abort_unsafe_write_retry", "RecoverySubgraph", Новый Структура("source,policy_id,reason", "recovery", "write_retry_missing_operation_id_policy", "abort_unsafe_write_retry"));
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Plan", "plan_ready", Ложь);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Execute", "execute_done", Ложь);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Recover", "execute_failed", Истина);
		Метрики = ИИА_Сервер.ПолучитьМетрикиОркестратора(СсылкаДиалога);
		Если НЕ Метрики.Свойство("transition_frequency")
			Или НЕ Метрики.Свойство("retry_count_per_policy")
			Или НЕ Метрики.Свойство("stage_outcome_metrics") Тогда
			Результат.Сообщение = "V2-метрики должны содержать transition/policy/stage агрегаты";
			Возврат Результат;
		КонецЕсли;
		Если ТипЗнч(Метрики.transition_frequency) <> Тип("Массив")
			Или ТипЗнч(Метрики.retry_count_per_policy) <> Тип("Массив")
			Или ТипЗнч(Метрики.stage_outcome_metrics) <> Тип("Массив") Тогда
			Результат.Сообщение = "V2-метрики должны возвращать массивы";
			Возврат Результат;
		КонецЕсли;
		Если Метрики.transition_frequency.Количество() = 0
			Или Метрики.retry_count_per_policy.Количество() = 0
			Или Метрики.stage_outcome_metrics.Количество() = 0 Тогда
			Результат.Сообщение = "V2-метрики должны агрегировать реальные runtime данные";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: V2-метрики возвращают transition/policy/stage агрегаты";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактRuntimeJournalValidator() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Plan", "bad_transition_value", Ложь);
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если Состояние.ПоследнийПереход <> "" Тогда
			Результат.Сообщение = "Runtime journal validator должен отбрасывать invalid state transition";
			Возврат Результат;
		КонецЕсли;
		Trace = ИИА_Сервер.ПолучитьRuntimeTrace(СсылкаДиалога, 10);
		Если ТипЗнч(Trace) <> Тип("Массив") Или Trace.Количество() = 0 Тогда
			Результат.Сообщение = "Runtime trace должен сохранять event_history после state transition";
			Возврат Результат;
		КонецЕсли;
		ПоследнееСобытие = Trace[Trace.Количество() - 1];
		Если ПоследнееСобытие.trace_type <> "checkpoint" Тогда
			ПоследнееСобытие = Trace[0];
		КонецЕсли;
		Если ПоследнееСобытие.Свойство("transition") И ПоследнееСобытие.transition <> "" Тогда
			Результат.Сообщение = "Runtime trace не должен пропускать invalid transition в journal";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: validator очищает invalid runtime transition";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестFailureInvestigationAPI() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Recover", "execute_failed", Истина);
		ИИА_Сервер.ДобавитьРешениеВDecisionLog(СсылкаДиалога, "recovery_policy", "abort_unsafe_write_retry", "RecoverySubgraph", Новый Структура("source,policy_id,reason", "recovery", "write_retry_missing_operation_id_policy", "abort_unsafe_write_retry"));
		Цепочки = ИИА_Сервер.ПолучитьЦепочкиСбоевОркестратора(СсылкаДиалога, 5);
		ПричинаОстановки = ИИА_Сервер.ПочемуОстановилсяОркестратор(СсылкаДиалога);
		Если ТипЗнч(Цепочки) <> Тип("Массив") Или Цепочки.Количество() = 0 Тогда
			Результат.Сообщение = "Failure investigation API должен возвращать failure chains";
			Возврат Результат;
		КонецЕсли;
		Если ПричинаОстановки = Неопределено
			Или НЕ ПричинаОстановки.Свойство("stage")
			Или НЕ ПричинаОстановки.Свойство("last_failure") Тогда
			Результат.Сообщение = "ПочемуОстановилсяОркестратор должен возвращать structured reason";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: investigation API возвращает failure chains и причину остановки";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестValidateИСummarizeSubgraphContract() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалогаValidate = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.СохранитьРезультатExecutor(СсылкаДиалогаValidate, Новый Структура("Успех,Сообщение", Истина, "ok"));
		ValidateResult = ИИА_Оркестратор.ВыполнитьValidateSubgraph(СсылкаДиалогаValidate);
		Если ValidateResult = Неопределено
			Или НЕ ValidateResult.Свойство("NextStage")
			Или НЕ ValidateResult.Свойство("Decision")
			Или ПустаяСтрока(Строка(ValidateResult.NextStage))
			Или ПустаяСтрока(Строка(ValidateResult.Decision)) Тогда
			Результат.Сообщение = "ValidateSubgraph должен возвращать subgraph contract";
			Возврат Результат;
		КонецЕсли;
		СсылкаДиалогаSummary = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		SummarizeResult = ИИА_Оркестратор.ВыполнитьSummarizeSubgraph(СсылкаДиалогаSummary);
		Если SummarizeResult = Неопределено
			Или НЕ SummarizeResult.Свойство("Policy")
			Или SummarizeResult.Policy <> "summarize_policy" Тогда
			Результат.Сообщение = "SummarizeSubgraph должен возвращать subgraph contract";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: Validate/Summarize работают через единый subgraph contract";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактReplaySafetyPolicy() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалогаRead = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.СохранитьАртефактПланировщика(СсылкаДиалогаRead, "{""dsl_version"":2,""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ Наименование ИЗ Справочник.Контрагенты""}]}", "test_read", "step_read");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалогаRead, "execute_subgraph_entered", "Execute");
		ReplayRead = ИИА_ReplayPolicy.КлассифицироватьReplaySafety(СсылкаДиалогаRead, 0);
		Если ReplayRead.classification <> "safe_replay" Тогда
			Результат.Сообщение = "Read-only DSL должен классифицироваться как safe_replay";
			Возврат Результат;
		КонецЕсли;

		СсылкаДиалогаWrite = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.СохранитьАртефактПланировщика(СсылкаДиалогаWrite, "{""dsl_version"":2,""steps"":[{""action"":""Write"",""operation_id"":""op_resume""}]}", "test_write", "step_write");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалогаWrite, "execute_subgraph_entered", "Execute");
		ReplayWrite = ИИА_ReplayPolicy.КлассифицироватьReplaySafety(СсылкаДиалогаWrite, 0);
		Если ReplayWrite.classification <> "resume_only" Тогда
			Результат.Сообщение = "Write с operation_id должен классифицироваться как resume_only";
			Возврат Результат;
		КонецЕсли;

		СсылкаДиалогаUnsafe = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.СохранитьАртефактПланировщика(СсылкаДиалогаUnsafe, "{""dsl_version"":2,""steps"":[{""action"":""Write""}]}", "test_unsafe", "step_unsafe");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалогаUnsafe, "execute_subgraph_entered", "Execute");
		ReplayUnsafe = ИИА_ReplayPolicy.КлассифицироватьReplaySafety(СсылкаДиалогаUnsafe, 0);
		Если ReplayUnsafe.classification <> "no_replay" Тогда
			Результат.Сообщение = "Write без operation_id должен классифицироваться как no_replay";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: replay safety policy классифицирует safe/resume_only/no_replay";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестResumeFromCheckpointБлокируетNoReplay() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.СохранитьАртефактПланировщика(СсылкаДиалога, "{""dsl_version"":2,""steps"":[{""action"":""Write""}]}", "test_unsafe_resume", "step_unsafe");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "execute_subgraph_entered", "Execute", Новый Структура("step_id", "step_unsafe"));

		Если ИИА_Оркестратор.ВозобновитьСЧекпоинта(СсылкаДиалога, 0, Ложь) Тогда
			Результат.Сообщение = "Resume from checkpoint должен блокироваться для no_replay";
			Возврат Результат;
		КонецЕсли;

		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если ТипЗнч(Состояние.decision_log) <> Тип("Массив") Или Состояние.decision_log.Количество() = 0 Тогда
			Результат.Сообщение = "Блокировка no_replay должна писаться в decision_log";
			Возврат Результат;
		КонецЕсли;
		ПоследнееРешение = Состояние.decision_log[Состояние.decision_log.Количество() - 1];
		Если ПоследнееРешение.kind <> "checkpoint_resume" Или ПоследнееРешение.decision <> "requested" Тогда
			Результат.Сообщение = "Блокировка no_replay должна фиксировать checkpoint_resume decision";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ПоследнееРешение.Свойство("policy") Или ПоследнееРешение.policy <> "no_replay" Тогда
			Результат.Сообщение = "Блокировка no_replay должна фиксировать policy=no_replay";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: resume from checkpoint блокирует no_replay";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSideEffectLogХранитReplaySafety() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ДобавитьЗаписьВСайдЭффектЛог(СсылкаДиалога, Новый Структура("effect_type,action,operation_id", "object_written", "Write", "op_effect"));
		Лог = ИИА_Сервер.ПолучитьSideEffectLog(СсылкаДиалога);
		Если ТипЗнч(Лог) <> Тип("Массив") Или Лог.Количество() = 0 Тогда
			Результат.Сообщение = "Side effect log должен содержать запись";
			Возврат Результат;
		КонецЕсли;
		Запись = Лог[Лог.Количество() - 1];
		Если НЕ Запись.Свойство("replay_safety") Или Запись.replay_safety <> "resume_only" Тогда
			Результат.Сообщение = "Side effect log должен автоматически хранить replay_safety";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: side effect log хранит replay_safety";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестCheckpointReplaySafetyИмеетПриоритет() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.СохранитьАртефактПланировщика(СсылкаДиалога, "{""dsl_version"":2,""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ 1""}]}", "test_checkpoint_priority", "step_cp");
		ИИА_Сервер.СохранитьЧекпоинтОркестратора(СсылкаДиалога, "execute_subgraph_entered", "Execute", Новый Структура("step_id,replay_safety,operation_id", "step_cp", "resume_only", "op_cp"));

		Replay = ИИА_ReplayPolicy.КлассифицироватьReplaySafety(СсылкаДиалога, 0);
		Если Replay.classification <> "resume_only" Или Replay.reason <> "checkpoint_replay_safety" Тогда
			Результат.Сообщение = "Checkpoint replay_safety должен иметь приоритет над DSL fallback";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: checkpoint replay_safety имеет приоритет";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактRecoveryPolicy() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Реестр = ИИА_RecoveryPolicy.ПолучитьРеестрПолитикВосстановления();
		Если Реестр = Неопределено Или Реестр.Количество() = 0 Тогда
			Результат.Сообщение = "Реестр recovery-политик пуст";
			Возврат Результат;
		КонецЕсли;
		Если Реестр.Получить("table_not_found") = Неопределено Тогда
			Результат.Сообщение = "Должна быть policy для table_not_found";
			Возврат Результат;
		КонецЕсли;
		Если Реестр.Получить("field_not_found") = Неопределено Тогда
			Результат.Сообщение = "Должна быть policy для field_not_found";
			Возврат Результат;
		КонецЕсли;
		Если Реестр.Получить("write_failed") = Неопределено Тогда
			Результат.Сообщение = "Должна быть policy для write_failed";
			Возврат Результат;
		КонецЕсли;
		Если Реестр.Получить("storage_write_error") = Неопределено Тогда
			Результат.Сообщение = "Должна быть policy для storage_write_error";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: recovery-policy реестр валиден";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактExecuteSubgraphRouting() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		WriteDSL = "{""dsl_version"":2,""steps"":[{""action"":""CreateReference"",""object_name"":""Контрагенты"",""operation_id"":""op_1""},{""action"":""Write"",""operation_id"":""op_2""}]}";
		ReadDSL = "{""dsl_version"":2,""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ ПЕРВЫЕ 1 Наименование ИЗ Справочник.Контрагенты""}]}";
		GenericDSL = "{""dsl_version"":2,""steps"":[{""action"":""ShowInfo"",""message"":""ok""}]}";

		РоутингWrite = ИИА_Оркестратор.ОпределитьИсполнительныйПодграф(WriteDSL);
		РоутингRead = ИИА_Оркестратор.ОпределитьИсполнительныйПодграф(ReadDSL);
		РоутингGeneric = ИИА_Оркестратор.ОпределитьИсполнительныйПодграф(GenericDSL);

		Если РоутингWrite = Неопределено Или РоутингWrite.SubgraphName <> "WriteActionSubgraph" Тогда
			Результат.Сообщение = "Write DSL должен маршрутизироваться в WriteActionSubgraph";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РоутингWrite.Свойство("HasWriteActions") Или НЕ РоутингWrite.HasWriteActions Тогда
			Результат.Сообщение = "Write routing должен маркировать HasWriteActions";
			Возврат Результат;
		КонецЕсли;

		Если РоутингRead = Неопределено Или РоутингRead.SubgraphName <> "ReadQuerySubgraph" Тогда
			Результат.Сообщение = "RunQuery DSL должен маршрутизироваться в ReadQuerySubgraph";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РоутингRead.Свойство("HasRunQuery") Или НЕ РоутингRead.HasRunQuery Тогда
			Результат.Сообщение = "Read routing должен маркировать HasRunQuery";
			Возврат Результат;
		КонецЕсли;

		Если РоутингGeneric = Неопределено Или РоутингGeneric.SubgraphName <> "GenericExecuteSubgraph" Тогда
			Результат.Сообщение = "ShowInfo DSL должен оставаться в GenericExecuteSubgraph";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: Execute subgraph routing работает";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестDiscoverySubgraph() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		РезультатDiscovery = ИИА_Оркестратор.ВыполнитьDiscoverySubgraph(СсылкаДиалога, "Сколько контрагентов в базе?", Перечисления.ИИА_ТипДиалога.Запрос1С);
		Если РезультатDiscovery = Неопределено Тогда
			Результат.Сообщение = "DiscoverySubgraph не вернул результат";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РезультатDiscovery.Свойство("SubgraphName") Или РезультатDiscovery.SubgraphName <> "DiscoverySubgraph" Тогда
			Результат.Сообщение = "DiscoverySubgraph должен возвращать свой идентификатор";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РезультатDiscovery.Свойство("Details") Или НЕ РезультатDiscovery.Details.Свойство("ИспользоватьRAG") Тогда
			Результат.Сообщение = "DiscoverySubgraph должен возвращать decision details по RAG";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РезультатDiscovery.Успех Тогда
			Результат.Сообщение = "DiscoverySubgraph должен завершаться успешно для обычной задачи";
			Возврат Результат;
		КонецЕсли;
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если ТипЗнч(Состояние.decision_log) <> Тип("Массив") Или Состояние.decision_log.Количество() < 2 Тогда
			Результат.Сообщение = "DiscoverySubgraph должен писать semantic decisions в decision_log";
			Возврат Результат;
		КонецЕсли;
		НайденDecisionRAG = Ложь;
		НайденDecisionPolicy = Ложь;
		Для Каждого ЗаписьРешения Из Состояние.decision_log Цикл
			Если ЗаписьРешения.Свойство("kind") И ЗаписьРешения.kind = "discovery_rag" Тогда
				НайденDecisionRAG = Истина;
			КонецЕсли;
			Если ЗаписьРешения.Свойство("kind") И ЗаписьРешения.kind = "execution_policy" И ЗаписьРешения.Свойство("subgraph") И ЗаписьРешения.subgraph = "DiscoverySubgraph" Тогда
				НайденDecisionPolicy = Истина;
			КонецЕсли;
		КонецЦикла;
		Если НЕ НайденDecisionRAG Тогда
			Результат.Сообщение = "DiscoverySubgraph должен логировать discovery_rag решение";
			Возврат Результат;
		КонецЕсли;
		Если НЕ НайденDecisionPolicy Тогда
			Результат.Сообщение = "DiscoverySubgraph должен логировать execution_policy решение";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: DiscoverySubgraph возвращает discovery decision";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестPlanningSubgraph() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		ИИА_Сервер.ОтправитьСообщениеСервера(СсылкаДиалога, Перечисления.ИИА_ТипДиалога.Запрос1С, "Сколько контрагентов в базе?");
		МассивMock = Новый Массив;
		МассивMock.Добавить(Новый Структура("Текст", "{""plan_version"":1,""goal"":""Сколько контрагентов в базе?"",""steps"":[{""id"":""step_1"",""kind"":""run_query"",""title"":""Подсчитать контрагентов"",""depends_on"":[],""input"":{""query"":""Сколько контрагентов в базе?""},""status"":""pending""}]}"));
		МассивMock.Добавить(Новый Структура("Текст,DSL,PlanStepId", "{""dsl_version"":2,""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ ПЕРВЫЕ 1 Наименование ИЗ Справочник.Контрагенты""}]}", "{""dsl_version"":2,""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ ПЕРВЫЕ 1 Наименование ИЗ Справочник.Контрагенты""}]}", "step_1"));
		ИИА_Сервер.УстановитьОчередьMockОтветов(СсылкаДиалога, МассивMock);
		РезультатПланирования = ИИА_Оркестратор.ВыполнитьPlanningSubgraph(СсылкаДиалога);
		ИИА_Сервер.ОчиститьОчередьMockОтветов(СсылкаДиалога);
		Если РезультатПланирования = Неопределено
			ИЛИ НЕ РезультатПланирования.Свойство("Успех")
			ИЛИ НЕ РезультатПланирования.Успех Тогда
			Результат.Сообщение = "PlanningSubgraph должен успешно построить следующий шаг";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РезультатПланирования.Свойство("NextStage") Или РезультатПланирования.NextStage <> "Execute" Тогда
			Результат.Сообщение = "PlanningSubgraph должен возвращать transition contract с NextStage=Execute";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РезультатПланирования.Свойство("StepId") Или РезультатПланирования.StepId <> "step_1" Тогда
			Результат.Сообщение = "PlanningSubgraph должен возвращать StepId в transition contract";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РезультатПланирования.Свойство("Policy") Или ПустаяСтрока(РезультатПланирования.Policy) Тогда
			Результат.Сообщение = "PlanningSubgraph должен возвращать Policy в transition contract";
			Возврат Результат;
		КонецЕсли;
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если Состояние = Неопределено Или Состояние.active_subgraph <> "PlanningSubgraph" Тогда
			Результат.Сообщение = "PlanningSubgraph должен фиксироваться как active_subgraph";
			Возврат Результат;
		КонецЕсли;
		Если ПустаяСтрока(Состояние.execution_policy) Тогда
			Результат.Сообщение = "PlanningSubgraph должен фиксировать execution_policy";
			Возврат Результат;
		КонецЕсли;
		Если ПустаяСтрока(Состояние.thread_id) Тогда
			Результат.Сообщение = "PlanningSubgraph должен фиксировать thread_id";
			Возврат Результат;
		КонецЕсли;
		Если ПустаяСтрока(Состояние.active_step_id) Тогда
			Результат.Сообщение = "PlanningSubgraph должен фиксировать active_step_id";
			Возврат Результат;
		КонецЕсли;
		Если ТипЗнч(Состояние.subgraph_history) <> Тип("Массив") Или Состояние.subgraph_history.Количество() = 0 Тогда
			Результат.Сообщение = "PlanningSubgraph должен писать subgraph_history";
			Возврат Результат;
		КонецЕсли;
		Если ТипЗнч(Состояние.decision_log) <> Тип("Массив") Или Состояние.decision_log.Количество() = 0 Тогда
			Результат.Сообщение = "PlanningSubgraph должен писать decision_log";
			Возврат Результат;
		КонецЕсли;
		НайденPolicyDecision = Ложь;
		НайденNextStepDecision = Ложь;
		Для Каждого ЗаписьРешения Из Состояние.decision_log Цикл
			Если ЗаписьРешения.Свойство("kind") И ЗаписьРешения.kind = "execution_policy" И ЗаписьРешения.Свойство("subgraph") И ЗаписьРешения.subgraph = "PlanningSubgraph" Тогда
				НайденPolicyDecision = Истина;
			КонецЕсли;
			Если ЗаписьРешения.Свойство("kind") И ЗаписьРешения.kind = "next_step_selected" И ЗаписьРешения.Свойство("decision") И ЗаписьРешения.decision = "step_1" Тогда
				НайденNextStepDecision = Истина;
			КонецЕсли;
		КонецЦикла;
		Если НЕ НайденPolicyDecision Тогда
			Результат.Сообщение = "PlanningSubgraph должен логировать execution_policy решение";
			Возврат Результат;
		КонецЕсли;
		Если НЕ НайденNextStepDecision Тогда
			Результат.Сообщение = "PlanningSubgraph должен логировать next_step_selected";
			Возврат Результат;
		КонецЕсли;
		Артефакт = ИИА_Сервер.ПолучитьАртефактПланировщика(СсылкаДиалога);
		Если Артефакт = Неопределено Или ПустаяСтрока(Артефакт.PlannedDSL) Тогда
			Результат.Сообщение = "PlanningSubgraph должен сохранить planner artifact";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: PlanningSubgraph обновляет runtime state и planner artifact";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестПолныйПутьApprovalApproveValidate() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);

		План = Новый Структура;
		План.Вставить("plan_version", 1);
		План.Вставить("goal", "Получить контрагентов");
		План.Вставить("steps", Новый Массив);
		Шаг = Новый Структура;
		Шаг.Вставить("id", "step_1");
		Шаг.Вставить("seq_no", 1);
		Шаг.Вставить("kind", "run_query");
		Шаг.Вставить("title", "Выполнить запрос");
		Шаг.Вставить("depends_on", Новый Массив);
		Шаг.Вставить("input", Новый Структура("query", "Контрагенты"));
		Шаг.Вставить("status", "pending");
		Шаг.Вставить("result_summary", "");
		Шаг.Вставить("result_data", Неопределено);
		План.steps.Добавить(Шаг);
		ИИА_TypedPlan.СохранитьTypedPlan(СсылкаДиалога, План);

		DSL = "{""dsl_version"":2,""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ Наименование ИЗ Справочник.Контрагенты""}]}";
		ИИА_Сервер.СохранитьАртефактПланировщика(СсылкаДиалога, DSL, "test_approval_path", "step_1");
		ИИА_Сервер.УстановитьActiveStepId(СсылкаДиалога, "step_1");
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Execute", "test_approval_path", Ложь);

		Если НЕ ИИА_Оркестратор.ВыполнитьСтадиюExecute(СсылкаДиалога) Тогда
			Результат.Сообщение = "Execute должен успешно перейти в AwaitApproval";
			Возврат Результат;
		КонецЕсли;

		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если Состояние = Неопределено Или Состояние.ТекущаяСтадия <> "AwaitApproval" Тогда
			Результат.Сообщение = "После первого Execute оркестратор должен перейти в AwaitApproval";
			Возврат Результат;
		КонецЕсли;

		СостояниеПодтверждения = ИИА_Сервер.ПолучитьСостояниеПодтверждения(СсылкаДиалога);
		Если СостояниеПодтверждения = Неопределено
			Или НЕ СостояниеПодтверждения.Свойство("status")
			Или ВРег(Строка(СостояниеПодтверждения.status)) <> "PENDING" Тогда
			Результат.Сообщение = "Должно появиться pending-состояние подтверждения";
			Возврат Результат;
		КонецЕсли;
		Если НЕ СостояниеПодтверждения.Свойство("subgraph_name") Или СостояниеПодтверждения.subgraph_name <> "ReadQuerySubgraph" Тогда
			Результат.Сообщение = "Approval state должен сохранять subgraph_name";
			Возврат Результат;
		КонецЕсли;

		Если НЕ ИИА_ApprovalPolicy.ПодтвердитьОжидающееДействие(СсылкаДиалога, Ложь) Тогда
			Результат.Сообщение = "Подтверждение ожидающего действия должно отработать";
			Возврат Результат;
		КонецЕсли;

		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если Состояние = Неопределено Или Состояние.ТекущаяСтадия <> "Execute" Тогда
			Результат.Сообщение = "После approve оркестратор должен вернуться в Execute";
			Возврат Результат;
		КонецЕсли;

		Если НЕ ИИА_Оркестратор.ВыполнитьСтадиюExecute(СсылкаДиалога) Тогда
			Результат.Сообщение = "Execute после approve должен исполнить DSL";
			Возврат Результат;
		КонецЕсли;

		РезультатValidate = ИИА_Оркестратор.ВыполнитьСтадиюValidate(СсылкаДиалога);
		Если РезультатValidate <> Неопределено Тогда
			Результат.Детали.Добавить("Validate: NextStage=" + ?(РезультатValidate.Свойство("NextStage"), Строка(РезультатValidate.NextStage), "")
				+ ", Reason=" + ?(РезультатValidate.Свойство("Reason"), Строка(РезультатValidate.Reason), "")
				+ ", Успех=" + ?(РезультатValidate.Свойство("Успех"), Строка(РезультатValidate.Успех), ""));
		КонецЕсли;
		Если РезультатValidate = Неопределено
			Или НЕ РезультатValidate.Свойство("NextStage")
			Или РезультатValidate.NextStage <> "Plan" Тогда
			Результат.Сообщение = "Validate после approve должен перейти к следующему шагу показа результата";
			Возврат Результат;
		КонецЕсли;

		ПланПосле = ИИА_TypedPlan.ПолучитьTypedPlan(СсылкаДиалога);
		Если ПланПосле = Неопределено Или ТипЗнч(ПланПосле.steps) <> Тип("Массив") Или ПланПосле.steps.Количество() = 0 Тогда
			Результат.Сообщение = "Typed plan должен сохраняться после validate";
			Возврат Результат;
		КонецЕсли;
		ШагПосле = ПланПосле.steps[0];
		Если НЕ ШагПосле.Свойство("status") Или ВРег(Строка(ШагПосле.status)) <> "DONE" Тогда
			Результат.Сообщение = "Validate должен помечать шаг как done";
			Возврат Результат;
		КонецЕсли;
		Если ПланПосле.steps.Количество() < 2
			Или НРег(Строка(ПланПосле.steps[1].kind)) <> "show_info"
			Или ВРег(Строка(ПланПосле.steps[1].status)) <> "PENDING" Тогда
			Результат.Сообщение = "После run_query должен остаться pending show_info для вывода результата";
			Возврат Результат;
		КонецЕсли;

		НайденApprovalRequested = Ложь;
		НайденApprovalApproved = Ложь;
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Для Каждого ЗаписьРешения Из Состояние.decision_log Цикл
			Если ЗаписьРешения.Свойство("kind")
				И ЗаписьРешения.kind = "approval"
				И ЗаписьРешения.Свойство("decision")
				И ЗаписьРешения.decision = "requested"
				И ЗаписьРешения.Свойство("subgraph")
				И ЗаписьРешения.subgraph = "ReadQuerySubgraph" Тогда
				НайденApprovalRequested = Истина;
			КонецЕсли;
			Если ЗаписьРешения.Свойство("kind")
				И ЗаписьРешения.kind = "approval"
				И ЗаписьРешения.Свойство("decision")
				И ЗаписьРешения.decision = "approved"
				И ЗаписьРешения.Свойство("subgraph")
				И ЗаписьРешения.subgraph = "ReadQuerySubgraph" Тогда
				НайденApprovalApproved = Истина;
			КонецЕсли;
		КонецЦикла;

		Если НЕ НайденApprovalRequested Или НЕ НайденApprovalApproved Тогда
			Результат.Сообщение = "Approval path должен писать requested и approved для ReadQuerySubgraph";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: путь Execute -> AwaitApproval -> Approve -> Validate работает";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестОчисткиСущностейRAG() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ГрязныйТекст = "Ошибка API (500): HTTP 402 detail=requested up to 65536 tokens";
		ОчищенныйГрязный = ИИА_Промты.ОчиститьСущностиДляRAG(ГрязныйТекст);
		Результат.Детали.Добавить("Очищенный грязный текст: '" + ОчищенныйГрязный + "'");
		Если НЕ ПустаяСтрока(ОчищенныйГрязный) Тогда
			Результат.Сообщение = "Служебный текст не должен попадать в RAG-сущности";
			Возврат Результат;
		КонецЕсли;

		ПолезныйТекст = "Контрагент Мир Мебели счет 123 приходная накладная";
		ОчищенныйПолезный = ИИА_Промты.ОчиститьСущностиДляRAG(ПолезныйТекст);
		Результат.Детали.Добавить("Очищенный полезный текст: '" + ОчищенныйПолезный + "'");
		Если ПустаяСтрока(ОчищенныйПолезный) Тогда
			Результат.Сообщение = "Полезные сущности не должны очищаться в пустоту";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(ОчищенныйПолезный), "КОНТРАГЕНТ") = 0 И СтрНайти(ВРег(ОчищенныйПолезный), "МЕБЕЛ") = 0 Тогда
			Результат.Сообщение = "Очистка сущностей потеряла смысловые токены: '" + ОчищенныйПолезный + "'";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: очистка сущностей RAG";
	Исключение
		Результат.Сообщение = "Исключение: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКомпактногоФорматаRAG() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СтрокаRAG = ИИА_Промты.СформироватьСтрокуОбъектаRAG("Catalog", "Контрагенты", "Контрагенты", "Наименование, Код, ИНН, КПП, Договор");
		Результат.Детали.Добавить("Строка RAG: " + СтрокаRAG);
		Если СтрНайти(СтрокаRAG, "Catalog.Контрагенты") = 0 И СтрНайти(СтрокаRAG, "Справочник.Контрагенты") = 0 Тогда
			Результат.Сообщение = "Компактный формат должен содержать имя объекта";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(СтрокаRAG, "Наименование, Код, ИНН, КПП") = 0 Тогда
			Результат.Сообщение = "Компактный формат должен оставлять короткий shortlist полей";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(СтрокаRAG, "Договор") > 0 Тогда
			Результат.Сообщение = "Компактный формат должен ограничивать список полей";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(СтрокаRAG, "Field-shortlist") > 0 Тогда
			Результат.Сообщение = "Компактный формат не должен содержать старую шумную маркировку";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: компактный формат RAG";
	Исключение
		Результат.Сообщение = "Исключение: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестRAGОбъединяетСущностиСКлючевымиСловами() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Объединенный = ИИА_Промты.ОбъединитьКлючевыеСловаRAG("приходная накладная поставщик", "счет мир мебели 123", 10);
		Результат.Детали.Добавить("Объединенный текст: " + Объединенный);
		Если СтрНайти(ВРег(Объединенный), "СЧЕТ") = 0 Тогда
			Результат.Сообщение = "RAG должен сохранять токен 'счет' из исходного запроса";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(Объединенный), "ПРИХОДНАЯ") = 0 Тогда
			Результат.Сообщение = "RAG не должен терять ключевые сущности экстрактора";
			Возврат Результат;
		КонецЕсли;

		Фильтр = ИИА_Оркестратор.ОпределитьФильтрGetMetadataИзИмениТаблицы("Документ.Счет");
		Результат.Детали.Добавить("Фильтр для Документ.Счет: " + Фильтр);
		Если ВРег(Фильтр) <> "СЧЕТНАОПЛАТУ" Тогда
			Результат.Сообщение = "Для Документ.Счет фильтр GetMetadata должен нормализоваться в СчетНаОплату";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: RAG объединяет сущности с ключевыми словами";
	Исключение
		Результат.Сообщение = "Исключение: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестЯвныйRunQueryБезОбъектаНеНормализуется() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		ШагПлана = Новый Структура;
		ШагПлана.Вставить("kind", "run_query");
		ШагПлана.Вставить("input", Новый Структура("query", "ВЫБРАТЬ Контрагент ИЗ Справочник.Контрагенты ГДЕ Наименование = ""Мир Мебели"""));

		DSL_JSON = ИИА_DSLПостроитель.ПостроитьDSLИзШагаПлана(ШагПлана);
		Результат.Детали.Добавить("Сгенерированный DSL: " + DSL_JSON);
		Если СтрНайти(DSL_JSON, "Справочник.Ссылка") > 0 Тогда
			Результат.Сообщение = "Явный RunQuery без object/object_name не должен переписываться в Справочник.Ссылка";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(DSL_JSON, "Ссылка КАК Ссылка") > 0 Тогда
			Результат.Сообщение = "Явный RunQuery без object/object_name не должен автоматически менять поле на Ссылка";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: явный RunQuery без объекта не нормализуется";
	Исключение
		Результат.Сообщение = "Исключение: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестGetMetadataНеПереписываетЯвныйRunQuery() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		План = Новый Структура;
		План.Вставить("steps", Новый Массив);

		ШагМетаданных = Новый Структура;
		ШагМетаданных.Вставить("kind", "get_metadata");
		ШагМетаданных.Вставить("result_summary", "Получены метаданные");
		ШагМетаданных.Вставить("result_data", Новый Структура("Сообщение", "- Документы: Документ.ЗаказПокупателя — Заказ покупателя, Документ.СчетНаОплату — Счет на оплату"));
		План.steps.Добавить(ШагМетаданных);

		ШагЗапроса = Новый Структура;
		ШагЗапроса.Вставить("kind", "run_query");
		ШагЗапроса.Вставить("title", "Найти поставщика");
		ШагЗапроса.Вставить("input", Новый Структура("query", "ВЫБРАТЬ Контрагент ИЗ Справочник.Контрагенты ГДЕ Наименование = ""Мир Мебели"""));
		План.steps.Добавить(ШагЗапроса);

		ИИА_Оркестратор.ПрименитьЭффектGetMetadataШагаПлана(План, ШагМетаданных);
		ИзмененныйШаг = План.steps[1];
		InputШага = ИзмененныйШаг.input;
		ИтоговыйЗапрос = Строка(InputШага.query);
		Результат.Детали.Добавить("Запрос после GetMetadata: " + ИтоговыйЗапрос);
		Если СтрНайти(ИтоговыйЗапрос, "Документ.ЗаказПокупателя") > 0 Тогда
			Результат.Сообщение = "GetMetadata не должен автоматически переписывать текст явного RunQuery";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ИтоговыйЗапрос, "Справочник.Контрагенты") = 0 Тогда
			Результат.Сообщение = "Текст явного RunQuery должен оставаться исходным";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: GetMetadata не переписывает явный RunQuery";
	Исключение
		Результат.Сообщение = "Исключение: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестWriteПропускаетПовторПослеCommittedSideEffect() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		КонтекстВыполнения = Новый Структура;
		КонтекстВыполнения.Вставить("СсылкаДиалога", СсылкаДиалога);

		OperationId = "op_test_skip_repeat_after_commit";
		ИИА_Сервер.ДобавитьЗаписьВСайдЭффектЛог(СсылкаДиалога, Новый Структура("effect_type,action,operation_id", "object_written", "Write", OperationId));

		Шаг = Новый Структура("action,operation_id", "Write", OperationId);
		РезультатШага = ИИА_DSLActionWrite.ВыполнитьWrite(Шаг, КонтекстВыполнения);
		Результат.Детали.Добавить("Сообщение: " + РезультатШага.Сообщение);
		Если НЕ РезультатШага.Успех Тогда
			Результат.Сообщение = "Повторный Write после committed side effect должен считаться успешным";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РезультатШага.Свойство("deduplicated_retry") Или НЕ РезультатШага.deduplicated_retry Тогда
			Результат.Сообщение = "Повторный Write должен маркироваться как deduplicated_retry";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: повторный Write пропускается после committed side effect";
	Исключение
		Результат.Сообщение = "Исключение: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестWriteДокументаЗаполняетДатуПоУмолчанию() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		КонтекстВыполнения = Новый Структура;
		КонтекстВыполнения.Вставить("СсылкаДиалога", ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент));

		ШагСоздания = Новый Структура;
		ШагСоздания.Вставить("action", "CreateDocument");
		ШагСоздания.Вставить("object_name", "ПриходнаяНакладная");
		ШагСоздания.Вставить("operation_id", "op_test_document_default_date_create");
		РезультатСоздания = ИИА_DSLActionWrite.ВыполнитьCreateDocument(ШагСоздания, КонтекстВыполнения);
		Если НЕ РезультатСоздания.Успех Тогда
			Результат.Сообщение = "CreateDocument должен успешно создавать документ для теста даты";
			Результат.Детали.Добавить("CreateDocument: " + РезультатСоздания.Сообщение);
			Возврат Результат;
		КонецЕсли;
		Шаг = Новый Структура;
		Шаг.Вставить("action", "Write");
		Шаг.Вставить("operation_id", "op_test_document_default_date");
		РезультатШага = ИИА_DSLActionWrite.ВыполнитьWrite(Шаг, КонтекстВыполнения);
		Результат.Детали.Добавить("Сообщение: " + РезультатШага.Сообщение);
		Если НЕ РезультатШага.Успех Тогда
			Результат.Сообщение = "Write документа должен заполнять дату по умолчанию вместо ошибки пустой даты";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(КонтекстВыполнения.ТекущийОбъект.Дата) Тогда
			Результат.Сообщение = "После Write дата документа должна быть заполнена";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: Write документа заполняет дату по умолчанию";
	Исключение
		Результат.Сообщение = "Исключение: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестDSLПостроительSetFieldFieldName() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);

		План = Новый Структура;
		План.Вставить("plan_version", 1);
		План.Вставить("goal", "Создать контрагента Петров");
		План.Вставить("steps", Новый Массив);

		Шаг1 = Новый Структура;
		Шаг1.Вставить("id", "step_1");
		Шаг1.Вставить("seq_no", 1);
		Шаг1.Вставить("kind", "create_reference");
		Шаг1.Вставить("title", "Создать новый элемент справочника Контрагенты");
		Шаг1.Вставить("depends_on", Новый Массив);
		Шаг1.Вставить("input", Новый Структура("object_name", "Справочник.Контрагенты"));
		Шаг1.Вставить("status", "pending");
		План.steps.Добавить(Шаг1);

		Шаг2 = Новый Структура;
		Шаг2.Вставить("id", "step_2");
		Шаг2.Вставить("seq_no", 2);
		Шаг2.Вставить("kind", "set_field");
		Шаг2.Вставить("title", "Установить наименование контрагента");
		Шаг2.Вставить("depends_on", Новый Массив);
		Шаг2.depends_on.Добавить("step_1");
		Шаг2.Вставить("input", Новый Структура("field_name,value", "Наименование", "Петров"));
		Шаг2.Вставить("status", "pending");
		План.steps.Добавить(Шаг2);

		Шаг3 = Новый Структура;
		Шаг3.Вставить("id", "step_3");
		Шаг3.Вставить("seq_no", 3);
		Шаг3.Вставить("kind", "write_object");
		Шаг3.Вставить("title", "Сохранить контрагента");
		Шаг3.Вставить("depends_on", Новый Массив);
		Шаг3.depends_on.Добавить("step_2");
		Шаг3.Вставить("input", Новый Структура);
		Шаг3.Вставить("status", "pending");
		План.steps.Добавить(Шаг3);

		Шаг4 = Новый Структура;
		Шаг4.Вставить("id", "step_4");
		Шаг4.Вставить("seq_no", 4);
		Шаг4.Вставить("kind", "show_info");
		Шаг4.Вставить("title", "Показать информацию о созданном контрагенте");
		Шаг4.Вставить("depends_on", Новый Массив);
		Шаг4.depends_on.Добавить("step_3");
		Шаг4.Вставить("input", Новый Структура);
		Шаг4.Вставить("status", "pending");
		План.steps.Добавить(Шаг4);

		ИИА_TypedPlan.СохранитьTypedPlan(СсылкаДиалога, План);
		DSL = ИИА_DSLПостроитель.ПостроитьDSLИзЦепочкиШаговПлана(СсылкаДиалога, Шаг1);
		Если ПустаяСтрока(DSL) Тогда
			Результат.Сообщение = "DSL builder должен собрать write-цепочку из typed plan";
			Возврат Результат;
		КонецЕсли;
		ЧтениеJSON = Новый ЧтениеJSON;
		ЧтениеJSON.УстановитьСтроку(DSL);
		DSLСтруктура = ПрочитатьJSON(ЧтениеJSON);
		ЧтениеJSON.Закрыть();
		Если ТипЗнч(DSLСтруктура) <> Тип("Структура")
			Или НЕ DSLСтруктура.Свойство("steps")
			Или ТипЗнч(DSLСтруктура.steps) <> Тип("Массив")
			Или DSLСтруктура.steps.Количество() < 2 Тогда
			Результат.Сообщение = "DSL builder должен возвращать steps-массив минимум из двух шагов";
			Результат.Детали.Добавить(DSL);
			Возврат Результат;
		КонецЕсли;
		ШагSetField = DSLСтруктура.steps[1];
		Если ТипЗнч(ШагSetField) <> Тип("Структура")
			Или НЕ ШагSetField.Свойство("action")
			Или ВРег(Строка(ШагSetField.action)) <> "SETFIELD"
			Или НЕ ШагSetField.Свойство("field")
			Или Строка(ШагSetField.field) <> "Наименование" Тогда
			Результат.Сообщение = "SetField должен сериализовать field_name в поле field";
			Результат.Детали.Добавить(DSL);
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: DSL builder переносит field_name в field для write-chain";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактQuerySafetyGate() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		DSL = "{""dsl_version"":2,""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ ПОДОБИЕ(Наименование, 'x') ИЗ Справочник.Номенклатура""}]}";
		РезультатDSL = ИИА_DSL.ВыполнитьDSL(DSL);
		Если РезультатDSL.Успех Тогда
			Результат.Сообщение = "Query Safety Gate должен блокировать недопустимые конструкции";
			Возврат Результат;
		КонецЕсли;
		Если НЕ РезультатDSL.Свойство("СистемныйОтвет") Тогда
			Результат.Сообщение = "Отсутствует системный ответ DSL";
			Возврат Результат;
		КонецЕсли;
		СистемныйОтвет = РезультатDSL.СистемныйОтвет;
		Если СтрНайти(ВРег(СистемныйОтвет), "QUERY_SAFETY_VIOLATION") = 0 Тогда
			Результат.Сообщение = "Ожидается error_code=query_safety_violation";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: Query Safety Gate возвращает typed error";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактRAGПоля() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Поля = ИИА_RAG_Поиск.ПолучитьКандидатыПолейДляОбъекта("продажи сумма", "AccumReg", "Продажи", 3);
		Если Поля = Неопределено Тогда
			Результат.Сообщение = "Функция field-level RAG должна возвращать строку (даже пустую)";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: контракт field-level RAG доступен";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактAntiGiveup() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Реестр = ИИА_RecoveryPolicy.ПолучитьРеестрПолитикВосстановления();
		ПолитикаПараметров = Реестр.Получить("parameter_missing");
		ПолитикаSafety = Реестр.Получить("query_safety_violation");
		ПолитикаExecution = Реестр.Получить("query_execution_error");
		Если ПолитикаПараметров = Неопределено Тогда
			Результат.Сообщение = "Отсутствует anti-giveup policy для parameter_missing";
			Возврат Результат;
		КонецЕсли;
		Если ПолитикаSafety = Неопределено Тогда
			Результат.Сообщение = "Отсутствует anti-giveup policy для query_safety_violation";
			Возврат Результат;
		КонецЕсли;
		Если ПолитикаExecution = Неопределено Тогда
			Результат.Сообщение = "Отсутствует anti-giveup policy для query_execution_error";
			Возврат Результат;
		КонецЕсли;

		Промпт = ИИА_Промты.ПолучитьПромптPlannerСледующегоШага("{""plan_version"":1,""goal"":""Тест"",""steps"":[{""id"":""step_1"",""kind"":""run_query"",""title"":""Тестовый запрос"",""depends_on"":[],""input"":{""query"":""ВЫБРАТЬ ...""}}]}", "{""id"":""step_1"",""kind"":""run_query"",""title"":""Тестовый запрос"",""depends_on"":[],""input"":{""query"":""ВЫБРАТЬ ...""}}", "Ошибка: field_not_found", "Период, Сумма", "");
		Если СтрНайти(ВРег(Промпт), "ANTI-GIVEUP") = 0 Тогда
			Результат.Сообщение = "Промпт Planner должен содержать anti-giveup правило";
			Возврат Результат;
		КонецЕсли;
		ПромптРемонта = ИИА_Промты.ПолучитьПромптИсправленияDSL("query_execution_error", "{""dsl_version"":2,""steps"":[{""action"":""RunQuery"",""query"":""ВЫБРАТЬ ...""}]}", "");
		Если СтрНайти(ВРег(ПромптРемонта), "ПРЕДЫДУЩИЙ") = 0 Тогда
			Результат.Сообщение = "Промпт исправления должен запрещать ПРЕДЫДУЩИЙ и нестабильные конструкции";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: anti-giveup контракт настроен";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестAntiGiveupБлокРаннегоShowInfo() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);

		ОшибкаExecutor = Новый Структура("Успех,Сообщение,error_code", Ложь, "field_not_found", "field_not_found");
		ИИА_Сервер.СохранитьРезультатExecutor(СсылкаДиалога, ОшибкаExecutor);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Recover", "test_attempt_1", Истина);

		DSLСдача = "{""dsl_version"":2,""steps"":[{""action"":""ShowInfo"",""message"":""Нет: задача не выполнена""}]}";
		ИИА_Сервер.СохранитьАртефактПланировщика(СсылкаДиалога, DSLСдача, "test", "1");

		УспехExecute = ИИА_Оркестратор.ВыполнитьСтадиюExecute(СсылкаДиалога);
		Если УспехExecute Тогда
			Результат.Сообщение = "Execute должен блокировать раннюю сдачу ShowInfo";
			Возврат Результат;
		КонецЕсли;

		РезультатExecutor = ИИА_Сервер.ПолучитьРезультатExecutor(СсылкаДиалога);
		Если РезультатExecutor = Неопределено Или НЕ РезультатExecutor.Свойство("error_code") Тогда
			Результат.Сообщение = "После блокировки должен быть сохранен результат с error_code";
			Возврат Результат;
		КонецЕсли;
		Если ВРег(Строка(РезультатExecutor.error_code)) <> "PREMATURE_GIVEUP_BLOCKED" Тогда
			Результат.Сообщение = "Ожидается error_code=premature_giveup_blocked";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: ранний ShowInfo блокируется";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестAntiGiveupRecoveryExhausted() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Запрос1С);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);

		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Recover", "attempt_1", Истина);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Recover", "attempt_2", Истина);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Recover", "attempt_3", Истина);

		ОшибкаExecutor = Новый Структура("Успех,Сообщение,error_code", Ложь, "field_not_found", "field_not_found");
		ИИА_Сервер.СохранитьРезультатExecutor(СсылкаДиалога, ОшибкаExecutor);

		РезультатValidate = ИИА_Оркестратор.ВыполнитьСтадиюValidate(СсылкаДиалога);
		Если РезультатValidate = Неопределено Или НЕ РезультатValidate.Свойство("NextStage") Тогда
			Результат.Сообщение = "Validate должен вернуть следующую стадию";
			Возврат Результат;
		КонецЕсли;
		Если РезультатValidate.NextStage <> "Summarize" Тогда
			Результат.Сообщение = "При исчерпании recovery должен быть переход в Summarize";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: recovery_exhausted корректно ведет в summary";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестНормализацииStepFailedВRecoverable() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Executor = Новый Структура("Успех,Сообщение,error_code", Ложь, "Ошибка выполнения шага 'RunQuery': Поле не найдено ""Документ.РасходнаяНакладная.Сумма""", "step_failed");
		Код = ИИА_RecoveryPolicy.ИзвлечьКодОшибки(Executor);
		Если Код <> "field_not_found" Тогда
			Результат.Сообщение = "step_failed должен нормализоваться в field_not_found для recovery-policy";
			Результат.Детали.Добавить("Фактический код: " + Строка(Код));
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: step_failed нормализуется в recoverable-код";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестRecoveryResumeAfterCommittedWrite() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);

		OperationId = "op_test_committed_write";
		ОшибкаExecutor = Новый Структура("Успех,Сообщение,error_code,operation_id", Ложь, "Ошибка записи объекта: таймаут", "write_failed", OperationId);
		ИИА_Сервер.СохранитьРезультатExecutor(СсылкаДиалога, ОшибкаExecutor);
		ИИА_Сервер.ДобавитьЗаписьВСайдЭффектЛог(СсылкаДиалога, Новый Структура("effect_type,action,operation_id", "object_written", "Write", OperationId));

		Политика = ИИА_Оркестратор.ПрименитьПолитикуВосстановления(СсылкаДиалога, "write_failed", "Ошибка записи объекта: таймаут");
		Если Политика = Неопределено ИЛИ НЕ Политика.Обработано Тогда
			Результат.Сообщение = "Write recovery должен обработать committed side effect";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Политика.Свойство("СледующаяСтадия") ИЛИ Политика.СледующаяСтадия <> "Validate" Тогда
			Результат.Сообщение = "Committed write должен вести recovery в Validate";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Политика.Свойство("Решение") ИЛИ Политика.Решение <> "resume" Тогда
			Результат.Сообщение = "Committed write должен давать решение resume";
			Возврат Результат;
		КонецЕсли;

		СинтетическийРезультат = ИИА_Сервер.ПолучитьРезультатExecutor(СсылкаДиалога);
		Если СинтетическийРезультат = Неопределено ИЛИ НЕ СинтетическийРезультат.Свойство("Успех") ИЛИ НЕ СинтетическийРезультат.Успех Тогда
			Результат.Сообщение = "Recovery должен сохранить синтетически успешный executor result";
			Возврат Результат;
		КонецЕсли;
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если ТипЗнч(Состояние.decision_log) <> Тип("Массив") Или Состояние.decision_log.Количество() = 0 Тогда
			Результат.Сообщение = "Recovery должен писать decision_log для committed write";
			Возврат Результат;
		КонецЕсли;
		ПоследнееРешение = Состояние.decision_log[Состояние.decision_log.Количество() - 1];
		Если НЕ ПоследнееРешение.Свойство("kind") Или ПоследнееРешение.kind <> "recovery_policy" Тогда
			Результат.Сообщение = "Последняя запись decision_log должна быть recovery_policy";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ПоследнееРешение.Свойство("decision") Или ПоследнееРешение.decision <> "resume_after_committed_write" Тогда
			Результат.Сообщение = "decision_log должен фиксировать resume_after_committed_write";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: committed write переводит recovery в Validate";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестRecoveryResumeSameOperationId() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);

		OperationId = "op_test_resume_same_operation";
		DSL = "{""dsl_version"":2,""steps"":[{""action"":""Write"",""operation_id"":""" + OperationId + """}]}";
		ИИА_Сервер.СохранитьАртефактПланировщика(СсылкаДиалога, DSL, "test_recovery", "step_write");
		ОшибкаExecutor = Новый Структура("Успех,Сообщение,error_code,operation_id", Ложь, "Ошибка записи объекта: блокировка", "write_failed", OperationId);
		ИИА_Сервер.СохранитьРезультатExecutor(СсылкаДиалога, ОшибкаExecutor);

		Политика = ИИА_Оркестратор.ПрименитьПолитикуВосстановления(СсылкаДиалога, "write_failed", "Ошибка записи объекта: блокировка");
		Если Политика = Неопределено ИЛИ НЕ Политика.Обработано Тогда
			Результат.Сообщение = "Write recovery должен уметь возобновлять сценарий с тем же operation_id";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Политика.Свойство("СледующаяСтадия") ИЛИ Политика.СледующаяСтадия <> "Execute" Тогда
			Результат.Сообщение = "Safe retry с тем же operation_id должен вести в Execute";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Политика.Свойство("Решение") ИЛИ Политика.Решение <> "resume" Тогда
			Результат.Сообщение = "Safe retry должен давать решение resume";
			Возврат Результат;
		КонецЕсли;

		Артефакт = ИИА_Сервер.ПолучитьАртефактПланировщика(СсылкаДиалога);
		Если Артефакт = Неопределено ИЛИ НЕ Артефакт.Свойство("PlannedDSL") ИЛИ СтрНайти(Артефакт.PlannedDSL, OperationId) = 0 Тогда
			Результат.Сообщение = "После recovery должен оставаться planner artifact с тем же operation_id";
			Возврат Результат;
		КонецЕсли;
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если ТипЗнч(Состояние.decision_log) <> Тип("Массив") Или Состояние.decision_log.Количество() = 0 Тогда
			Результат.Сообщение = "Recovery должен писать decision_log для resume_same_operation_id";
			Возврат Результат;
		КонецЕсли;
		ПоследнееРешение = Состояние.decision_log[Состояние.decision_log.Количество() - 1];
		Если НЕ ПоследнееРешение.Свойство("decision") Или ПоследнееРешение.decision <> "resume_same_operation_id" Тогда
			Результат.Сообщение = "decision_log должен фиксировать resume_same_operation_id";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: write recovery возобновляет Execute с тем же operation_id";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестRecoveryAbortUnsafeWriteRetry() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);

		ОшибкаExecutor = Новый Структура("Успех,Сообщение,error_code", Ложь, "Ошибка записи объекта: неизвестный сбой", "write_failed");
		ИИА_Сервер.СохранитьРезультатExecutor(СсылкаДиалога, ОшибкаExecutor);

		Политика = ИИА_Оркестратор.ПрименитьПолитикуВосстановления(СсылкаДиалога, "write_failed", "Ошибка записи объекта: неизвестный сбой");
		Если Политика = Неопределено ИЛИ НЕ Политика.Обработано Тогда
			Результат.Сообщение = "Write recovery должен явно обрабатывать unsafe retry без operation_id";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Политика.Свойство("СледующаяСтадия") ИЛИ Политика.СледующаяСтадия <> "Summarize" Тогда
			Результат.Сообщение = "Unsafe write retry без operation_id должен вести в Summarize";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Политика.Свойство("Решение") ИЛИ Политика.Решение <> "abort" Тогда
			Результат.Сообщение = "Unsafe write retry без operation_id должен давать решение abort";
			Возврат Результат;
		КонецЕсли;
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если ТипЗнч(Состояние.decision_log) <> Тип("Массив") Или Состояние.decision_log.Количество() = 0 Тогда
			Результат.Сообщение = "Recovery должен писать decision_log для abort_unsafe_write_retry";
			Возврат Результат;
		КонецЕсли;
		ПоследнееРешение = Состояние.decision_log[Состояние.decision_log.Количество() - 1];
		Если НЕ ПоследнееРешение.Свойство("decision") Или ПоследнееРешение.decision <> "abort_unsafe_write_retry" Тогда
			Результат.Сообщение = "decision_log должен фиксировать abort_unsafe_write_retry";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: unsafe write retry без operation_id прерывается";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестRecoverySubgraphПереходВValidate() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Recover", "test_recover", Ложь);

		OperationId = "op_test_subgraph_validate";
		ОшибкаExecutor = Новый Структура("Успех,Сообщение,error_code,operation_id", Ложь, "Ошибка записи объекта: таймаут", "write_failed", OperationId);
		ИИА_Сервер.СохранитьРезультатExecutor(СсылкаДиалога, ОшибкаExecutor);
		ИИА_Сервер.ДобавитьЗаписьВСайдЭффектЛог(СсылкаДиалога, Новый Структура("effect_type,action,operation_id", "object_written", "Write", OperationId));

		РезультатRecover = ИИА_Оркестратор.ВыполнитьRecoverySubgraph(СсылкаДиалога);
		Если РезультатRecover = Неопределено Или НЕ РезультатRecover.Свойство("SubgraphName") Или РезультатRecover.SubgraphName <> "RecoverySubgraph" Тогда
			Результат.Сообщение = "Recovery subgraph должен возвращать маркер SubgraphName";
			Возврат Результат;
		КонецЕсли;

		ИИА_Оркестратор.ПрименитьПереходRecoverSubgraph(СсылкаДиалога, РезультатRecover);
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если Состояние = Неопределено Или Состояние.ТекущаяСтадия <> "Validate" Тогда
			Результат.Сообщение = "Recovery subgraph должен перевести state machine в Validate";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: RecoverySubgraph переводит оркестратор в Validate";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестRecoverySubgraphПереходВExecute() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Recover", "test_recover", Ложь);

		OperationId = "op_test_subgraph_execute";
		DSL = "{""dsl_version"":2,""steps"":[{""action"":""Write"",""operation_id"":""" + OperationId + """}]}";
		ИИА_Сервер.СохранитьАртефактПланировщика(СсылкаДиалога, DSL, "test_recovery_subgraph", "step_write");
		ОшибкаExecutor = Новый Структура("Успех,Сообщение,error_code,operation_id", Ложь, "Ошибка записи объекта: блокировка", "write_failed", OperationId);
		ИИА_Сервер.СохранитьРезультатExecutor(СсылкаДиалога, ОшибкаExecutor);

		РезультатRecover = ИИА_Оркестратор.ВыполнитьRecoverySubgraph(СсылкаДиалога);
		ИИА_Оркестратор.ПрименитьПереходRecoverSubgraph(СсылкаДиалога, РезультатRecover);
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если Состояние = Неопределено Или Состояние.ТекущаяСтадия <> "Execute" Тогда
			Результат.Сообщение = "Recovery subgraph должен перевести state machine в Execute для safe resume";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: RecoverySubgraph переводит оркестратор в Execute";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестRecoverySubgraphПереходВSummarize() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ИИА_Сервер.ИнициализироватьКонтекстАрхитектуры(СсылкаДиалога);
		ИИА_Сервер.УстановитьСостояниеОркестратора(СсылкаДиалога, "Recover", "test_recover", Ложь);

		ОшибкаExecutor = Новый Структура("Успех,Сообщение,error_code", Ложь, "Ошибка записи объекта: неизвестный сбой", "write_failed");
		ИИА_Сервер.СохранитьРезультатExecutor(СсылкаДиалога, ОшибкаExecutor);

		РезультатRecover = ИИА_Оркестратор.ВыполнитьRecoverySubgraph(СсылкаДиалога);
		ИИА_Оркестратор.ПрименитьПереходRecoverSubgraph(СсылкаДиалога, РезультатRecover);
		Состояние = ИИА_Сервер.ПолучитьСостояниеОркестратора(СсылкаДиалога);
		Если Состояние = Неопределено Или Состояние.ТекущаяСтадия <> "Summarize" Тогда
			Результат.Сообщение = "Recovery subgraph должен перевести state machine в Summarize для unsafe abort";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: RecoverySubgraph переводит оркестратор в Summarize";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестQuerySafetyGateВложенныеПоля() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		DSLВалидный =
			"{""dsl_version"":2,""steps"":[" +
			"{""action"":""GetObjectFields"",""object_type"":""Документ"",""object_name"":""РасходнаяНакладная""}," +
			"{""action"":""RunQuery"",""query"":""ВЫБРАТЬ ПЕРВЫЕ 1 РасходнаяНакладная.Контрагент.Наименование КАК Контрагент ИЗ Документ.РасходнаяНакладная КАК РасходнаяНакладная""}" +
			"]}";
		РезультатВалидный = ИИА_DSL.ВыполнитьDSL(DSLВалидный);
		Если НЕ РезультатВалидный.Свойство("СистемныйОтвет") Тогда
			Результат.Сообщение = "Отсутствует системный ответ для проверки вложенного валидного пути";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(РезультатВалидный.СистемныйОтвет), "QUERY_SAFETY_VIOLATION") > 0 Тогда
			Результат.Сообщение = "Валидный nested-path Контрагент.Наименование не должен блокироваться safety gate";
			Результат.Детали.Добавить(Лев(РезультатВалидный.СистемныйОтвет, 800));
			Возврат Результат;
		КонецЕсли;

		DSLНевалидный =
			"{""dsl_version"":2,""steps"":[" +
			"{""action"":""GetObjectFields"",""object_type"":""Документ"",""object_name"":""РасходнаяНакладная""}," +
			"{""action"":""RunQuery"",""query"":""ВЫБРАТЬ ПЕРВЫЕ 1 РасходнаяНакладная.Контрагент.НесуществующееПоле КАК Х ИЗ Документ.РасходнаяНакладная КАК РасходнаяНакладная""}" +
			"]}";
		РезультатНевалидный = ИИА_DSL.ВыполнитьDSL(DSLНевалидный);
		Если НЕ РезультатНевалидный.Свойство("СистемныйОтвет") Тогда
			Результат.Сообщение = "Отсутствует системный ответ для проверки вложенного невалидного пути";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ВРег(РезультатНевалидный.СистемныйОтвет), "QUERY_SAFETY_VIOLATION") = 0 Тогда
			Результат.Сообщение = "Невалидный nested-path должен блокироваться Query Safety Gate";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: nested-path в Query Safety Gate валидируется по whitelist_paths";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция СоздатьВременныйMXLДляТеста()
	Путь = ПолучитьИмяВременногоФайла("mxl");
	Данные = Новый Массив;
	Данные.Добавить(Новый Структура("Колонка1,Колонка2", "Значение1", "Значение2"));
	ТабДок = ИИА_Сервер.СоздатьТабличныйДокументИзДанных(Данные);
	ТабДок.Записать(Путь);
	Возврат Путь;
КонецФункции

Функция СоздатьВременныйXLSXДляТеста()
	Путь = ПолучитьИмяВременногоФайла("xlsx");
	Данные = Новый Массив;
	Данные.Добавить(Новый Структура("Код,Наименование,Количество", "001", "Тестовый товар", 5));
	Данные.Добавить(Новый Структура("Код,Наименование,Количество", "002", "Второй товар", 7));
	ТабДок = ИИА_Сервер.СоздатьТабличныйДокументИзДанных(Данные);
	Попытка
		ТабДок.Записать(Путь, ТипФайлаТабличногоДокумента.XLSX);
	Исключение
		ТабДок.Записать(Путь);
	КонецПопытки;
	Возврат Путь;
КонецФункции

Функция СоздатьВременныйТекстовыйФайлДляТеста()
	Путь = ПолучитьИмяВременногоФайла("txt");
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.УстановитьТекст("Первая строка" + Символы.ПС + "Вторая строка");
	ТекстовыйДокумент.Записать(Путь);
	Возврат Путь;
КонецФункции

Функция СоздатьВременныйUTF8ТекстовыйФайлДляТеста()
	Путь = ПолучитьИмяВременногоФайла("txt");
	Запись = Новый ЗаписьТекста(Путь, КодировкаТекста.UTF8, Символы.ПС, Ложь);
	Запись.ЗаписатьСтроку("Привет");
	Запись.ЗаписатьСтроку("мир");
	Запись.Закрыть();
	Возврат Путь;
КонецФункции

Функция СоздатьВременныйПустойТекстовыйФайлДляТеста()
	Путь = ПолучитьИмяВременногоФайла("txt");
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.УстановитьТекст("");
	ТекстовыйДокумент.Записать(Путь);
	Возврат Путь;
КонецФункции

Функция СоздатьВременныйASCIIТекстовыйФайлДляТеста()
	Путь = ПолучитьИмяВременногоФайла("txt");
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.УстановитьТекст("alpha line" + Символы.ПС + "beta line");
	ТекстовыйДокумент.Записать(Путь);
	Возврат Путь;
КонецФункции

Функция СоздатьВременныйДлинныйТекстовыйФайлДляТеста()
	Путь = ПолучитьИмяВременногоФайла("txt");
	Текст = "";
	Для Индекс = 1 По 320 Цикл
		Текст = Текст + "Строка " + Формат(Индекс, "ЧГ=0") + ": контрольная последовательность для длинного вложения." + Символы.ПС;
	КонецЦикла;
	Текст = Текст + "ФИНАЛЬНЫЙ_МАРКЕР_ДЛЯ_ПРОВЕРКИ_ПОЛНОГО_ТЕКСТА";
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.УстановитьТекст(Текст);
	ТекстовыйДокумент.Записать(Путь);
	Возврат Путь;
КонецФункции

Функция СоздатьВременныйФайлИзображенияДляТеста()
	Путь = ПолучитьИмяВременногоФайла("png");
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.УстановитьТекст("fake image payload");
	ТекстовыйДокумент.Записать(Путь);
	Возврат Путь;
КонецФункции

Функция СоздатьВременныйPDFДляТеста()
	Путь = ПолучитьИмяВременногоФайла("pdf");
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.УстановитьТекст("%PDF-1.4" + Символы.ПС + "fake pdf payload");
	ТекстовыйДокумент.Записать(Путь);
	Возврат Путь;
КонецФункции

Функция СоздатьВременныйDOCXДляТеста()
	Путь = ПолучитьИмяВременногоФайла("docx");
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.УстановитьТекст("docx line 1" + Символы.ПС + "docx line 2");
	ТекстовыйДокумент.Записать(Путь);
	Возврат Путь;
КонецФункции

Функция ТестВложенияТекстовыйФайл() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьФайла = СоздатьВременныйТекстовыйФайлДляТеста();
		Вложение = ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьФайла, "notes.txt");
		Если НЕ Вложение.Свойство("kind") ИЛИ Вложение.kind <> "text" Тогда
			Результат.Сообщение = "TXT должен классифицироваться как text";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Вложение.Свойство("extracted_text") ИЛИ СтрНайти(Строка(Вложение.extracted_text), "Первая строка") = 0 Тогда
			Результат.Сообщение = "Из текстового файла должен извлекаться текст";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: текстовый файл сохраняется и извлекается";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестВложенияUTF8ТекстовыйФайл() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьФайла = СоздатьВременныйUTF8ТекстовыйФайлДляТеста();
		Вложение = ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьФайла, "utf8.txt");
		Если НЕ Вложение.Свойство("extracted_text") ИЛИ СтрНайти(Строка(Вложение.extracted_text), "Привет") = 0 Тогда
			Результат.Сообщение = "UTF-8 текстовый файл должен читаться без искажения кодировки";
			Возврат Результат;
		КонецЕсли;
		Если Вложение.Свойство("summary") И СтрНайти(Строка(Вложение.summary), "Извлечен текст из файла.") = 0 Тогда
			Результат.Сообщение = "Для UTF-8 файла должна формироваться успешная summary";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: UTF-8 текст читается корректно";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестВложенияПустойТекстовыйФайл() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьФайла = СоздатьВременныйПустойТекстовыйФайлДляТеста();
		Вложение = ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьФайла, "empty.txt");
		Если НЕ Вложение.Свойство("summary") ИЛИ СтрНайти(Строка(Вложение.summary), "Файл пуст.") = 0 Тогда
			Результат.Сообщение = "Для пустого txt должна формироваться summary 'Файл пуст.'";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: пустой txt различается от ошибки чтения";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестВложенияИзображение() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьФайла = СоздатьВременныйФайлИзображенияДляТеста();
		Вложение = ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьФайла, "image.png");
		Если НЕ Вложение.Свойство("kind") ИЛИ Вложение.kind <> "image" Тогда
			Результат.Сообщение = "PNG должен классифицироваться как image";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Вложение.Свойство("summary") ИЛИ СтрНайти(ВРег(Строка(Вложение.summary)), "ИЗОБРАЖЕНИЕ") = 0 Тогда
			Результат.Сообщение = "Для изображения должна формироваться summary";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: изображение сохраняется как image-вложение";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестВложенияMXL() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ПутьMXL = "";
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьMXL = СоздатьВременныйMXLДляТеста();
		Вложение = ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьMXL);
		Если Вложение = Неопределено Тогда
			Результат.Сообщение = "Функция должна вернуть структуру вложения для MXL";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Вложение.Свойство("kind") ИЛИ Вложение.kind <> "table" Тогда
			Результат.Сообщение = "MXL должен классифицироваться как table";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Вложение.Свойство("summary") ИЛИ ПустаяСтрока(Строка(Вложение.summary)) Тогда
			Результат.Сообщение = "Для MXL должна строиться summary";
			Возврат Результат;
		КонецЕсли;
		Если НайтиНедопустимыйСимволXDTO(Строка(Вложение.summary)) > 0 Тогда
			Результат.Сообщение = "Summary MXL не должна содержать недопустимые для XDTO символы";
			Возврат Результат;
		КонецЕсли;
		Если Вложение.Свойство("extracted_text") И НайтиНедопустимыйСимволXDTO(Строка(Вложение.extracted_text)) > 0 Тогда
			Результат.Сообщение = "Извлеченный текст MXL не должен содержать недопустимые для XDTO символы";
			Возврат Результат;
		КонецЕсли;
		Вложения = ИИА_Вложения.ПолучитьВложенияДиалога(СсылкаДиалога);
		Если Вложения.Количество() <> 1 Тогда
			Результат.Сообщение = "Вложение MXL должно сохраняться в диалог";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: MXL сохраняется как table-вложение";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестВложенияXLSX() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ПутьXLSX = "";
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьXLSX = СоздатьВременныйXLSXДляТеста();
		Вложение = ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьXLSX, "sample.xlsx");
		Если Вложение = Неопределено Тогда
			Результат.Сообщение = "Функция должна вернуть структуру вложения для XLSX";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Вложение.Свойство("kind") ИЛИ Вложение.kind <> "table" Тогда
			Результат.Сообщение = "XLSX должен классифицироваться как table";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Вложение.Свойство("summary") ИЛИ ПустаяСтрока(Строка(Вложение.summary)) Тогда
			Результат.Сообщение = "Для XLSX должна строиться summary";
			Возврат Результат;
		КонецЕсли;
		Если НайтиНедопустимыйСимволXDTO(Строка(Вложение.summary)) > 0 Тогда
			Результат.Сообщение = "Summary XLSX не должна содержать недопустимые для XDTO символы";
			Возврат Результат;
		КонецЕсли;
		Если Вложение.Свойство("extracted_text") И НайтиНедопустимыйСимволXDTO(Строка(Вложение.extracted_text)) > 0 Тогда
			Результат.Сообщение = "Извлеченный текст XLSX не должен содержать недопустимые для XDTO символы";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Строка(Вложение.summary), "ТАБЛИЦА (preview):") = 0 И СтрНайти(Строка(Вложение.summary), "Excel-файл прочитан через ТабличныйДокумент") = 0 Тогда
			Результат.Сообщение = "Для XLSX должна формироваться summary из ТабличногоДокумента";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: XLSX сохраняется как table-вложение";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестВложенияХранятПолныйТекст() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьФайла = СоздатьВременныйДлинныйТекстовыйФайлДляТеста();
		Вложение = ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьФайла, "long-notes.txt");
		Если НЕ Вложение.Свойство("extracted_text") Тогда
			Результат.Сообщение = "У вложения должен сохраняться extracted_text";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Строка(Вложение.extracted_text), "ФИНАЛЬНЫЙ_МАРКЕР_ДЛЯ_ПРОВЕРКИ_ПОЛНОГО_ТЕКСТА") = 0 Тогда
			Результат.Сообщение = "В extracted_text должен сохраняться весь распознанный текст без усечения";
			Возврат Результат;
		КонецЕсли;
		ПолныйТекст = ИИА_Вложения.ПолучитьПолныйТекстВложения(СсылкаДиалога, "long-notes.txt");
		Если СтрНайти(ПолныйТекст, "ФИНАЛЬНЫЙ_МАРКЕР_ДЛЯ_ПРОВЕРКИ_ПОЛНОГО_ТЕКСТА") = 0 Тогда
			Результат.Сообщение = "API полного текста должен возвращать весь сохраненный текст вложения";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ПолныйТекст, "...(truncated)") > 0 Тогда
			Результат.Сообщение = "Полный текст вложения не должен содержать признак усечения";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: extracted_text хранится полностью";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестПромптОграничиваетТекстВложений() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьФайла = СоздатьВременныйДлинныйТекстовыйФайлДляТеста();
		ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьФайла, "long-prompt.txt");
		Сводка = ИИА_Вложения.ПолучитьСводкуВложенийДляПромпта(СсылкаДиалога, 3);
		Если СтрНайти(Сводка, "КОНТЕКСТ ВЛОЖЕНИЙ:") = 0 Тогда
			Результат.Сообщение = "Сводка вложений должна формироваться для prompt";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Сводка, "ФИНАЛЬНЫЙ_МАРКЕР_ДЛЯ_ПРОВЕРКИ_ПОЛНОГО_ТЕКСТА") > 0 Тогда
			Результат.Сообщение = "Prompt должен содержать только ограниченный фрагмент вложения, а не весь текст";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Сводка, "...(truncated)") = 0 Тогда
			Результат.Сообщение = "Ограниченный контекст вложения в prompt должен явно помечаться как усеченный";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: prompt ограничивает текст вложений";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестПоискВоВложениях() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьФайла = СоздатьВременныйТекстовыйФайлДляТеста();
		ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьФайла, "search-notes.txt");
		Совпадения = ИИА_Вложения.НайтиВоВложениях(СсылкаДиалога, "Вторая строка", 10);
		Если Совпадения = Неопределено ИЛИ Совпадения.Количество() = 0 Тогда
			Результат.Сообщение = "Поиск по распознанным вложениям должен находить совпадения";
			Возврат Результат;
		КонецЕсли;
		ПервоеСовпадение = Совпадения[0];
		Если НЕ ПервоеСовпадение.Свойство("file_name") ИЛИ Строка(ПервоеСовпадение.file_name) <> "search-notes.txt" Тогда
			Результат.Сообщение = "Поиск должен возвращать имя файла совпавшего вложения";
			Возврат Результат;
		КонецЕсли;
		Если НЕ ПервоеСовпадение.Свойство("line_text") ИЛИ СтрНайти(Строка(ПервоеСовпадение.line_text), "Вторая строка") = 0 Тогда
			Результат.Сообщение = "Поиск должен возвращать строку с совпадением";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: поиск по распознанным вложениям работает";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестКонтрактИнструментовВложений() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьФайла = СоздатьВременныйТекстовыйФайлДляТеста();
		ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьФайла, "tool-notes.txt");
		Инструменты = ИИА_Провайдеры.ПолучитьИнструментыВложенийДляДиалога(СсылкаДиалога);
		Если Инструменты = Неопределено ИЛИ Инструменты.Количество() < 2 Тогда
			Результат.Сообщение = "Для диалога с вложениями должны формироваться инструменты чтения и поиска";
			Возврат Результат;
		КонецЕсли;
		НашлиЧтение = Ложь;
		НашлиПоиск = Ложь;
		Для Каждого Инструмент Из Инструменты Цикл
			Если Инструмент.Свойство("function") И Инструмент.function.Свойство("name") Тогда
				Если Инструмент.function.name = "get_attachment_text" Тогда
					НашлиЧтение = Истина;
				ИначеЕсли Инструмент.function.name = "search_attachments" Тогда
					НашлиПоиск = Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		Если НЕ НашлиЧтение ИЛИ НЕ НашлиПоиск Тогда
			Результат.Сообщение = "Контракт инструментов вложений должен содержать get_attachment_text и search_attachments";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: контракт инструментов вложений сформирован";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестИсполнениеИнструментаВложения() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьФайла = СоздатьВременныйASCIIТекстовыйФайлДляТеста();
		ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьФайла, "tool-run.txt");
		ОтветЧтения = ИИА_Провайдеры.ВыполнитьИнструментВложения("get_attachment_text", "{""attachment_name"":""tool-run.txt"",""max_chars"":1000}", СсылкаДиалога);
		Если СтрНайти(ОтветЧтения, """tool"": ""get_attachment_text""") = 0 Тогда
			Результат.Сообщение = "Инструмент чтения вложения должен успешно выполняться";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(НРег(ОтветЧтения), """ok"": true") = 0 Тогда
			Результат.Сообщение = "Инструмент чтения должен возвращать успешный статус";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ОтветЧтения, "alpha line") = 0 Тогда
			Результат.Сообщение = "Инструмент чтения должен возвращать содержимое вложения";
			Возврат Результат;
		КонецЕсли;
		ОтветПоиска = ИИА_Провайдеры.ВыполнитьИнструментВложения("search_attachments", "{""query"":""beta"",""attachment_name"":""tool-run.txt"",""limit"":5}", СсылкаДиалога);
		Если СтрНайти(НРег(ОтветПоиска), """match_count"": 1") = 0 Тогда
			Результат.Сообщение = "Инструмент поиска должен возвращать количество совпадений";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(ОтветПоиска, "beta line") = 0 Тогда
			Результат.Сообщение = "Инструмент поиска по вложению должен возвращать совпадение";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: инструменты вложений выполняются";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSubmitDSLБезОберткиDSL() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Сообщение = Новый Структура;
		Сообщение.Вставить("role", "assistant");
		ToolCalls = Новый Массив;
		ФункцияTool = Новый Структура;
		ФункцияTool.Вставить("name", "submit_dsl");
		ФункцияTool.Вставить("arguments", "{""dsl_version"":2,""steps"":[{""action"":""ShowInfo"",""message"":""ok""}]}");
		ToolCall = Новый Структура;
		ToolCall.Вставить("id", "tool-test-1");
		ToolCall.Вставить("type", "function");
		ToolCall.Вставить("function", ФункцияTool);
		ToolCalls.Добавить(ToolCall);
		Сообщение.Вставить("tool_calls", ToolCalls);

		ПараметрыИИ = Новый Структура;
		Распознанный = ИИА_Провайдеры.ПостроитьРезультатИзToolCall(Сообщение, "Чат", ПараметрыИИ);
		Если Распознанный.ТипОтвета <> "DSL" Тогда
			Результат.Сообщение = "submit_dsl без обертки dsl должен распознаваться как DSL";
			Возврат Результат;
		КонецЕсли;
		Если ПустаяСтрока(Распознанный.DSL) ИЛИ СтрНайти(Распознанный.DSL, """ShowInfo""") = 0 Тогда
			Результат.Сообщение = "DSL из tool arguments должен сохраняться в результате";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: submit_dsl без обертки dsl поддержан";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSubmitDSLВосстанавливаетсяИзПлана() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		План = Новый Структура;
		План.Вставить("plan_version", 1);
		План.Вставить("goal", "Тест");
		Шаги = Новый Массив;
		Шаг = Новый Структура;
		Шаг.Вставить("id", "step_1");
		Шаг.Вставить("seq_no", 1);
		Шаг.Вставить("kind", "run_query");
		Шаг.Вставить("title", "Выполнить тестовый запрос");
		Шаг.Вставить("depends_on", Новый Массив);
		Input = Новый Структура;
		Input.Вставить("query", "ВЫБРАТЬ 1 КАК Номер");
		Шаг.Вставить("input", Input);
		Шаг.Вставить("status", "pending");
		Шаг.Вставить("result_summary", "");
		Шаг.Вставить("result_data", Неопределено);
		Шаги.Добавить(Шаг);
		План.Вставить("steps", Шаги);
		ИИА_TypedPlan.СохранитьTypedPlan(СсылкаДиалога, План);

		Сообщение = Новый Структура;
		Сообщение.Вставить("role", "assistant");
		ToolCalls = Новый Массив;
		ФункцияTool = Новый Структура;
		ФункцияTool.Вставить("name", "submit_dsl");
		ФункцияTool.Вставить("arguments", "{""dsl_version"":2}");
		ToolCall = Новый Структура;
		ToolCall.Вставить("id", "tool-test-recover-1");
		ToolCall.Вставить("type", "function");
		ToolCall.Вставить("function", ФункцияTool);
		ToolCalls.Добавить(ToolCall);
		Сообщение.Вставить("tool_calls", ToolCalls);

		ПараметрыИИ = Новый Структура;
		ПараметрыИИ.Вставить("СсылкаДиалога", СсылкаДиалога);
		Распознанный = ИИА_Провайдеры.ПостроитьРезультатИзToolCall(Сообщение, "Чат", ПараметрыИИ);
		Если Распознанный.ТипОтвета <> "DSL" Тогда
			Результат.Сообщение = "submit_dsl с неполным JSON должен восстанавливаться из текущего плана";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Распознанный.DSL, """RunQuery""") = 0 ИЛИ СтрНайти(Распознанный.DSL, """steps""") = 0 Тогда
			Результат.Сообщение = "Восстановленный DSL должен содержать шаг RunQuery из плана";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: submit_dsl восстанавливается из текущего плана";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестНативнаяПодготовкаImagePdf() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьИзображения = СоздатьВременныйФайлИзображенияДляТеста();
		ПутьPDF = СоздатьВременныйPDFДляТеста();
		ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьИзображения, "native.png");
		ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьPDF, "native.pdf");
		Сообщения = Новый Массив;
		СообщениеСистема = Новый Структура("role,content", "system", "system");
		СообщениеПользователь = Новый Структура("role,content", "user", "Опиши приложенные файлы");
		Сообщения.Добавить(СообщениеСистема);
		Сообщения.Добавить(СообщениеПользователь);
		ПараметрыИИ = Новый Структура;
		ПараметрыИИ.Вставить("СсылкаДиалога", СсылкаДиалога);
		ПараметрыИИ.Вставить("Provider_BaseUrl", "https://api.openai.com/v1");
		Подготовленные = ИИА_Провайдеры.ПодготовитьСообщенияСНативнымиВложениями(Сообщения, ПараметрыИИ);
		Последнее = Подготовленные[Подготовленные.Количество() - 1];
		Если НЕ Последнее.Свойство("content") ИЛИ ТипЗнч(Последнее.content) <> Тип("Массив") Тогда
			Результат.Сообщение = "Для image/pdf content последнего сообщения должен превращаться в multimodal-массив";
			Возврат Результат;
		КонецЕсли;
		НашлиИзображение = Ложь;
		НашлиФайл = Ложь;
		Для Каждого Часть Из Последнее.content Цикл
			Если Часть.Свойство("type") И Часть.type = "image_url" Тогда
				НашлиИзображение = Истина;
			ИначеЕсли Часть.Свойство("type") И Часть.type = "file" Тогда
				НашлиФайл = Истина;
			КонецЕсли;
		КонецЦикла;
		Если НЕ НашлиИзображение ИЛИ НЕ НашлиФайл Тогда
			Результат.Сообщение = "Multimodal content должен содержать native image и native file для PDF";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: image/pdf готовятся для нативной передачи";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестВложенияDOCX() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьDOCX = СоздатьВременныйDOCXДляТеста();
		Вложение = ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьDOCX, "sample.docx");
		Если НЕ Вложение.Свойство("kind") ИЛИ Вложение.kind <> "text" Тогда
			Результат.Сообщение = "DOCX должен классифицироваться как text";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Вложение.Свойство("extracted_text") ИЛИ СтрНайти(Строка(Вложение.extracted_text), "docx line 1") = 0 Тогда
			Результат.Сообщение = "DOCX должен извлекаться в текст через локальный контур";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: DOCX локально расшифровывается";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция НайтиНедопустимыйСимволXDTO(Текст)
	Если ПустаяСтрока(Текст) Тогда
		Возврат 0;
	КонецЕсли;
	Для Позиция = 1 По СтрДлина(Текст) Цикл
		Символ = Сред(Текст, Позиция, 1);
		Код = КодСимвола(Символ);
		Если НЕ (Код = 9 ИЛИ Код = 10 ИЛИ Код = 13 ИЛИ Код >= 32) Тогда
			Возврат Позиция;
		КонецЕсли;
	КонецЦикла;
	Возврат 0;
КонецФункции

Функция ТестСообщенияДиалогаОчищаютсяДляXDTO() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ТекстСлужебный = "До" + Символ(0) + "После";
		ИИА_Сервер.ДобавитьСообщениеВДиалог(
			СсылкаДиалога,
			Перечисления.ИИА_АвторСообщения.Пользователь,
			Перечисления.ИИА_ТипСообщения.Текст,
			ТекстСлужебный
		);
		Сообщения = ИИА_Сервер.ПолучитьСообщенияДиалога(СсылкаДиалога, 10);
		Если Сообщения.Количество() <> 1 Тогда
			Результат.Сообщение = "Ожидалось одно сообщение в диалоге";
			Возврат Результат;
		КонецЕсли;
		Если НайтиНедопустимыйСимволXDTO(Строка(Сообщения[0].Текст)) > 0 Тогда
			Результат.Сообщение = "ПолучитьСообщенияДиалога должна возвращать XDTO-безопасный текст";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Строка(Сообщения[0].Текст), "ДоПосле") = 0 Тогда
			Результат.Сообщение = "После очистки сообщение должно сохранять полезный текст";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: сообщения диалога очищаются для XDTO";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестПромптВключаетКонтекстВложений() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ПутьMXL = "";
	Попытка
		СсылкаДиалога = ИИА_Сервер.СоздатьНовыйДиалог("Администратор", Перечисления.ИИА_ТипДиалога.Агент);
		ПутьMXL = СоздатьВременныйMXLДляТеста();
		ИИА_Вложения.ДобавитьВложениеИзФайла(СсылкаДиалога, ПутьMXL);

		ИсторияМассив = ИИА_Сервер.ПолучитьСообщенияДиалога(СсылкаДиалога, 10);
		История = ИИА_Сервер.ПреобразоватьМассивСообщенийВТаблицу(ИсторияМассив);
		Промпт = ИИА_Промты.СформироватьПромпт(Перечисления.ИИА_ТипДиалога.Агент, "Чат", "Что в приложенной таблице?", История, "", СсылкаДиалога, Ложь);
		Если Промпт = Неопределено ИЛИ НЕ Промпт.Свойство("Сообщения") Тогда
			Результат.Сообщение = "Промпт должен содержать сообщения";
			Возврат Результат;
		КонецЕсли;
		ПоследнееСообщение = Промпт.Сообщения[Промпт.Сообщения.Количество() - 1];
		Контент = ?(ПоследнееСообщение.Свойство("content"), Строка(ПоследнееСообщение.content), "");
		Если СтрНайти(Контент, "КОНТЕКСТ ВЛОЖЕНИЙ:") = 0 Тогда
			Результат.Сообщение = "Контекст вложений должен попадать в prompt";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Контент, ".mxl") = 0 Тогда
			Результат.Сообщение = "Имя MXL-файла должно попадать в prompt";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: prompt содержит контекст вложений";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsSelectorЗапрос1С() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Скилы = ИИА_Skills.ПодобратьСкилы("Покажи топ контрагентов по продажам за месяц", Перечисления.ИИА_ТипДиалога.Агент, , 3);
		Имена = ИИА_Skills.ПолучитьИменаСкиловСтрокой(Скилы);
		Если СтрНайти(Имена, "query-1c-safe") = 0 Тогда
			Результат.Сообщение = "Selector должен выбрать query-1c-safe, выбрано: " + Имена;
			Возврат Результат;
		КонецЕсли;
		Если Скилы.Количество() > 3 Тогда
			Результат.Сообщение = "Selector должен соблюдать лимит top-N";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: skills selector выбирает query-1c-safe";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsБлокПопадаетВПромпт() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ИмяТестовогоСкила = "user-test-prompt-target";
	Попытка
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		JSON = "{"
			+ """name"":""" + ИмяТестовогоСкила + ""","
			+ """title"":""Prompt target test"","
			+ """description"":""Проверка target diagnostics в prompt"","
			+ """enabled"":true,"
			+ """triggers"":[""e2e target skill"",""заказ клиента""],"
			+ """dialog_types"":[""Агент""],"
			+ """target_object_type"":""Document"","
			+ """target_object_name"":""ЗаказПокупателя"","
			+ """prompt"":""Проверь метаданные Документ.ЗаказПокупателя перед заполнением заказа клиента."","
			+ """risk_level"":""write"","
			+ """enforcement"":""warn"","
			+ """approval_required"":true,"
			+ """workflow"":[],"
			+ """policy"":{"
			+ """risk_level"":""write"","
			+ """enforcement"":""warn"","
			+ """approval_required"":true,"
			+ """allowed_actions"":[""GetMetadata"",""GetObjectFields"",""CreateDocument"",""SetField"",""Write"",""ShowInfo""],"
			+ """forbidden_actions"":[""DeleteObject""],"
			+ """required_checks"":[""metadata_before_write"",""approval_before_write""]"
			+ "}}";
		ИИА_Skills.СохранитьПользовательскийСкил(ИИА_Skills.КарточкаИзJSON(JSON));

		История = Новый ТаблицаЗначений;
		История.Колонки.Добавить("Автор");
		История.Колонки.Добавить("ТипСообщения");
		История.Колонки.Добавить("Текст");
		История.Колонки.Добавить("ТекстКода");

		ПакетПромпта = ИИА_Промты.СформироватьПромпт(Перечисления.ИИА_ТипДиалога.Агент, "Чат", "e2e target skill создай заказ клиента", История, "", Неопределено, Ложь);
		ПоследнееСообщение = ПакетПромпта.Сообщения[ПакетПромпта.Сообщения.Количество() - 1];
		Контент = ?(ПоследнееСообщение.Свойство("content"), Строка(ПоследнееСообщение.content), "");

		Если СтрНайти(Контент, "АКТИВНЫЕ SKILLS:") = 0 Тогда
			ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
			Результат.Сообщение = "Skill-блок должен попадать в prompt";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Контент, ИмяТестовогоСкила) = 0 Тогда
			ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
			Результат.Сообщение = "Prompt должен содержать выбранный пользовательский skill";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Контент, "target_object_name=ЗаказПокупателя") = 0 Тогда
			ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
			Результат.Сообщение = "Skill-блок должен выводить target_object_name для диагностики";
			Возврат Результат;
		КонецЕсли;
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: skill-блок добавляется в prompt";
	Исключение
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестWriteFlowЗаказПокупателяСТранзакционнымКлинапом() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ТранзакцияОткрыта = Ложь;
	Попытка
		Маркер = "E2E_WRITE_ZP_" + Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		НачатьТранзакцию();
		ТранзакцияОткрыта = Истина;

		Док = Документы.ЗаказПокупателя.СоздатьДокумент();
		Док.Дата = ТекущаяДата();
		Если ИИА_Метаданные.СуществуетРеквизитДокумента("ЗаказПокупателя", "Комментарий") Тогда
			Док.Комментарий = Маркер;
		КонецЕсли;
		Док.Записать();
		Результат.Детали.Добавить("Записан документ в транзакции: " + Строка(Док.Ссылка));

		ОтменитьТранзакцию();
		ТранзакцияОткрыта = Ложь;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: ЗаказПокупателя создается и записывается с rollback cleanup";
	Исключение
		Если ТранзакцияОткрыта Тогда
			ОтменитьТранзакцию();
		КонецЕсли;
		Результат.Сообщение = "Ошибка write-flow ЗаказПокупателя: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsWarnВалидаторDSL() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Скилы = Новый Массив;
		Скилы.Добавить(ИИА_Skills.ПолучитьСкил("query-1c-safe"));
		DSLJSON = "{""dsl_version"":2,""steps"":[{""action"":""Write""}]}";
		Проверка = ИИА_Skills.ПроверитьDSLПоСкилам(DSLJSON, Скилы, "warn");
		Если Проверка = Неопределено ИЛИ НЕ Проверка.Свойство("Нарушения") Тогда
			Результат.Сообщение = "Warn-валидатор должен вернуть структуру с нарушениями";
			Возврат Результат;
		КонецЕсли;
		Если Проверка.Нарушения.Количество() = 0 Тогда
			Результат.Сообщение = "Warn-валидатор должен найти Write для read-skill";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Проверка.Успех Тогда
			Результат.Сообщение = "В режиме warn проверка не должна блокировать DSL";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: skills warn-валидатор находит нарушение";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsПользовательскийЧерновик() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Описание = "Создай skill: когда я пишу оформи заявку на оплату, агент должен создать документ ЗаявкаНаРасходованиеДенежныхСредств, заполнить контрагента, сумму, статью ДДС, договор и показать перед записью.";
		Карточка = ИИА_Skills.СформироватьЧерновикСкилаИзОписания(Описание);
		Если Карточка = Неопределено Тогда
			Результат.Сообщение = "Генератор черновика вернул Неопределено";
			Возврат Результат;
		КонецЕсли;
		Если Карточка.ЦелевойОбъектТип <> "Document" Тогда
			Результат.Сообщение = "Черновик должен определить Document";
			Возврат Результат;
		КонецЕсли;
		Если Карточка.ЦелевойОбъектИмя <> "ЗаявкаНаРасходованиеДенежныхСредств" Тогда
			Результат.Сообщение = "Черновик должен определить ЗаявкаНаРасходованиеДенежныхСредств";
			Возврат Результат;
		КонецЕсли;
		Если НЕ Карточка.ТребоватьПодтверждение Тогда
			Результат.Сообщение = "Write-skill должен требовать подтверждение";
			Возврат Результат;
		КонецЕсли;
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: пользовательский черновик skill формируется";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsПользовательскийLifecycle() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ИмяТестовогоСкила = "user-test-payment-request";
	Попытка
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);

		Описание = "Создай skill: когда я пишу оформи заявку на оплату, агент должен создать документ ЗаявкаНаРасходованиеДенежныхСредств, заполнить контрагента, сумму, статью ДДС, договор и показать перед записью.";
		Карточка = ИИА_Skills.СформироватьЧерновикСкилаИзОписания(Описание);
		Карточка.Имя = ИмяТестовогоСкила;
		Карточка.Заголовок = "Тестовая заявка на оплату";
		Карточка.Активен = Истина;
		ИИА_Skills.СохранитьПользовательскийСкил(Карточка);

		Скилы = ИИА_Skills.ПодобратьСкилы("оформи заявку на оплату", Перечисления.ИИА_ТипДиалога.Агент, , 5);
		Имена = ИИА_Skills.ПолучитьИменаСкиловСтрокой(Скилы);
		Если СтрНайти(Имена, ИмяТестовогоСкила) = 0 Тогда
			Результат.Сообщение = "Selector должен выбрать пользовательский skill, выбрано: " + Имена;
			Возврат Результат;
		КонецЕсли;

		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		СкилыПослеУдаления = ИИА_Skills.ПодобратьСкилы("оформи заявку на оплату", Перечисления.ИИА_ТипДиалога.Агент, , 5);
		ИменаПослеУдаления = ИИА_Skills.ПолучитьИменаСкиловСтрокой(СкилыПослеУдаления);
		Если СтрНайти(ИменаПослеУдаления, ИмяТестовогоСкила) > 0 Тогда
			Результат.Сообщение = "Удаленный пользовательский skill не должен выбираться";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: пользовательский skill сохраняется, выбирается и удаляется";
	Исключение
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsJSONLifecycle() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ИмяТестовогоСкила = "user-test-json-skill";
	Попытка
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);

		JSON = "{"
			+ """name"":""" + ИмяТестовогоСкила + ""","
			+ """title"":""JSON skill тест"","
			+ """description"":""Проверка переносимого JSON skill"","
			+ """enabled"":true,"
			+ """triggers"":[""создай документ"",""поступление""],"
			+ """dialog_types"":[""Агент""],"
			+ """target_object_type"":""Document"","
			+ """target_object_name"":""ПоступлениеТоваровУслуг"","
			+ """prompt"":""Создай документ с нужными пользователю действиями."","
			+ """risk_level"":""write"","
			+ """enforcement"":""warn"","
			+ """approval_required"":true,"
			+ """workflow"":[],"
			+ """policy"":{"
			+ """risk_level"":""write"","
			+ """enforcement"":""warn"","
			+ """approval_required"":true,"
			+ """allowed_actions"":[""GetMetadata"",""CreateDocument"",""SetField"",""Write"",""ShowInfo""],"
			+ """forbidden_actions"":[""DeleteObject""],"
			+ """required_checks"":[""metadata_before_write"",""approval_before_write""]"
			+ "}}";

		Карточка = ИИА_Skills.КарточкаИзJSON(JSON);
		Если Карточка.Имя <> ИмяТестовогоСкила Тогда
			Результат.Сообщение = "JSON import должен заполнить код skill";
			Возврат Результат;
		КонецЕсли;
		Проверка = ИИА_Skills.ПротестироватьJSONСкила(JSON, "создай документ поступление для поставщика");
		Если НЕ Проверка.matched Тогда
			Результат.Сообщение = "Тест JSON skill должен найти совпадение по триггерам";
			Возврат Результат;
		КонецЕсли;

		ИИА_Skills.СохранитьПользовательскийСкил(Карточка);
		Скилы = ИИА_Skills.ПодобратьСкилы("создай документ поступление для поставщика", Перечисления.ИИА_ТипДиалога.Агент, , 5);
		Имена = ИИА_Skills.ПолучитьИменаСкиловСтрокой(Скилы);
		Если СтрНайти(Имена, ИмяТестовогоСкила) = 0 Тогда
			Результат.Сообщение = "Selector должен выбрать skill из JSON, выбрано: " + Имена;
			Возврат Результат;
		КонецЕсли;

		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: JSON skill импортируется, тестируется, сохраняется и удаляется";
	Исключение
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsStrictPolicyBlocksForbidden() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ИмяТестовогоСкила = "user-test-policy-strict";
	Попытка
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		JSON = "{"
			+ """name"":""" + ИмяТестовогоСкила + ""","
			+ """title"":""Policy strict test"","
			+ """description"":""Проверка strict policy"","
			+ """enabled"":true,"
			+ """triggers"":[""policy strict e2e""],"
			+ """dialog_types"":[""Агент""],"
			+ """prompt"":""Разрешай только чтение и ShowInfo."","
			+ """risk_level"":""read"","
			+ """enforcement"":""strict"","
			+ """approval_required"":false,"
			+ """policy"":{"
			+ """risk_level"":""read"","
			+ """enforcement"":""strict"","
			+ """approval_required"":false,"
			+ """allowed_actions"":[""GetMetadata"",""ShowInfo""],"
			+ """forbidden_actions"":[""DeleteObject"",""PostDocument""],"
			+ """required_checks"":[]"
			+ "}}";
		ИИА_Skills.СохранитьПользовательскийСкил(ИИА_Skills.КарточкаИзJSON(JSON));
		Скил = ИИА_Skills.ПолучитьСкил(ИмяТестовогоСкила);
		Скилы = Новый Массив;
		Скилы.Добавить(Скил);

		ПроверкаAllowed = ИИА_Skills.ПроверитьDSLПоСкилам("{""dsl_version"":2,""steps"":[{""action"":""ShowInfo"",""message"":""ok""}]}", Скилы, "strict");
		Если НЕ ПроверкаAllowed.Успех Тогда
			Результат.Сообщение = "Strict policy не должна блокировать allowed action ShowInfo";
			Возврат Результат;
		КонецЕсли;

		ПроверкаWrite = ИИА_Skills.ПроверитьDSLПоСкилам("{""dsl_version"":2,""steps"":[{""action"":""Write""}]}", Скилы, "strict");
		Если ПроверкаWrite.Успех ИЛИ ПроверкаWrite.Нарушения.Количество() = 0 Тогда
			Результат.Сообщение = "Strict policy должна блокировать write action для read skill";
			Возврат Результат;
		КонецЕсли;

		ПроверкаForbidden = ИИА_Skills.ПроверитьDSLПоСкилам("{""dsl_version"":2,""steps"":[{""action"":""DeleteObject""}]}", Скилы, "strict");
		Если ПроверкаForbidden.Успех ИЛИ ПроверкаForbidden.Нарушения.Количество() = 0 Тогда
			Результат.Сообщение = "Strict policy должна блокировать forbidden action DeleteObject";
			Возврат Результат;
		КонецЕсли;

		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: strict policy блокирует forbidden/write действия";
	Исключение
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsJSONПереносимостьИНетриггер() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	ИмяТестовогоСкила = "user-test-portable-json";
	Попытка
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		JSON = "{"
			+ """name"":""" + ИмяТестовогоСкила + ""","
			+ """title"":""Portable JSON test"","
			+ """description"":""Проверка копирования JSON skill"","
			+ """enabled"":true,"
			+ """owner"":""user"","
			+ """triggers"":[""portable skill marker""],"
			+ """dialog_types"":[""Агент""],"
			+ """target_object_type"":""Document"","
			+ """target_object_name"":""ЗаказПокупателя"","
			+ """prompt"":""Создай заказ покупателя по переносимому JSON."","
			+ """risk_level"":""write"","
			+ """enforcement"":""warn"","
			+ """approval_required"":true,"
			+ """template_vars"":{""comment"":{""type"":""string"",""required"":true}},"
			+ """dsl_template_mode"":""strict"","
			+ """dsl_template"":{""dsl_version"":2,""steps"":[{""action"":""CreateDocument"",""object_name"":""ЗаказПокупателя""},{""action"":""Write""}]},"
			+ """policy"":{""risk_level"":""write"",""enforcement"":""warn"",""approval_required"":true,""allowed_actions"":[""CreateDocument"",""Write"",""ShowInfo""],""forbidden_actions"":[""DeleteObject""],""required_checks"":[]}"
			+ "}";
		Карточка = ИИА_Skills.КарточкаИзJSON(JSON);
		ИИА_Skills.СохранитьПользовательскийСкил(Карточка);
		ЭкспортJSON = ИИА_Skills.КарточкаВJSON(ИИА_Skills.ПолучитьКарточкуСкила(ИмяТестовогоСкила));
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		ИИА_Skills.СохранитьПользовательскийСкил(ИИА_Skills.КарточкаИзJSON(ЭкспортJSON));

		СкилыСовпали = ИИА_Skills.ПодобратьСкилы("portable skill marker создай заказ", Перечисления.ИИА_ТипДиалога.Агент, , 5);
		Если СтрНайти(ИИА_Skills.ПолучитьИменаСкиловСтрокой(СкилыСовпали), ИмяТестовогоСкила) = 0 Тогда
			Результат.Сообщение = "Импортированный JSON skill должен выбираться по триггеру";
			Возврат Результат;
		КонецЕсли;

		СкилыНеСовпали = ИИА_Skills.ПодобратьСкилы("совершенно другой запрос без маркера", Перечисления.ИИА_ТипДиалога.Агент, , 5);
		Если СтрНайти(ИИА_Skills.ПолучитьИменаСкиловСтрокой(СкилыНеСовпали), ИмяТестовогоСкила) > 0 Тогда
			Результат.Сообщение = "Skill не должен выбираться без совпадения триггера";
			Возврат Результат;
		КонецЕсли;

		Если СтрНайти(ЭкспортJSON, """dsl_template""") = 0 ИЛИ СтрНайти(ЭкспортJSON, """dsl_template_mode""") = 0 Тогда
			Результат.Сообщение = "Экспорт JSON должен сохранять DSL template";
			Возврат Результат;
		КонецЕсли;

		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: JSON skill переносится и не выбирается без триггера";
	Исключение
		ИИА_Skills.УдалитьПользовательскийСкил(ИмяТестовогоСкила);
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестDSLПостроительCreateDocumentFieldsWrite() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Шаг = Новый Структура;
		Шаг.Вставить("id", "step_create_order");
		Шаг.Вставить("seq_no", 1);
		Шаг.Вставить("kind", "create_document");
		Шаг.Вставить("title", "Создать и записать заказ покупателя");
		Input = Новый Структура;
		Input.Вставить("object_name", "ЗаказПокупателя");
		Поля = Новый Структура;
		Поля.Вставить("Комментарий", "DSL_BUILDER_FIELDS_WRITE");
		Поля.Вставить("Дата", ТекущаяДата());
		Input.Вставить("fields", Поля);
		Input.Вставить("write", Истина);
		Шаг.Вставить("input", Input);

		DSLJSON = ИИА_DSLПостроитель.ПостроитьDSLИзШагаПлана(Шаг, 1);
		Если ПустаяСтрока(DSLJSON) Тогда
			Результат.Сообщение = "DSL builder должен вернуть JSON для create_document";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(DSLJSON, """CreateDocument""") = 0 ИЛИ СтрНайти(DSLJSON, """Write""") = 0 Тогда
			Результат.Сообщение = "DSL должен содержать CreateDocument и Write";
			Результат.Детали.Добавить(DSLJSON);
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(DSLJSON, """data""") = 0 ИЛИ СтрНайти(DSLJSON, "DSL_BUILDER_FIELDS_WRITE") = 0 Тогда
			Результат.Сообщение = "DSL должен переносить fields/data из плана";
			Результат.Детали.Добавить(DSLJSON);
			Возврат Результат;
		КонецЕсли;
		Проверка = ИИА_DSL.ПроверитьDSL(DSLJSON);
		Если НЕ Проверка.Успех Тогда
			Результат.Сообщение = "Сгенерированный DSL должен проходить контракт";
			Результат.Детали.Добавить(Проверка.Сообщение);
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: create_document переносит fields и write=true";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsГенерацияИзСистемнойКарточки() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		СистемнаяКарточка = ИИА_Skills.ПолучитьКарточкуСкила("query-1c-safe");
		Описание = "Когда пользователь просит создать заявку на оплату поставщику, агент должен создать документ ЗаявкаНаРасходованиеДенежныхСредств, найти контрагента и договор, заполнить сумму, дату платежа, назначение платежа, показать предварительный результат и записать документ только после подтверждения пользователя.";
		Карточка = ИИА_Skills.СформироватьЧерновикСкилаИзОписания(Описание, СистемнаяКарточка);

		Если Карточка.Имя = "query-1c-safe" Тогда
			Результат.Сообщение = "Генерация из системной карточки должна создать новый пользовательский код";
			Возврат Результат;
		КонецЕсли;
		Если НРег(Карточка.Владелец) = "system" Тогда
			Результат.Сообщение = "Генерация из системной карточки не должна наследовать владельца system";
			Возврат Результат;
		КонецЕсли;
		Если Карточка.Заголовок = "Безопасные запросы 1С" Тогда
			Результат.Сообщение = "Генерация из системной карточки не должна наследовать системный заголовок";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(НРег(Карточка.Заголовок), "заявк") = 0 Тогда
			Результат.Сообщение = "Черновик должен получить понятный заголовок по найденному объекту, получено: " + Карточка.Заголовок;
			Возврат Результат;
		КонецЕсли;
		Если НЕ Карточка.Активен Тогда
			Результат.Сообщение = "Сформированный пользовательский skill должен быть активным по умолчанию";
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Карточка.WorkflowJSON, "Заполнить сумму") = 0 Тогда
			Результат.Сообщение = "Workflow должен включать заполнение суммы";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: генерация из системной карточки создает пользовательский skill";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ТестSkillsГенераторИщетОбъектПоМетаданным() Экспорт
	Результат = СформироватьРезультатТеста(Ложь, "", Новый Массив);
	Попытка
		Описание = "Когда пользователь просит добавить новую номенклатуру, агент должен найти или создать элемент справочника Номенклатура, заполнить наименование и артикул, показать результат и записать после подтверждения.";
		Карточка = ИИА_Skills.СформироватьЧерновикСкилаИзОписания(Описание);

		Если Карточка.ЦелевойОбъектТип <> "Catalog" Тогда
			Результат.Сообщение = "Генератор должен определить тип Catalog, получено: " + Карточка.ЦелевойОбъектТип;
			Возврат Результат;
		КонецЕсли;
		Если Карточка.ЦелевойОбъектИмя <> "Номенклатура" Тогда
			Результат.Сообщение = "Генератор должен найти справочник Номенклатура по метаданным, получено: " + Карточка.ЦелевойОбъектИмя;
			Возврат Результат;
		КонецЕсли;
		Если СтрНайти(Карточка.WorkflowJSON, "Найти или создать элемент справочника") = 0 Тогда
			Результат.Сообщение = "Workflow должен строиться по типу найденного объекта";
			Возврат Результат;
		КонецЕсли;

		Результат.Успех = Истина;
		Результат.Сообщение = "Тест пройден: генератор ищет объект по метаданным";
	Исключение
		Результат.Сообщение = "Ошибка: " + ОписаниеОшибки();
		Результат.Детали.Добавить(Результат.Сообщение);
	КонецПопытки;
	Возврат Результат;
КонецФункции

#КонецОбласти




