# TODO

## Глобальные улучшения агента

1. [ ] Сделать агента stateful по знаниям о БД
   - Кэшировать валидные объекты/поля/связи и переиспользовать между сессиями.
   - Эффект: меньше «угадывания» и recovery-циклов.

2. [ ] Разделить стратегию на `fast-path` и `safe-path`
   - `fast-path`: минимум шагов для типовых запросов.
   - `safe-path`: обязательные проверки схемы/полей/доступов для сложных или неясных задач.
   - Переключение автоматически по риску запроса.

3. [ ] Ввести бюджет выполнения на уровне оркестрации
   - Лимиты не только на токены, но и на число циклов `Plan -> Execute -> Recover`.
   - При превышении — ранний stop с техничным, полезным summary.

4. [ ] Формализовать DSL как `contract-first`
   - Единый валидатор + нормализатор до execute.
   - Строгие версии контракта (`dsl_version`) и совместимость между версиями.

5. [ ] Укрепить стратегию деградации (`graceful degradation`)
   - Если «идеальный» путь не проходит, агент должен отдавать частично полезный результат, а не застревать.
   - Пример: «вот данные по ближайшему валидному объекту + что нужно уточнить для точного ответа».

6. [ ] Сделать observability как у production-сервиса
   - Трассировка по `trace_id`, метрики `p50/p95 latency`, `token cost per intent`, `recovery success rate`.
   - Дашборд причин деградации качества (а не только `pass/fail`).

7. [ ] Улучшить качество промптов через policy-layer
   - Не раздувать один огромный промпт, а собирать его из модульных политик.
   - Политики: безопасность, формат ответа, SQL/1C-style ограничения, anti-loop.

8. [ ] Развивать автотесты как систему качества, а не только gate
   - Добавить regression-набор «из боевых логов».
   - Делать дифф-проверку не только по `pass/fail`, но и по эффективности (токены, шаги, retries).

9. [ ] Стабилизировать RAG-контур
   - Регулярное обновление индекса, контроль качества чанков, отсев шумных кандидатов.
   - Ранжирование по практической пригодности для `RunQuery`, а не по одной текстовой похожести.
   - Связи по объектам метаданных (ссылочные реквизиты, граф зависимостей).
   - Единый лимит токенов на RAG-блок в промпте (сейчас: Top-5 × 300 символов, без общего ceiling).
   - Опционально: семантический поиск (embeddings + Qdrant/MCP) вместо только TF-IDF.

10. [ ] Защита RunQuery от тяжёлых запросов
   - Текущее состояние: нет предварительного просмотра и одобрения — DSL выполняется сразу, без подтверждения пользователем.
   - Детекция декартова соединения (JOIN без условий / без ограничения).
   - Лимит на число строк результата (timeout или max_rows).
   - Предварительный просмотр и одобрение пользователем перед выполнением (preview → approve) — было бы неплохо сделать.

11. [ ] Настроить управление рисками изменений
    - Canary rollout: сначала на части запросов.
    - Авто-rollback промпт/политик при резком росте ошибок или стоимости.

12. [ ] Рассмотреть вариант 2: отдельный локальный сервис `rag-retriever`
    - Суть: вынести RAG в отдельный сервис рядом с БД (`Postgres + pgvector`), а LLM оставить за внешним AI-прокси.
    - Зона ответственности сервиса: индексация данных БД (контрагенты, документы и т.п.), поиск top-k, опциональный rerank, сбор контекста.
    - Минимальный API-контракт: `POST /index/upsert`, `POST /search`, `GET /health`, `GET /version`.
    - Плюсы: локальность данных, независимое масштабирование retrieval, проще контроль качества индекса и обновлений.
    - Риски/что проверить: фильтрация по правам доступа (RBAC/tenant), стратегия переиндексации, latency на связке `retriever -> proxy`.

13. [ ] Подключение локальных моделей: доработки в общем модуле ИИА_Провайдеры
    - В функции `ВыполнитьHTTPИнструмент`:
      1. Выделять из `БазовыйДомен` порт подключения (не у всех это порт 443).
      2. Учесть, что локальный ИИ может быть развёрнут без SSL.

14. [ ] Проверка попадания в индексы в селектах и джойнах
    - Анализ условий WHERE и JOIN на использование индексированных полей.
    - Предупреждение или блокировка запросов, которые могут привести к full table scan.

    Приоритетно

Усилить проверку содержимого, а не только факта завершения.
Сейчас UI-тесты хорошо ловят поломку сценария, но слабо доказывают корректность данных. Для RunQuery стоит проверять не только завершение, но и минимум один ожидаемый маркер в результате: колонку, имя объекта, число строк, открытие Таблица с нужными заголовками.

Закрыть стык typed plan -> DSL builder -> execute/resume интеграционными тестами.
Именно там уже были реальные дефекты: потеря field_name, ПЕРВЫЕ 1, неверная нормализация полей. Нужны отдельные серверные тесты на:

create_reference -> set_field -> write
resume после AwaitApproval
нормализацию похожих полей
генерацию query без лишних ограничений
Убрать зависимость UX от “скрытых” форм 1С.
Поведение с ТабличныйДокумент и фокусом уже правилось вручную. Лучше сделать единый контракт:

если есть табличный результат, всегда показывать ссылку
фокус всегда возвращать на агент
summary всегда короткий и предсказуемый
Средний приоритет
4. Нормализовать enum/UI-значения в одном месте.
У вас уже есть смешение Agent, Агент, Запрос1С, Запрос 1С. Это надо централизовать в один mapping, чтобы не чинить это отдельно в bridge, UI и тестах.

Сделать structured result для UI.
Сейчас UI и тесты часто читают текстовые следы. Лучше хранить в диалоге формализованный результат шага:
status
action
row_count
columns
created_object_ref
approval_required
Тогда и UI, и тесты будут опираться на данные, а не на текст.
Улучшить observability логов.
Сейчас по логам можно расследовать проблему, но не быстро. Я бы добавил обязательные поля в каждый step log:
step_id
kind
input
normalized_input
effective_dsl
resume_from_step
result_summary
Низкий, но полезный приоритет
7. Сделать отдельный nightly прогон “examples + mutation”.
Не только README examples, но и вариации формулировок:

синонимы объектов
опечатки в полях
пустые данные
конфликт дубликатов
Это быстро покажет деградацию планировщика.
Разделить качество на 3 слоя.
Сейчас у вас местами смешаны UX, orchestration и data correctness. Я бы фиксировал качество отдельно:
Product UX
Execution correctness
Business result correctness
Если сжать до самого практичного плана, я бы делал так:

Интеграционные тесты на AwaitApproval/Resume и typed plan -> DSL.
Structured result contract для RunQuery/Write/CreateReference.
UI-проверку содержимого таблицы, а не только факта открытия.
Централизованный mapping типов диалога и action names.
