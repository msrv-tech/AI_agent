# 1C AI Agent v0.9.0

Основной фокус релиза - функционал skills для обычного агента 1С.

## Что нового

- Добавлен реестр skills в 1С: системные и пользовательские сценарии.
- Добавлена форма `ИИА_Skills` с JSON-редактором, генерацией JSON по описанию задачи и тестовым запуском.
- Skills подключаются к обычной форме `ИИ Агент` и попадают в prompt-контекст при совпадении с запросом пользователя.
- В prompt-блок добавлены диагностические поля `target_object_type` и `target_object_name`.
- Для write-сценариев поддерживаются policy, список разрешенных/запрещенных действий и требование подтверждения.
- Улучшен UI формы агента: компактный ввод, пустое состояние, панель хода выполнения, меньше лишних обновлений.

## Проверка

- `automation/bsl/run-bsl-analyze.bat`
- `python automation/ui/web_agent_skill_e2e.py`
- `python automation/ui/web_skills_negative_ui.py`
- `python automation/ui/web_agent_modes_e2e.py`
- `python automation/ui/capture_skills_release_screenshots.py`

## Артефакт

- `ИИ_Агент_0.9.0_8.5.cfe`

Локальная сборка 8.3 через временную файловую базу не выполнялась до конца из-за отсутствия доступной локальной лицензии 8.3 на машине сборки. Расширение сохранено с compatibility mode 8.3.26, релизный `.cfe` выгружен из dev-базы платформой 8.5.1.1150.
