# CLAUDE.md — Контекст проекта для AI-ассистента

## Проект: Хакатон AI Sprint 2026 — Кейс 11

Форк репозитория `msrv-tech/AI_agent` для участия в хакатоне AI Sprint 2026.

## Кейс 11: Агент-консультант менеджера по продажам

**Задача:** Создать диалогового AI-агента внутри 1С, который по запросу менеджера выдаёт:
1. Актуальные цены по позициям из прайс-листа
2. Историю продаж клиенту за период с расчётом маржинальности
3. Состояние дебиторской задолженности и просрочки

Агент объединяет данные из нескольких источников и отвечает в рамках одного диалога.

**Бонусные сценарии (реализованы):**
4. Загрузка прайс-листа из Excel-файла (`ImportPriceList`)
5. Создание карточки контрагента из файла xlsx/pdf/docx/txt (`ParseFileForContractor`)
6. Формирование сводного отчёта по продажам (мульти-источник)

**Овер-бонус (реализован):**
7. Семантический поиск через Yandex Embeddings (гибридный TF-IDF + cosine similarity)

## Критерии оценки

| Критерий | Вес |
|----------|-----|
| Корректность данных (цены, суммы, задолженности) | 25% |
| Интеграция нескольких источников данных | 25% |
| Качество диалога (контекст, уточняющие вопросы) | 20% |
| Полнота сценария (все 3 обязательных) | 15% |
| UX и демонстрация | 15% |

Бонус: расчёт маржинальности, сравнение с ценой производителя.

## Технический стек

- **Платформа:** 1С:Предприятие 8.3.26+
- **Конфигурация-основа:** 1С:ERP Управление предприятием 2
- **Расширение:** Форк msrv-tech/AI_agent v0.8.0 (CFE, префикс ИИА_)
- **LLM-провайдер:** Яндекс.Cloud (OpenAI-совместимый API, токен от организаторов)
- **Запасной LLM:** LM Studio на локальном сервере (GMKtec EVO X2, Ryzen AI Max+ 395, 128GB RAM), http://192.168.11.150:1234/v1
- **Embeddings:** Yandex Foundation Models (`text-search-doc/query`, 256-dim)
- **СУБД:** PostgreSQL

## Архитектура расширения

### Ключевые модули

| Модуль | Назначение |
|--------|------------|
| ИИА_Сервер | Управление диалогами, логирование, исправление ошибок |
| ИИА_Оркестратор | Серверный цикл: план → DSL → проверка → summary |
| ИИА_DSL | Интерпретатор JSON-команд (Domain Specific Language) |
| ИИА_Метаданные | Работа со структурой конфигурации, поиск объектов |
| ИИА_Промты | Генератор системных инструкций, динамический доменный промпт |
| ИИА_Провайдеры | Интеграция с LLM (OpenAI-совместимые API, настраиваемый порт/SSL) |
| ИИА_RAG_Поиск | RAG-поиск по метаданным конфигурации (TF-IDF + Embeddings rerank) |
| ИИА_RAG_Настройки | Синонимы, стоп-слова, настройки RAG |
| ИИА_RAG_Индексатор | Индексация чанков метаданных + эмбеддинги |
| ИИА_RAG_Эмбеддинги | **[НОВЫЙ]** Yandex Embeddings: получение векторов, cosine similarity, rerank |
| ИИА_ЯдроМетаданных | Ядро метаданных конфигурации (расширено объектами ERP 2) |

### DSL-команды (доступные действия агента)

**Чтение:**
- `GetMetadata` — списки объектов метаданных
- `GetObjectFields` — реквизиты и табличные части объекта
- `CheckObjectExists` — проверка существования объекта
- `RunQuery` — произвольный запрос на языке запросов 1С
- `FindReferenceByName` — поиск ссылки по наименованию
- `SelectObject` — загрузка объекта в контекст

**Запись:**
- `CreateReference` — создание элемента справочника
- `CreateDocument` — создание документа
- `SetField` — установка значения реквизита
- `Write` — запись объекта в базу

**Бонусные (Кейс 11):**
- `ImportPriceList` — загрузка прайс-листа из Excel (xlsx), запись в регистр ЦеныНоменклатуры
- `ParseFileForContractor` — извлечение реквизитов контрагента из файла (xlsx/pdf/docx/txt)

**Системные:**
- `ShowInfo` — сообщение пользователю
- `ForEach` — цикл по коллекции
- `SaveToStorage` / `LoadFromStorage` — временное хранилище диалога

### Динамический доменный промпт

Доменные знания для агента генерируются **динамически** из реальных метаданных конфигурации:
- `СформироватьДоменныйПромпт()` — собирает описания объектов (регистры цен, документы продаж, расчёты)
- `НайтиРегистрЦен()` — пробует `ЦеныНоменклатуры` → `ЦеныНоменклатуры25` → `ЦеныТоваров`
- `НайтиДокументПродаж()` — пробует `РеализацияТоваровУслуг` → `РасходнаяНакладная` → альтернативы
- `НайтиРегистрРасчетов()` — пробует `РасчетыСКлиентами` → `ВзаиморасчетыСКонтрагентами` → варианты

Промпт автоматически адаптируется к любой конфигурации (ERP 2, УТ 11, и т.д.) и содержит актуальные имена полей.

### RAG-система

Гибридный поиск:
1. **TF-IDF** (BM25-lite) на BSL — индексирует метаданные в регистры сведений 1С
2. **Yandex Embeddings** (опционально) — rerank top-20 кандидатов TF-IDF через cosine similarity
3. Итоговый скор: 30% TF-IDF + 70% cosine similarity

Синонимы добавлены в `ИИА_RAG_Настройки`:
- «дебиторка», «задолженность», «просрочка» → расчёты с клиентами
- «прайс», «прайслист» → цены номенклатуры
- «маржа», «маржинальность», «себестоимость» → прибыль, наценка
- «покупатель» → партнер, контрагент, клиент

### Хранение эмбеддингов

Регистр `ИИА_ЭмбеддингиЧанков`:
- Измерения: КлючЧанка (String 72)
- Ресурсы: Вектор (ХранилищеЗначения), МодельЭмбеддинга (String 50), ДатаИндекса (DateTime)

### Настройки Yandex Embeddings

В регистре `ИИА_НастройкиПользователей` нужны записи:
- `YandexEmbeddings_FolderId` — ID каталога Yandex Cloud
- `YandexEmbeddings_ApiKey` — API-ключ
- `YandexEmbeddings_Enabled` — включить/выключить (Истина/Ложь)

### Известные ограничения

1. **Серверные уведомления** требуют платформу 8.3.26+ — более ранние версии не поддерживаются.
2. **Безопасный режим** расширения должен быть снят.
3. ~~**Порт и SSL захардкожены**~~ — **ИСПРАВЛЕНО**: порт и SSL определяются из URL автоматически.

## Данные для демо

В базе ERP 2 нужны мок-данные:
- 50-100 контрагентов (Партнеры) с разной задолженностью и условиями оплаты
- 100-200 позиций номенклатуры (фарма/FMCG тематика)
- Прайс-лист с ценами продажи и закупочными ценами (для маржи)
- 500-1000 документов РеализацияТоваровУслуг за последние 6 месяцев
- Ситуации: клиенты с просрочкой, без просрочки, разные ценовые категории

## Команда

- **Дмитрий (Desko77):** 1С-разработчик, ведущий. Опыт: EDT, Claude Code, MCP, ERP-интеграции.
- **Аналитик:** подготовка данных, эталонные вопросы, презентация.

## Сроки

- Работа над проектом: ~3 недели (до 16.03.2026)
- Презентация жюри: 16-20.03.2026
- Итоги: 23-24.03.2026

## Структура репозитория

```
AI_agent/
├── xml/                    # XML-выгрузка расширения 1С
│   ├── CommonModules/      # Общие модули (BSL-код)
│   │   ├── ИИА_Промты/     # Системные промпты + динамический доменный блок
│   │   ├── ИИА_DSL/        # DSL-интерпретатор + ImportPriceList, ParseFileForContractor
│   │   ├── ИИА_Провайдеры/ # HTTP-провайдер (настраиваемый порт/SSL)
│   │   ├── ИИА_RAG_Эмбеддинги/ # Yandex Embeddings модуль
│   │   └── ...
│   ├── CommonForms/        # Формы (агент, настройки, RAG)
│   ├── Catalogs/           # Справочники (диалоги, настройки)
│   ├── InformationRegisters/ # Регистры (чанки, токены, логи, эмбеддинги)
│   └── Configuration.xml   # Корневой файл конфигурации
├── docs/                   # Документация
├── automation/             # Скрипты автоматизации (Python, тесты)
│   └── test_examples.py    # Тест-кейсы включая case11_* сценарии
├── assets/                 # Ресурсы (иконки)
└── CLAUDE.md               # ← этот файл
```

## Что делать при работе с кодом

- BSL-код расширения лежит в `xml/CommonModules/*/Ext/Module.bsl`
- Основная логика: ИИА_Оркестратор, ИИА_DSL, ИИА_Промты, ИИА_Провайдеры
- Доменный промпт генерируется динамически в `ИИА_Промты.СформироватьДоменныйПромпт()` — не хардкодить!
- RAG-настройки (синонимы, стоп-слова) в ИИА_RAG_Настройки
- Эмбеддинги — в ИИА_RAG_Эмбеддинги (Yandex API)
- Тесты: `automation/run_tests.py` или модуль ИИА_Тесты в 1С
- Группа тестов кейса 11: `case11` (case11_pricing, case11_sales_history, case11_receivables, case11_multi_source)

## Что сделано (Кейс 11)

- [x] Фаза 1: Динамический доменный промпт из метаданных конфигурации
- [x] Фаза 2: RAG-синонимы (10 шт) + расширение ядра метаданных ERP 2 (7 объектов)
- [x] Фаза 3: Патч HTTP-провайдера — настраиваемый порт и SSL для LM Studio
- [x] Фаза 6.2: ParseFileForContractor — создание контрагента из файла (xlsx/pdf/docx/txt)
- [x] Фаза 6.3: ImportPriceList — загрузка прайс-листа из Excel
- [x] Фаза 7: Yandex Embeddings — семантический rerank (TF-IDF + cosine similarity)
- [x] Фаза 4: Тестовые сценарии case11_* (4 шт)

## Что осталось перед демо

- [ ] RAG реиндекс (из формы ИИА_RAG) после загрузки расширения
- [ ] Настроить Yandex Embeddings (FolderId, ApiKey) в ИИА_НастройкиПользователей
- [ ] Проверить все сценарии на демо-данных
- [ ] Подготовить презентацию
